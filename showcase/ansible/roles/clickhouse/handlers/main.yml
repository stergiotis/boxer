---
# Restart only when a config.d drop-in actually changed (the daily ansible-pull
# must not bounce a running server every converge).
- name: Restart clickhouse-server
  ansible.builtin.systemd:
    name: clickhouse-server
    state: restarted
