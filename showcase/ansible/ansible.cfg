# Used when you run `ansible-playbook` from THIS directory by hand. ansible-pull
# does not auto-load it (it runs from the clone root), but roles_path defaults to
# the playbook-adjacent roles/ either way — so the pull path needs no config.
[defaults]
roles_path = roles
retry_files_enabled = False
stdout_callback = yaml
nocows = True
