\* Negative caching ON: the replay loop quiesces even though the upstream
\* lacks k2 — expect "No error has been found."
SPECIFICATION Spec
CONSTANTS
  k1 = k1
  k2 = k2
  i1 = i1
  i2 = i2
  Keys = {k1, k2}
  Items = {i1, i2}
  NegCache = TRUE
  needs <- NeedsDef
  upstream <- UpstreamDef
PROPERTY
  Quiescence
  SatisfiableDone
