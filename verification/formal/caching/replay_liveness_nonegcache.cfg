\* Negative caching OFF: the item needing the absent k2 re-queues on every
\* replay, forever, despite weak fairness on every action — expect TLC to
\* print the livelock lasso violating Quiescence. SatisfiableDone still
\* holds (checked in the ON config; here the violation aborts first).
SPECIFICATION Spec
CONSTANTS
  k1 = k1
  k2 = k2
  i1 = i1
  i2 = i2
  Keys = {k1, k2}
  Items = {i1, i2}
  NegCache = FALSE
  needs <- NeedsDef
  upstream <- UpstreamDef
PROPERTY
  Quiescence
