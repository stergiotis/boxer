# Entry-points audit baseline (CODINGSTANDARDS.md "Entry Points").
#
# Each non-empty, non-comment line is a fully-qualified package import
# path whose `package main` is grandfathered: `boxer dev entry-points`
# still lists it but its non-conformance does not fail the --strict gate
# wired into scripts/ci/lint.sh.
#
# A legitimate exception is grandfathered with a brief rationale comment
# immediately above its line. New ad-hoc `package main` declarations must
# NOT be added here as a workaround; fix them at the source (fold into
# ./boxer.sh as a subcommand). The lone exception today is the designlint
# vettool, which the `go vet -vettool=` protocol forces to be a standalone
# multichecker.Main binary rather than a boxer.sh subcommand.

# designlint — IDS Tier 1 mechanical-rule vettool (ADR-0029 §SD8). A
# `go vet -vettool=` driver must be a standalone unitchecker/multichecker
# binary, so it cannot fold into ./boxer.sh as a urfave/cli subcommand.
github.com/stergiotis/boxer/public/keelson/designsystem/lint/cmd/designlint
