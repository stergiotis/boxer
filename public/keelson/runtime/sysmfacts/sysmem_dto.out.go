// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package sysmfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindSysmKindMem           uint64 = 2305843022098595842
	kindSysmMemHost           uint64 = 2305843022098595845
	kindSysmMemTotalBytes     uint64 = 2305843022098595856
	kindSysmMemFreeBytes      uint64 = 2305843022098595857
	kindSysmMemAvailableBytes uint64 = 2305843022098595858
	kindSysmMemBuffersBytes   uint64 = 2305843022098595859
	kindSysmMemCachedBytes    uint64 = 2305843022098595860
	kindSysmMemSwapTotalBytes uint64 = 2305843022098595861
	kindSysmMemSwapFreeBytes  uint64 = 2305843022098595862
	kindSysmMemUsedBytes      uint64 = 2305843022098595863
	kindSysmMemSwapUsedBytes  uint64 = 2305843022098595864
	kindSysmMemArcSizeBytes   uint64 = 2305843022098595865
	kindSysmMemArcMinBytes    uint64 = 2305843022098595866
)

// sysMemSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysMemSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// sysMemSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysMemSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// sysMemU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysMemU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// sysMemU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysMemU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// sysMemEntityI is the entity-builder surface sysMemAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type sysMemEntityI[
	SymbolAttr sysMemSymbolAttrI,
	SymbolSec sysMemSymbolSecI[SymbolAttr, Ent],
	U64ArrayAttr sysMemU64ArrayAttrI,
	U64ArraySec sysMemU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionU64Array() U64ArraySec
}

// sysMemAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func sysMemAddSections[
	SymbolAttr sysMemSymbolAttrI,
	SymbolSec sysMemSymbolSecI[SymbolAttr, Ent],
	U64ArrayAttr sysMemU64ArrayAttrI,
	U64ArraySec sysMemU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
	DML sysMemEntityI[
		SymbolAttr, SymbolSec,
		U64ArrayAttr, U64ArraySec,
		Ent,
	],
](dml DML, row SysMem) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindSysmKindMem)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Host := symbolSec.BeginAttribute(row.Host)
	symbolSecAttr_Host.AddMembershipLowCardRefP(kindSysmMemHost)
	symbolSecAttr_Host.EndAttributeP()
	symbolSec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	u64ArraySecAttr_TotalBytes := u64ArraySec.BeginAttributeSingle(row.TotalBytes)
	u64ArraySecAttr_TotalBytes.AddMembershipLowCardRefP(kindSysmMemTotalBytes)
	u64ArraySecAttr_TotalBytes.EndAttributeP()
	u64ArraySecAttr_FreeBytes := u64ArraySec.BeginAttributeSingle(row.FreeBytes)
	u64ArraySecAttr_FreeBytes.AddMembershipLowCardRefP(kindSysmMemFreeBytes)
	u64ArraySecAttr_FreeBytes.EndAttributeP()
	u64ArraySecAttr_AvailableBytes := u64ArraySec.BeginAttributeSingle(row.AvailableBytes)
	u64ArraySecAttr_AvailableBytes.AddMembershipLowCardRefP(kindSysmMemAvailableBytes)
	u64ArraySecAttr_AvailableBytes.EndAttributeP()
	u64ArraySecAttr_BuffersBytes := u64ArraySec.BeginAttributeSingle(row.BuffersBytes)
	u64ArraySecAttr_BuffersBytes.AddMembershipLowCardRefP(kindSysmMemBuffersBytes)
	u64ArraySecAttr_BuffersBytes.EndAttributeP()
	u64ArraySecAttr_CachedBytes := u64ArraySec.BeginAttributeSingle(row.CachedBytes)
	u64ArraySecAttr_CachedBytes.AddMembershipLowCardRefP(kindSysmMemCachedBytes)
	u64ArraySecAttr_CachedBytes.EndAttributeP()
	u64ArraySecAttr_SwapTotalBytes := u64ArraySec.BeginAttributeSingle(row.SwapTotalBytes)
	u64ArraySecAttr_SwapTotalBytes.AddMembershipLowCardRefP(kindSysmMemSwapTotalBytes)
	u64ArraySecAttr_SwapTotalBytes.EndAttributeP()
	u64ArraySecAttr_SwapFreeBytes := u64ArraySec.BeginAttributeSingle(row.SwapFreeBytes)
	u64ArraySecAttr_SwapFreeBytes.AddMembershipLowCardRefP(kindSysmMemSwapFreeBytes)
	u64ArraySecAttr_SwapFreeBytes.EndAttributeP()
	u64ArraySecAttr_UsedBytes := u64ArraySec.BeginAttributeSingle(row.UsedBytes)
	u64ArraySecAttr_UsedBytes.AddMembershipLowCardRefP(kindSysmMemUsedBytes)
	u64ArraySecAttr_UsedBytes.EndAttributeP()
	u64ArraySecAttr_SwapUsedBytes := u64ArraySec.BeginAttributeSingle(row.SwapUsedBytes)
	u64ArraySecAttr_SwapUsedBytes.AddMembershipLowCardRefP(kindSysmMemSwapUsedBytes)
	u64ArraySecAttr_SwapUsedBytes.EndAttributeP()
	u64ArraySecAttr_ARCSizeBytes := u64ArraySec.BeginAttributeSingle(row.ARCSizeBytes)
	u64ArraySecAttr_ARCSizeBytes.AddMembershipLowCardRefP(kindSysmMemArcSizeBytes)
	u64ArraySecAttr_ARCSizeBytes.EndAttributeP()
	u64ArraySecAttr_ARCMinBytes := u64ArraySec.BeginAttributeSingle(row.ARCMinBytes)
	u64ArraySecAttr_ARCMinBytes.AddMembershipLowCardRefP(kindSysmMemArcMinBytes)
	u64ArraySecAttr_ARCMinBytes.EndAttributeP()
	u64ArraySec.EndSection()
	return
}

// sysMemSymbolAttrsReadI is the Attributes-side view of the symbol section.
type sysMemSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysMemSymbolMembsReadI is the Memberships-side view of the symbol section.
type sysMemSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysMemU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type sysMemU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysMemU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type sysMemU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysMemReadRow reads row i as one optional SysMem component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func sysMemReadRow[
	SymbolAttrs sysMemSymbolAttrsReadI,
	SymbolMembs sysMemSymbolMembsReadI,
	U64ArrayAttrs sysMemU64ArrayAttrsReadI,
	U64ArrayMembs sysMemU64ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
) (row SysMem, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolHostVal string
	var symbolHostCount int
	var symbolHostLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmKindMem:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindSysmMemHost:
				if symbolHostLastAttr != attrJ+1 {
					symbolHostLastAttr = attrJ + 1
					symbolHostCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolHostVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmKindMem").Int("got", symbolKindCount).Errorf("slot symbol@sysmKindMem (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolHostCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmMemHost").Int("got", symbolHostCount).Errorf("slot symbol@sysmMemHost (field Host) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolHostCount)
		return
	}
	if symbolHostCount == 1 {
		row.Host = symbolHostVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayTotalBytesVal uint64
	var u64ArrayTotalBytesCount int
	var u64ArrayTotalBytesLastAttr int64
	var u64ArrayFreeBytesVal uint64
	var u64ArrayFreeBytesCount int
	var u64ArrayFreeBytesLastAttr int64
	var u64ArrayAvailableBytesVal uint64
	var u64ArrayAvailableBytesCount int
	var u64ArrayAvailableBytesLastAttr int64
	var u64ArrayBuffersBytesVal uint64
	var u64ArrayBuffersBytesCount int
	var u64ArrayBuffersBytesLastAttr int64
	var u64ArrayCachedBytesVal uint64
	var u64ArrayCachedBytesCount int
	var u64ArrayCachedBytesLastAttr int64
	var u64ArraySwapTotalBytesVal uint64
	var u64ArraySwapTotalBytesCount int
	var u64ArraySwapTotalBytesLastAttr int64
	var u64ArraySwapFreeBytesVal uint64
	var u64ArraySwapFreeBytesCount int
	var u64ArraySwapFreeBytesLastAttr int64
	var u64ArrayUsedBytesVal uint64
	var u64ArrayUsedBytesCount int
	var u64ArrayUsedBytesLastAttr int64
	var u64ArraySwapUsedBytesVal uint64
	var u64ArraySwapUsedBytesCount int
	var u64ArraySwapUsedBytesLastAttr int64
	var u64ArrayARCSizeBytesVal uint64
	var u64ArrayARCSizeBytesCount int
	var u64ArrayARCSizeBytesLastAttr int64
	var u64ArrayARCMinBytesVal uint64
	var u64ArrayARCMinBytesCount int
	var u64ArrayARCMinBytesLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmMemTotalBytes:
				if u64ArrayTotalBytesLastAttr != attrJ+1 {
					u64ArrayTotalBytesLastAttr = attrJ + 1
					u64ArrayTotalBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemTotalBytes").Str("field", "TotalBytes").Errorf("slot u64Array@sysmMemTotalBytes (field TotalBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayTotalBytesVal = val
			case kindSysmMemFreeBytes:
				if u64ArrayFreeBytesLastAttr != attrJ+1 {
					u64ArrayFreeBytesLastAttr = attrJ + 1
					u64ArrayFreeBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemFreeBytes").Str("field", "FreeBytes").Errorf("slot u64Array@sysmMemFreeBytes (field FreeBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayFreeBytesVal = val
			case kindSysmMemAvailableBytes:
				if u64ArrayAvailableBytesLastAttr != attrJ+1 {
					u64ArrayAvailableBytesLastAttr = attrJ + 1
					u64ArrayAvailableBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemAvailableBytes").Str("field", "AvailableBytes").Errorf("slot u64Array@sysmMemAvailableBytes (field AvailableBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayAvailableBytesVal = val
			case kindSysmMemBuffersBytes:
				if u64ArrayBuffersBytesLastAttr != attrJ+1 {
					u64ArrayBuffersBytesLastAttr = attrJ + 1
					u64ArrayBuffersBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemBuffersBytes").Str("field", "BuffersBytes").Errorf("slot u64Array@sysmMemBuffersBytes (field BuffersBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayBuffersBytesVal = val
			case kindSysmMemCachedBytes:
				if u64ArrayCachedBytesLastAttr != attrJ+1 {
					u64ArrayCachedBytesLastAttr = attrJ + 1
					u64ArrayCachedBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemCachedBytes").Str("field", "CachedBytes").Errorf("slot u64Array@sysmMemCachedBytes (field CachedBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayCachedBytesVal = val
			case kindSysmMemSwapTotalBytes:
				if u64ArraySwapTotalBytesLastAttr != attrJ+1 {
					u64ArraySwapTotalBytesLastAttr = attrJ + 1
					u64ArraySwapTotalBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemSwapTotalBytes").Str("field", "SwapTotalBytes").Errorf("slot u64Array@sysmMemSwapTotalBytes (field SwapTotalBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArraySwapTotalBytesVal = val
			case kindSysmMemSwapFreeBytes:
				if u64ArraySwapFreeBytesLastAttr != attrJ+1 {
					u64ArraySwapFreeBytesLastAttr = attrJ + 1
					u64ArraySwapFreeBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemSwapFreeBytes").Str("field", "SwapFreeBytes").Errorf("slot u64Array@sysmMemSwapFreeBytes (field SwapFreeBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArraySwapFreeBytesVal = val
			case kindSysmMemUsedBytes:
				if u64ArrayUsedBytesLastAttr != attrJ+1 {
					u64ArrayUsedBytesLastAttr = attrJ + 1
					u64ArrayUsedBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemUsedBytes").Str("field", "UsedBytes").Errorf("slot u64Array@sysmMemUsedBytes (field UsedBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayUsedBytesVal = val
			case kindSysmMemSwapUsedBytes:
				if u64ArraySwapUsedBytesLastAttr != attrJ+1 {
					u64ArraySwapUsedBytesLastAttr = attrJ + 1
					u64ArraySwapUsedBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemSwapUsedBytes").Str("field", "SwapUsedBytes").Errorf("slot u64Array@sysmMemSwapUsedBytes (field SwapUsedBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArraySwapUsedBytesVal = val
			case kindSysmMemArcSizeBytes:
				if u64ArrayARCSizeBytesLastAttr != attrJ+1 {
					u64ArrayARCSizeBytesLastAttr = attrJ + 1
					u64ArrayARCSizeBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemArcSizeBytes").Str("field", "ARCSizeBytes").Errorf("slot u64Array@sysmMemArcSizeBytes (field ARCSizeBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayARCSizeBytesVal = val
			case kindSysmMemArcMinBytes:
				if u64ArrayARCMinBytesLastAttr != attrJ+1 {
					u64ArrayARCMinBytesLastAttr = attrJ + 1
					u64ArrayARCMinBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemArcMinBytes").Str("field", "ARCMinBytes").Errorf("slot u64Array@sysmMemArcMinBytes (field ARCMinBytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayARCMinBytesVal = val
			}
		}
	}
	if u64ArrayTotalBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemTotalBytes").Int("got", u64ArrayTotalBytesCount).Errorf("slot u64Array@sysmMemTotalBytes (field TotalBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayTotalBytesCount)
		return
	}
	if u64ArrayTotalBytesCount == 1 {
		row.TotalBytes = u64ArrayTotalBytesVal
		present = true
	}
	if u64ArrayFreeBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemFreeBytes").Int("got", u64ArrayFreeBytesCount).Errorf("slot u64Array@sysmMemFreeBytes (field FreeBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayFreeBytesCount)
		return
	}
	if u64ArrayFreeBytesCount == 1 {
		row.FreeBytes = u64ArrayFreeBytesVal
		present = true
	}
	if u64ArrayAvailableBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemAvailableBytes").Int("got", u64ArrayAvailableBytesCount).Errorf("slot u64Array@sysmMemAvailableBytes (field AvailableBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayAvailableBytesCount)
		return
	}
	if u64ArrayAvailableBytesCount == 1 {
		row.AvailableBytes = u64ArrayAvailableBytesVal
		present = true
	}
	if u64ArrayBuffersBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemBuffersBytes").Int("got", u64ArrayBuffersBytesCount).Errorf("slot u64Array@sysmMemBuffersBytes (field BuffersBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayBuffersBytesCount)
		return
	}
	if u64ArrayBuffersBytesCount == 1 {
		row.BuffersBytes = u64ArrayBuffersBytesVal
		present = true
	}
	if u64ArrayCachedBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemCachedBytes").Int("got", u64ArrayCachedBytesCount).Errorf("slot u64Array@sysmMemCachedBytes (field CachedBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayCachedBytesCount)
		return
	}
	if u64ArrayCachedBytesCount == 1 {
		row.CachedBytes = u64ArrayCachedBytesVal
		present = true
	}
	if u64ArraySwapTotalBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemSwapTotalBytes").Int("got", u64ArraySwapTotalBytesCount).Errorf("slot u64Array@sysmMemSwapTotalBytes (field SwapTotalBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArraySwapTotalBytesCount)
		return
	}
	if u64ArraySwapTotalBytesCount == 1 {
		row.SwapTotalBytes = u64ArraySwapTotalBytesVal
		present = true
	}
	if u64ArraySwapFreeBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemSwapFreeBytes").Int("got", u64ArraySwapFreeBytesCount).Errorf("slot u64Array@sysmMemSwapFreeBytes (field SwapFreeBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArraySwapFreeBytesCount)
		return
	}
	if u64ArraySwapFreeBytesCount == 1 {
		row.SwapFreeBytes = u64ArraySwapFreeBytesVal
		present = true
	}
	if u64ArrayUsedBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemUsedBytes").Int("got", u64ArrayUsedBytesCount).Errorf("slot u64Array@sysmMemUsedBytes (field UsedBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayUsedBytesCount)
		return
	}
	if u64ArrayUsedBytesCount == 1 {
		row.UsedBytes = u64ArrayUsedBytesVal
		present = true
	}
	if u64ArraySwapUsedBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemSwapUsedBytes").Int("got", u64ArraySwapUsedBytesCount).Errorf("slot u64Array@sysmMemSwapUsedBytes (field SwapUsedBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArraySwapUsedBytesCount)
		return
	}
	if u64ArraySwapUsedBytesCount == 1 {
		row.SwapUsedBytes = u64ArraySwapUsedBytesVal
		present = true
	}
	if u64ArrayARCSizeBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemArcSizeBytes").Int("got", u64ArrayARCSizeBytesCount).Errorf("slot u64Array@sysmMemArcSizeBytes (field ARCSizeBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayARCSizeBytesCount)
		return
	}
	if u64ArrayARCSizeBytesCount == 1 {
		row.ARCSizeBytes = u64ArrayARCSizeBytesVal
		present = true
	}
	if u64ArrayARCMinBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmMemArcMinBytes").Int("got", u64ArrayARCMinBytesCount).Errorf("slot u64Array@sysmMemArcMinBytes (field ARCMinBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayARCMinBytesCount)
		return
	}
	if u64ArrayARCMinBytesCount == 1 {
		row.ARCMinBytes = u64ArrayARCMinBytesVal
		present = true
	}
	return
}
