// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package sysmfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindSysmKindGpu             uint64 = 2305843022098595934
	kindSysmGpuHost             uint64 = 2305843022098595935
	kindSysmGpuVendor           uint64 = 2305843022098595936
	kindSysmGpuIndex            uint64 = 2305843022098595937
	kindSysmGpuName             uint64 = 2305843022098595938
	kindSysmGpuPciId            uint64 = 2305843022098595939
	kindSysmGpuBusyPct          uint64 = 2305843022098595940
	kindSysmGpuMemoryUsedBytes  uint64 = 2305843022098595941
	kindSysmGpuMemoryTotalBytes uint64 = 2305843022098595942
	kindSysmGpuPowerWatts       uint64 = 2305843022098595943
	kindSysmGpuTempC            uint64 = 2305843022098595944
	kindSysmGpuFreqMhz          uint64 = 2305843022098595945
)

// sysGpuSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysGpuSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// sysGpuSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysGpuSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// sysGpuSymbolArrayAttrI is the InAttr-side view of the symbolArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysGpuSymbolArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value string)
	EndAttributeP()
}

// sysGpuSymbolArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysGpuSymbolArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysGpuI32ArrayAttrI is the InAttr-side view of the i32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysGpuI32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value int32)
	EndAttributeP()
}

// sysGpuI32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysGpuI32ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysGpuU8ArrayAttrI is the InAttr-side view of the u8Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysGpuU8ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint8)
	EndAttributeP()
}

// sysGpuU8ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysGpuU8ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysGpuU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysGpuU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint64)
	EndAttributeP()
}

// sysGpuU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysGpuU64ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysGpuF32ArrayAttrI is the InAttr-side view of the f32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysGpuF32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value float32)
	EndAttributeP()
}

// sysGpuF32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysGpuF32ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysGpuU32ArrayAttrI is the InAttr-side view of the u32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysGpuU32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint32)
	EndAttributeP()
}

// sysGpuU32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysGpuU32ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysGpuEntityI is the entity-builder surface sysGpuAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type sysGpuEntityI[
	SymbolAttr sysGpuSymbolAttrI,
	SymbolSec sysGpuSymbolSecI[SymbolAttr, Ent],
	SymbolArrayAttr sysGpuSymbolArrayAttrI,
	SymbolArraySec sysGpuSymbolArraySecI[SymbolArrayAttr, Ent],
	I32ArrayAttr sysGpuI32ArrayAttrI,
	I32ArraySec sysGpuI32ArraySecI[I32ArrayAttr, Ent],
	U8ArrayAttr sysGpuU8ArrayAttrI,
	U8ArraySec sysGpuU8ArraySecI[U8ArrayAttr, Ent],
	U64ArrayAttr sysGpuU64ArrayAttrI,
	U64ArraySec sysGpuU64ArraySecI[U64ArrayAttr, Ent],
	F32ArrayAttr sysGpuF32ArrayAttrI,
	F32ArraySec sysGpuF32ArraySecI[F32ArrayAttr, Ent],
	U32ArrayAttr sysGpuU32ArrayAttrI,
	U32ArraySec sysGpuU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionSymbolArray() SymbolArraySec
	GetSectionI32Array() I32ArraySec
	GetSectionU8Array() U8ArraySec
	GetSectionU64Array() U64ArraySec
	GetSectionF32Array() F32ArraySec
	GetSectionU32Array() U32ArraySec
}

// sysGpuEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func sysGpuEmitSectionSymbol[
	SymbolAttr sysGpuSymbolAttrI,
	SymbolSec sysGpuSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row SysGpu) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindSysmKindGpu)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Host := symbolSec.BeginAttribute(row.Host)
	symbolSecAttr_Host.AddMembershipLowCardRefP(kindSysmGpuHost)
	symbolSecAttr_Host.EndAttributeP()
	return
}

// sysGpuEmitSectionSymbolArray writes this kind's symbolArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func sysGpuEmitSectionSymbolArray[
	SymbolArrayAttr sysGpuSymbolArrayAttrI,
	SymbolArraySec sysGpuSymbolArraySecI[SymbolArrayAttr, Ent],
	Ent any,
](symbolArraySec SymbolArraySec, row SysGpu) (err error) {
	if len(row.Vendor) > 0 {
		symbolArraySecAttr_Vendor := symbolArraySec.BeginAttribute()
		for _, v := range row.Vendor {
			symbolArraySecAttr_Vendor.AddToContainerP(v)
		}
		symbolArraySecAttr_Vendor.AddMembershipLowCardRefP(kindSysmGpuVendor)
		symbolArraySecAttr_Vendor.EndAttributeP()
	}
	if len(row.Name) > 0 {
		symbolArraySecAttr_Name := symbolArraySec.BeginAttribute()
		for _, v := range row.Name {
			symbolArraySecAttr_Name.AddToContainerP(v)
		}
		symbolArraySecAttr_Name.AddMembershipLowCardRefP(kindSysmGpuName)
		symbolArraySecAttr_Name.EndAttributeP()
	}
	if len(row.PCIID) > 0 {
		symbolArraySecAttr_PCIID := symbolArraySec.BeginAttribute()
		for _, v := range row.PCIID {
			symbolArraySecAttr_PCIID.AddToContainerP(v)
		}
		symbolArraySecAttr_PCIID.AddMembershipLowCardRefP(kindSysmGpuPciId)
		symbolArraySecAttr_PCIID.EndAttributeP()
	}
	return
}

// sysGpuEmitSectionI32Array writes this kind's i32Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func sysGpuEmitSectionI32Array[
	I32ArrayAttr sysGpuI32ArrayAttrI,
	I32ArraySec sysGpuI32ArraySecI[I32ArrayAttr, Ent],
	Ent any,
](i32ArraySec I32ArraySec, row SysGpu) (err error) {
	if len(row.Index) > 0 {
		i32ArraySecAttr_Index := i32ArraySec.BeginAttribute()
		for _, v := range row.Index {
			i32ArraySecAttr_Index.AddToContainerP(v)
		}
		i32ArraySecAttr_Index.AddMembershipLowCardRefP(kindSysmGpuIndex)
		i32ArraySecAttr_Index.EndAttributeP()
	}
	return
}

// sysGpuEmitSectionU8Array writes this kind's u8Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func sysGpuEmitSectionU8Array[
	U8ArrayAttr sysGpuU8ArrayAttrI,
	U8ArraySec sysGpuU8ArraySecI[U8ArrayAttr, Ent],
	Ent any,
](u8ArraySec U8ArraySec, row SysGpu) (err error) {
	if len(row.BusyPercent) > 0 {
		u8ArraySecAttr_BusyPercent := u8ArraySec.BeginAttribute()
		for _, v := range row.BusyPercent {
			u8ArraySecAttr_BusyPercent.AddToContainerP(v)
		}
		u8ArraySecAttr_BusyPercent.AddMembershipLowCardRefP(kindSysmGpuBusyPct)
		u8ArraySecAttr_BusyPercent.EndAttributeP()
	}
	return
}

// sysGpuEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func sysGpuEmitSectionU64Array[
	U64ArrayAttr sysGpuU64ArrayAttrI,
	U64ArraySec sysGpuU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row SysGpu) (err error) {
	if len(row.MemoryUsedBytes) > 0 {
		u64ArraySecAttr_MemoryUsedBytes := u64ArraySec.BeginAttribute()
		for _, v := range row.MemoryUsedBytes {
			u64ArraySecAttr_MemoryUsedBytes.AddToContainerP(v)
		}
		u64ArraySecAttr_MemoryUsedBytes.AddMembershipLowCardRefP(kindSysmGpuMemoryUsedBytes)
		u64ArraySecAttr_MemoryUsedBytes.EndAttributeP()
	}
	if len(row.MemoryTotalBytes) > 0 {
		u64ArraySecAttr_MemoryTotalBytes := u64ArraySec.BeginAttribute()
		for _, v := range row.MemoryTotalBytes {
			u64ArraySecAttr_MemoryTotalBytes.AddToContainerP(v)
		}
		u64ArraySecAttr_MemoryTotalBytes.AddMembershipLowCardRefP(kindSysmGpuMemoryTotalBytes)
		u64ArraySecAttr_MemoryTotalBytes.EndAttributeP()
	}
	return
}

// sysGpuEmitSectionF32Array writes this kind's f32Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func sysGpuEmitSectionF32Array[
	F32ArrayAttr sysGpuF32ArrayAttrI,
	F32ArraySec sysGpuF32ArraySecI[F32ArrayAttr, Ent],
	Ent any,
](f32ArraySec F32ArraySec, row SysGpu) (err error) {
	if len(row.PowerWatts) > 0 {
		f32ArraySecAttr_PowerWatts := f32ArraySec.BeginAttribute()
		for _, v := range row.PowerWatts {
			f32ArraySecAttr_PowerWatts.AddToContainerP(v)
		}
		f32ArraySecAttr_PowerWatts.AddMembershipLowCardRefP(kindSysmGpuPowerWatts)
		f32ArraySecAttr_PowerWatts.EndAttributeP()
	}
	if len(row.TempC) > 0 {
		f32ArraySecAttr_TempC := f32ArraySec.BeginAttribute()
		for _, v := range row.TempC {
			f32ArraySecAttr_TempC.AddToContainerP(v)
		}
		f32ArraySecAttr_TempC.AddMembershipLowCardRefP(kindSysmGpuTempC)
		f32ArraySecAttr_TempC.EndAttributeP()
	}
	return
}

// sysGpuEmitSectionU32Array writes this kind's u32Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func sysGpuEmitSectionU32Array[
	U32ArrayAttr sysGpuU32ArrayAttrI,
	U32ArraySec sysGpuU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
](u32ArraySec U32ArraySec, row SysGpu) (err error) {
	if len(row.FreqMHz) > 0 {
		u32ArraySecAttr_FreqMHz := u32ArraySec.BeginAttribute()
		for _, v := range row.FreqMHz {
			u32ArraySecAttr_FreqMHz.AddToContainerP(v)
		}
		u32ArraySecAttr_FreqMHz.AddMembershipLowCardRefP(kindSysmGpuFreqMhz)
		u32ArraySecAttr_FreqMHz.EndAttributeP()
	}
	return
}

// sysGpuAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func sysGpuAddSections[
	SymbolAttr sysGpuSymbolAttrI,
	SymbolSec sysGpuSymbolSecI[SymbolAttr, Ent],
	SymbolArrayAttr sysGpuSymbolArrayAttrI,
	SymbolArraySec sysGpuSymbolArraySecI[SymbolArrayAttr, Ent],
	I32ArrayAttr sysGpuI32ArrayAttrI,
	I32ArraySec sysGpuI32ArraySecI[I32ArrayAttr, Ent],
	U8ArrayAttr sysGpuU8ArrayAttrI,
	U8ArraySec sysGpuU8ArraySecI[U8ArrayAttr, Ent],
	U64ArrayAttr sysGpuU64ArrayAttrI,
	U64ArraySec sysGpuU64ArraySecI[U64ArrayAttr, Ent],
	F32ArrayAttr sysGpuF32ArrayAttrI,
	F32ArraySec sysGpuF32ArraySecI[F32ArrayAttr, Ent],
	U32ArrayAttr sysGpuU32ArrayAttrI,
	U32ArraySec sysGpuU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
	DML sysGpuEntityI[
		SymbolAttr, SymbolSec,
		SymbolArrayAttr, SymbolArraySec,
		I32ArrayAttr, I32ArraySec,
		U8ArrayAttr, U8ArraySec,
		U64ArrayAttr, U64ArraySec,
		F32ArrayAttr, F32ArraySec,
		U32ArrayAttr, U32ArraySec,
		Ent,
	],
](dml DML, row SysGpu) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = sysGpuEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- symbolArray. ---
	symbolArraySec := dml.GetSectionSymbolArray()
	err = sysGpuEmitSectionSymbolArray(symbolArraySec, row)
	if err != nil {
		return
	}
	symbolArraySec.EndSection()
	// --- i32Array. ---
	i32ArraySec := dml.GetSectionI32Array()
	err = sysGpuEmitSectionI32Array(i32ArraySec, row)
	if err != nil {
		return
	}
	i32ArraySec.EndSection()
	// --- u8Array. ---
	u8ArraySec := dml.GetSectionU8Array()
	err = sysGpuEmitSectionU8Array(u8ArraySec, row)
	if err != nil {
		return
	}
	u8ArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = sysGpuEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	// --- f32Array. ---
	f32ArraySec := dml.GetSectionF32Array()
	err = sysGpuEmitSectionF32Array(f32ArraySec, row)
	if err != nil {
		return
	}
	f32ArraySec.EndSection()
	// --- u32Array. ---
	u32ArraySec := dml.GetSectionU32Array()
	err = sysGpuEmitSectionU32Array(u32ArraySec, row)
	if err != nil {
		return
	}
	u32ArraySec.EndSection()
	return
}

// sysGpuSymbolAttrsReadI is the Attributes-side view of the symbol section.
type sysGpuSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysGpuSymbolMembsReadI is the Memberships-side view of the symbol section.
type sysGpuSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysGpuSymbolArrayAttrsReadI is the Attributes-side view of the symbolArray section.
type sysGpuSymbolArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[string]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysGpuSymbolArrayMembsReadI is the Memberships-side view of the symbolArray section.
type sysGpuSymbolArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysGpuI32ArrayAttrsReadI is the Attributes-side view of the i32Array section.
type sysGpuI32ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[int32]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysGpuI32ArrayMembsReadI is the Memberships-side view of the i32Array section.
type sysGpuI32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysGpuU8ArrayAttrsReadI is the Attributes-side view of the u8Array section.
type sysGpuU8ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint8]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysGpuU8ArrayMembsReadI is the Memberships-side view of the u8Array section.
type sysGpuU8ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysGpuU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type sysGpuU64ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysGpuU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type sysGpuU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysGpuF32ArrayAttrsReadI is the Attributes-side view of the f32Array section.
type sysGpuF32ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[float32]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysGpuF32ArrayMembsReadI is the Memberships-side view of the f32Array section.
type sysGpuF32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysGpuU32ArrayAttrsReadI is the Attributes-side view of the u32Array section.
type sysGpuU32ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint32]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysGpuU32ArrayMembsReadI is the Memberships-side view of the u32Array section.
type sysGpuU32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysGpuReadRow reads row i as one optional SysGpu component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func sysGpuReadRow[
	SymbolAttrs sysGpuSymbolAttrsReadI,
	SymbolMembs sysGpuSymbolMembsReadI,
	SymbolArrayAttrs sysGpuSymbolArrayAttrsReadI,
	SymbolArrayMembs sysGpuSymbolArrayMembsReadI,
	I32ArrayAttrs sysGpuI32ArrayAttrsReadI,
	I32ArrayMembs sysGpuI32ArrayMembsReadI,
	U8ArrayAttrs sysGpuU8ArrayAttrsReadI,
	U8ArrayMembs sysGpuU8ArrayMembsReadI,
	U64ArrayAttrs sysGpuU64ArrayAttrsReadI,
	U64ArrayMembs sysGpuU64ArrayMembsReadI,
	F32ArrayAttrs sysGpuF32ArrayAttrsReadI,
	F32ArrayMembs sysGpuF32ArrayMembsReadI,
	U32ArrayAttrs sysGpuU32ArrayAttrsReadI,
	U32ArrayMembs sysGpuU32ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	symbolArrayAttrs SymbolArrayAttrs,
	symbolArrayMembs SymbolArrayMembs,
	i32ArrayAttrs I32ArrayAttrs,
	i32ArrayMembs I32ArrayMembs,
	u8ArrayAttrs U8ArrayAttrs,
	u8ArrayMembs U8ArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
	f32ArrayAttrs F32ArrayAttrs,
	f32ArrayMembs F32ArrayMembs,
	u32ArrayAttrs U32ArrayAttrs,
	u32ArrayMembs U32ArrayMembs,
) (row SysGpu, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolHostVal string
	var symbolHostCount int
	var symbolHostLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmKindGpu:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindSysmGpuHost:
				if symbolHostLastAttr != attrJ+1 {
					symbolHostLastAttr = attrJ + 1
					symbolHostCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolHostVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmKindGpu").Int("got", symbolKindCount).Errorf("slot symbol@sysmKindGpu (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolHostCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmGpuHost").Int("got", symbolHostCount).Errorf("slot symbol@sysmGpuHost (field Host) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolHostCount)
		return
	}
	if symbolHostCount == 1 {
		row.Host = symbolHostVal
		present = true
	}
	// --- symbolArray. ---
	var symbolArrayVendorSlice []string
	var symbolArrayVendorCount int
	var symbolArrayVendorLastAttr int64
	var symbolArrayNameSlice []string
	var symbolArrayNameCount int
	var symbolArrayNameLastAttr int64
	var symbolArrayPCIIDSlice []string
	var symbolArrayPCIIDCount int
	var symbolArrayPCIIDLastAttr int64
	nsymbolArray := symbolArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbolArray; attrJ++ {
		for membID := range symbolArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmGpuVendor:
				if symbolArrayVendorLastAttr != attrJ+1 {
					symbolArrayVendorLastAttr = attrJ + 1
					symbolArrayVendorCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayVendorSlice = append(symbolArrayVendorSlice, v)
				}
			case kindSysmGpuName:
				if symbolArrayNameLastAttr != attrJ+1 {
					symbolArrayNameLastAttr = attrJ + 1
					symbolArrayNameCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayNameSlice = append(symbolArrayNameSlice, v)
				}
			case kindSysmGpuPciId:
				if symbolArrayPCIIDLastAttr != attrJ+1 {
					symbolArrayPCIIDLastAttr = attrJ + 1
					symbolArrayPCIIDCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayPCIIDSlice = append(symbolArrayPCIIDSlice, v)
				}
			}
		}
	}
	if symbolArrayVendorCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "sysmGpuVendor").Int("got", symbolArrayVendorCount).Errorf("slot symbolArray@sysmGpuVendor (field Vendor) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayVendorCount)
		return
	}
	if symbolArrayVendorSlice != nil {
		row.Vendor = symbolArrayVendorSlice
		present = true
	}
	if symbolArrayNameCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "sysmGpuName").Int("got", symbolArrayNameCount).Errorf("slot symbolArray@sysmGpuName (field Name) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayNameCount)
		return
	}
	if symbolArrayNameSlice != nil {
		row.Name = symbolArrayNameSlice
		present = true
	}
	if symbolArrayPCIIDCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "sysmGpuPciId").Int("got", symbolArrayPCIIDCount).Errorf("slot symbolArray@sysmGpuPciId (field PCIID) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayPCIIDCount)
		return
	}
	if symbolArrayPCIIDSlice != nil {
		row.PCIID = symbolArrayPCIIDSlice
		present = true
	}
	// --- i32Array. ---
	var i32ArrayIndexSlice []int32
	var i32ArrayIndexCount int
	var i32ArrayIndexLastAttr int64
	ni32Array := i32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < ni32Array; attrJ++ {
		for membID := range i32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmGpuIndex:
				if i32ArrayIndexLastAttr != attrJ+1 {
					i32ArrayIndexLastAttr = attrJ + 1
					i32ArrayIndexCount++
				}
				for v := range i32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					i32ArrayIndexSlice = append(i32ArrayIndexSlice, v)
				}
			}
		}
	}
	if i32ArrayIndexCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "i32Array").Str("membership", "sysmGpuIndex").Int("got", i32ArrayIndexCount).Errorf("slot i32Array@sysmGpuIndex (field Index) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", i32ArrayIndexCount)
		return
	}
	if i32ArrayIndexSlice != nil {
		row.Index = i32ArrayIndexSlice
		present = true
	}
	// --- u8Array. ---
	var u8ArrayBusyPercentSlice []uint8
	var u8ArrayBusyPercentCount int
	var u8ArrayBusyPercentLastAttr int64
	nu8Array := u8ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu8Array; attrJ++ {
		for membID := range u8ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmGpuBusyPct:
				if u8ArrayBusyPercentLastAttr != attrJ+1 {
					u8ArrayBusyPercentLastAttr = attrJ + 1
					u8ArrayBusyPercentCount++
				}
				for v := range u8ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u8ArrayBusyPercentSlice = append(u8ArrayBusyPercentSlice, v)
				}
			}
		}
	}
	if u8ArrayBusyPercentCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u8Array").Str("membership", "sysmGpuBusyPct").Int("got", u8ArrayBusyPercentCount).Errorf("slot u8Array@sysmGpuBusyPct (field BusyPercent) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u8ArrayBusyPercentCount)
		return
	}
	if u8ArrayBusyPercentSlice != nil {
		row.BusyPercent = u8ArrayBusyPercentSlice
		present = true
	}
	// --- u64Array. ---
	var u64ArrayMemoryUsedBytesSlice []uint64
	var u64ArrayMemoryUsedBytesCount int
	var u64ArrayMemoryUsedBytesLastAttr int64
	var u64ArrayMemoryTotalBytesSlice []uint64
	var u64ArrayMemoryTotalBytesCount int
	var u64ArrayMemoryTotalBytesLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmGpuMemoryUsedBytes:
				if u64ArrayMemoryUsedBytesLastAttr != attrJ+1 {
					u64ArrayMemoryUsedBytesLastAttr = attrJ + 1
					u64ArrayMemoryUsedBytesCount++
				}
				for v := range u64ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u64ArrayMemoryUsedBytesSlice = append(u64ArrayMemoryUsedBytesSlice, v)
				}
			case kindSysmGpuMemoryTotalBytes:
				if u64ArrayMemoryTotalBytesLastAttr != attrJ+1 {
					u64ArrayMemoryTotalBytesLastAttr = attrJ + 1
					u64ArrayMemoryTotalBytesCount++
				}
				for v := range u64ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u64ArrayMemoryTotalBytesSlice = append(u64ArrayMemoryTotalBytesSlice, v)
				}
			}
		}
	}
	if u64ArrayMemoryUsedBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmGpuMemoryUsedBytes").Int("got", u64ArrayMemoryUsedBytesCount).Errorf("slot u64Array@sysmGpuMemoryUsedBytes (field MemoryUsedBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayMemoryUsedBytesCount)
		return
	}
	if u64ArrayMemoryUsedBytesSlice != nil {
		row.MemoryUsedBytes = u64ArrayMemoryUsedBytesSlice
		present = true
	}
	if u64ArrayMemoryTotalBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmGpuMemoryTotalBytes").Int("got", u64ArrayMemoryTotalBytesCount).Errorf("slot u64Array@sysmGpuMemoryTotalBytes (field MemoryTotalBytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayMemoryTotalBytesCount)
		return
	}
	if u64ArrayMemoryTotalBytesSlice != nil {
		row.MemoryTotalBytes = u64ArrayMemoryTotalBytesSlice
		present = true
	}
	// --- f32Array. ---
	var f32ArrayPowerWattsSlice []float32
	var f32ArrayPowerWattsCount int
	var f32ArrayPowerWattsLastAttr int64
	var f32ArrayTempCSlice []float32
	var f32ArrayTempCCount int
	var f32ArrayTempCLastAttr int64
	nf32Array := f32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nf32Array; attrJ++ {
		for membID := range f32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmGpuPowerWatts:
				if f32ArrayPowerWattsLastAttr != attrJ+1 {
					f32ArrayPowerWattsLastAttr = attrJ + 1
					f32ArrayPowerWattsCount++
				}
				for v := range f32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					f32ArrayPowerWattsSlice = append(f32ArrayPowerWattsSlice, v)
				}
			case kindSysmGpuTempC:
				if f32ArrayTempCLastAttr != attrJ+1 {
					f32ArrayTempCLastAttr = attrJ + 1
					f32ArrayTempCCount++
				}
				for v := range f32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					f32ArrayTempCSlice = append(f32ArrayTempCSlice, v)
				}
			}
		}
	}
	if f32ArrayPowerWattsCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "f32Array").Str("membership", "sysmGpuPowerWatts").Int("got", f32ArrayPowerWattsCount).Errorf("slot f32Array@sysmGpuPowerWatts (field PowerWatts) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", f32ArrayPowerWattsCount)
		return
	}
	if f32ArrayPowerWattsSlice != nil {
		row.PowerWatts = f32ArrayPowerWattsSlice
		present = true
	}
	if f32ArrayTempCCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "f32Array").Str("membership", "sysmGpuTempC").Int("got", f32ArrayTempCCount).Errorf("slot f32Array@sysmGpuTempC (field TempC) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", f32ArrayTempCCount)
		return
	}
	if f32ArrayTempCSlice != nil {
		row.TempC = f32ArrayTempCSlice
		present = true
	}
	// --- u32Array. ---
	var u32ArrayFreqMHzSlice []uint32
	var u32ArrayFreqMHzCount int
	var u32ArrayFreqMHzLastAttr int64
	nu32Array := u32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu32Array; attrJ++ {
		for membID := range u32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmGpuFreqMhz:
				if u32ArrayFreqMHzLastAttr != attrJ+1 {
					u32ArrayFreqMHzLastAttr = attrJ + 1
					u32ArrayFreqMHzCount++
				}
				for v := range u32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u32ArrayFreqMHzSlice = append(u32ArrayFreqMHzSlice, v)
				}
			}
		}
	}
	if u32ArrayFreqMHzCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "sysmGpuFreqMhz").Int("got", u32ArrayFreqMHzCount).Errorf("slot u32Array@sysmGpuFreqMhz (field FreqMHz) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayFreqMHzCount)
		return
	}
	if u32ArrayFreqMHzSlice != nil {
		row.FreqMHz = u32ArrayFreqMHzSlice
		present = true
	}
	return
}
