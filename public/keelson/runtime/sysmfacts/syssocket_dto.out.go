// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package sysmfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindSysmKindSocket  uint64 = 3098476543630901377
	kindSysmSocketHost  uint64 = 3098476543630901378
	kindSysmSocketProto uint64 = 3098476543630901379
	kindSysmSocketAddr  uint64 = 3098476543630901380
	kindSysmSocketPort  uint64 = 3098476543630901381
	kindSysmSocketInode uint64 = 3098476543630901382
	kindSysmSocketUid   uint64 = 3098476543630901383
	kindSysmSocketPid   uint64 = 3098476543630901384
)

// sysSocketSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysSocketSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// sysSocketSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysSocketSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// sysSocketSymbolArrayAttrI is the InAttr-side view of the symbolArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysSocketSymbolArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value string)
	EndAttributeP()
}

// sysSocketSymbolArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysSocketSymbolArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysSocketU16ArrayAttrI is the InAttr-side view of the u16Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysSocketU16ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint16)
	EndAttributeP()
}

// sysSocketU16ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysSocketU16ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysSocketU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysSocketU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint64)
	EndAttributeP()
}

// sysSocketU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysSocketU64ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysSocketU32ArrayAttrI is the InAttr-side view of the u32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysSocketU32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint32)
	EndAttributeP()
}

// sysSocketU32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysSocketU32ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysSocketEntityI is the entity-builder surface sysSocketAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type sysSocketEntityI[
	SymbolAttr sysSocketSymbolAttrI,
	SymbolSec sysSocketSymbolSecI[SymbolAttr, Ent],
	SymbolArrayAttr sysSocketSymbolArrayAttrI,
	SymbolArraySec sysSocketSymbolArraySecI[SymbolArrayAttr, Ent],
	U16ArrayAttr sysSocketU16ArrayAttrI,
	U16ArraySec sysSocketU16ArraySecI[U16ArrayAttr, Ent],
	U64ArrayAttr sysSocketU64ArrayAttrI,
	U64ArraySec sysSocketU64ArraySecI[U64ArrayAttr, Ent],
	U32ArrayAttr sysSocketU32ArrayAttrI,
	U32ArraySec sysSocketU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionSymbolArray() SymbolArraySec
	GetSectionU16Array() U16ArraySec
	GetSectionU64Array() U64ArraySec
	GetSectionU32Array() U32ArraySec
}

// sysSocketAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func sysSocketAddSections[
	SymbolAttr sysSocketSymbolAttrI,
	SymbolSec sysSocketSymbolSecI[SymbolAttr, Ent],
	SymbolArrayAttr sysSocketSymbolArrayAttrI,
	SymbolArraySec sysSocketSymbolArraySecI[SymbolArrayAttr, Ent],
	U16ArrayAttr sysSocketU16ArrayAttrI,
	U16ArraySec sysSocketU16ArraySecI[U16ArrayAttr, Ent],
	U64ArrayAttr sysSocketU64ArrayAttrI,
	U64ArraySec sysSocketU64ArraySecI[U64ArrayAttr, Ent],
	U32ArrayAttr sysSocketU32ArrayAttrI,
	U32ArraySec sysSocketU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
	DML sysSocketEntityI[
		SymbolAttr, SymbolSec,
		SymbolArrayAttr, SymbolArraySec,
		U16ArrayAttr, U16ArraySec,
		U64ArrayAttr, U64ArraySec,
		U32ArrayAttr, U32ArraySec,
		Ent,
	],
](dml DML, row SysSocket) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindSysmKindSocket)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Host := symbolSec.BeginAttribute(row.Host)
	symbolSecAttr_Host.AddMembershipLowCardRefP(kindSysmSocketHost)
	symbolSecAttr_Host.EndAttributeP()
	symbolSec.EndSection()
	// --- symbolArray. ---
	symbolArraySec := dml.GetSectionSymbolArray()
	if len(row.Proto) > 0 {
		symbolArraySecAttr_Proto := symbolArraySec.BeginAttribute()
		for _, v := range row.Proto {
			symbolArraySecAttr_Proto.AddToContainerP(v)
		}
		symbolArraySecAttr_Proto.AddMembershipLowCardRefP(kindSysmSocketProto)
		symbolArraySecAttr_Proto.EndAttributeP()
	}
	if len(row.Addr) > 0 {
		symbolArraySecAttr_Addr := symbolArraySec.BeginAttribute()
		for _, v := range row.Addr {
			symbolArraySecAttr_Addr.AddToContainerP(v)
		}
		symbolArraySecAttr_Addr.AddMembershipLowCardRefP(kindSysmSocketAddr)
		symbolArraySecAttr_Addr.EndAttributeP()
	}
	symbolArraySec.EndSection()
	// --- u16Array. ---
	u16ArraySec := dml.GetSectionU16Array()
	if len(row.Port) > 0 {
		u16ArraySecAttr_Port := u16ArraySec.BeginAttribute()
		for _, v := range row.Port {
			u16ArraySecAttr_Port.AddToContainerP(v)
		}
		u16ArraySecAttr_Port.AddMembershipLowCardRefP(kindSysmSocketPort)
		u16ArraySecAttr_Port.EndAttributeP()
	}
	u16ArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	if len(row.Inode) > 0 {
		u64ArraySecAttr_Inode := u64ArraySec.BeginAttribute()
		for _, v := range row.Inode {
			u64ArraySecAttr_Inode.AddToContainerP(v)
		}
		u64ArraySecAttr_Inode.AddMembershipLowCardRefP(kindSysmSocketInode)
		u64ArraySecAttr_Inode.EndAttributeP()
	}
	u64ArraySec.EndSection()
	// --- u32Array. ---
	u32ArraySec := dml.GetSectionU32Array()
	if len(row.Uid) > 0 {
		u32ArraySecAttr_Uid := u32ArraySec.BeginAttribute()
		for _, v := range row.Uid {
			u32ArraySecAttr_Uid.AddToContainerP(v)
		}
		u32ArraySecAttr_Uid.AddMembershipLowCardRefP(kindSysmSocketUid)
		u32ArraySecAttr_Uid.EndAttributeP()
	}
	if len(row.Pid) > 0 {
		u32ArraySecAttr_Pid := u32ArraySec.BeginAttribute()
		for _, v := range row.Pid {
			u32ArraySecAttr_Pid.AddToContainerP(v)
		}
		u32ArraySecAttr_Pid.AddMembershipLowCardRefP(kindSysmSocketPid)
		u32ArraySecAttr_Pid.EndAttributeP()
	}
	u32ArraySec.EndSection()
	return
}

// sysSocketSymbolAttrsReadI is the Attributes-side view of the symbol section.
type sysSocketSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysSocketSymbolMembsReadI is the Memberships-side view of the symbol section.
type sysSocketSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysSocketSymbolArrayAttrsReadI is the Attributes-side view of the symbolArray section.
type sysSocketSymbolArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[string]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysSocketSymbolArrayMembsReadI is the Memberships-side view of the symbolArray section.
type sysSocketSymbolArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysSocketU16ArrayAttrsReadI is the Attributes-side view of the u16Array section.
type sysSocketU16ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint16]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysSocketU16ArrayMembsReadI is the Memberships-side view of the u16Array section.
type sysSocketU16ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysSocketU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type sysSocketU64ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysSocketU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type sysSocketU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysSocketU32ArrayAttrsReadI is the Attributes-side view of the u32Array section.
type sysSocketU32ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint32]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysSocketU32ArrayMembsReadI is the Memberships-side view of the u32Array section.
type sysSocketU32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysSocketReadRow reads row i as one optional SysSocket component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func sysSocketReadRow[
	SymbolAttrs sysSocketSymbolAttrsReadI,
	SymbolMembs sysSocketSymbolMembsReadI,
	SymbolArrayAttrs sysSocketSymbolArrayAttrsReadI,
	SymbolArrayMembs sysSocketSymbolArrayMembsReadI,
	U16ArrayAttrs sysSocketU16ArrayAttrsReadI,
	U16ArrayMembs sysSocketU16ArrayMembsReadI,
	U64ArrayAttrs sysSocketU64ArrayAttrsReadI,
	U64ArrayMembs sysSocketU64ArrayMembsReadI,
	U32ArrayAttrs sysSocketU32ArrayAttrsReadI,
	U32ArrayMembs sysSocketU32ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	symbolArrayAttrs SymbolArrayAttrs,
	symbolArrayMembs SymbolArrayMembs,
	u16ArrayAttrs U16ArrayAttrs,
	u16ArrayMembs U16ArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
	u32ArrayAttrs U32ArrayAttrs,
	u32ArrayMembs U32ArrayMembs,
) (row SysSocket, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolHostVal string
	var symbolHostCount int
	var symbolHostLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmKindSocket:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindSysmSocketHost:
				if symbolHostLastAttr != attrJ+1 {
					symbolHostLastAttr = attrJ + 1
					symbolHostCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolHostVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmKindSocket").Int("got", symbolKindCount).Errorf("slot symbol@sysmKindSocket (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolHostCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmSocketHost").Int("got", symbolHostCount).Errorf("slot symbol@sysmSocketHost (field Host) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolHostCount)
		return
	}
	if symbolHostCount == 1 {
		row.Host = symbolHostVal
		present = true
	}
	// --- symbolArray. ---
	var symbolArrayProtoSlice []string
	var symbolArrayProtoCount int
	var symbolArrayProtoLastAttr int64
	var symbolArrayAddrSlice []string
	var symbolArrayAddrCount int
	var symbolArrayAddrLastAttr int64
	nsymbolArray := symbolArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbolArray; attrJ++ {
		for membID := range symbolArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmSocketProto:
				if symbolArrayProtoLastAttr != attrJ+1 {
					symbolArrayProtoLastAttr = attrJ + 1
					symbolArrayProtoCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayProtoSlice = append(symbolArrayProtoSlice, v)
				}
			case kindSysmSocketAddr:
				if symbolArrayAddrLastAttr != attrJ+1 {
					symbolArrayAddrLastAttr = attrJ + 1
					symbolArrayAddrCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayAddrSlice = append(symbolArrayAddrSlice, v)
				}
			}
		}
	}
	if symbolArrayProtoCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "sysmSocketProto").Int("got", symbolArrayProtoCount).Errorf("slot symbolArray@sysmSocketProto (field Proto) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayProtoCount)
		return
	}
	if symbolArrayProtoSlice != nil {
		row.Proto = symbolArrayProtoSlice
		present = true
	}
	if symbolArrayAddrCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "sysmSocketAddr").Int("got", symbolArrayAddrCount).Errorf("slot symbolArray@sysmSocketAddr (field Addr) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayAddrCount)
		return
	}
	if symbolArrayAddrSlice != nil {
		row.Addr = symbolArrayAddrSlice
		present = true
	}
	// --- u16Array. ---
	var u16ArrayPortSlice []uint16
	var u16ArrayPortCount int
	var u16ArrayPortLastAttr int64
	nu16Array := u16ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu16Array; attrJ++ {
		for membID := range u16ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmSocketPort:
				if u16ArrayPortLastAttr != attrJ+1 {
					u16ArrayPortLastAttr = attrJ + 1
					u16ArrayPortCount++
				}
				for v := range u16ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u16ArrayPortSlice = append(u16ArrayPortSlice, v)
				}
			}
		}
	}
	if u16ArrayPortCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u16Array").Str("membership", "sysmSocketPort").Int("got", u16ArrayPortCount).Errorf("slot u16Array@sysmSocketPort (field Port) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u16ArrayPortCount)
		return
	}
	if u16ArrayPortSlice != nil {
		row.Port = u16ArrayPortSlice
		present = true
	}
	// --- u64Array. ---
	var u64ArrayInodeSlice []uint64
	var u64ArrayInodeCount int
	var u64ArrayInodeLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmSocketInode:
				if u64ArrayInodeLastAttr != attrJ+1 {
					u64ArrayInodeLastAttr = attrJ + 1
					u64ArrayInodeCount++
				}
				for v := range u64ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u64ArrayInodeSlice = append(u64ArrayInodeSlice, v)
				}
			}
		}
	}
	if u64ArrayInodeCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "sysmSocketInode").Int("got", u64ArrayInodeCount).Errorf("slot u64Array@sysmSocketInode (field Inode) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayInodeCount)
		return
	}
	if u64ArrayInodeSlice != nil {
		row.Inode = u64ArrayInodeSlice
		present = true
	}
	// --- u32Array. ---
	var u32ArrayUidSlice []uint32
	var u32ArrayUidCount int
	var u32ArrayUidLastAttr int64
	var u32ArrayPidSlice []uint32
	var u32ArrayPidCount int
	var u32ArrayPidLastAttr int64
	nu32Array := u32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu32Array; attrJ++ {
		for membID := range u32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmSocketUid:
				if u32ArrayUidLastAttr != attrJ+1 {
					u32ArrayUidLastAttr = attrJ + 1
					u32ArrayUidCount++
				}
				for v := range u32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u32ArrayUidSlice = append(u32ArrayUidSlice, v)
				}
			case kindSysmSocketPid:
				if u32ArrayPidLastAttr != attrJ+1 {
					u32ArrayPidLastAttr = attrJ + 1
					u32ArrayPidCount++
				}
				for v := range u32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u32ArrayPidSlice = append(u32ArrayPidSlice, v)
				}
			}
		}
	}
	if u32ArrayUidCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "sysmSocketUid").Int("got", u32ArrayUidCount).Errorf("slot u32Array@sysmSocketUid (field Uid) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayUidCount)
		return
	}
	if u32ArrayUidSlice != nil {
		row.Uid = u32ArrayUidSlice
		present = true
	}
	if u32ArrayPidCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "sysmSocketPid").Int("got", u32ArrayPidCount).Errorf("slot u32Array@sysmSocketPid (field Pid) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayPidCount)
		return
	}
	if u32ArrayPidSlice != nil {
		row.Pid = u32ArrayPidSlice
		present = true
	}
	return
}
