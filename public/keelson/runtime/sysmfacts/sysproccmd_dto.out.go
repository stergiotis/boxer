// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package sysmfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindSysmKindProcCmd uint64 = 2305843022098595962
	kindSysmProcCmdHost uint64 = 2305843022098595963
	kindSysmProcCmdPid  uint64 = 2305843022098595964
	kindSysmProcCmdLine uint64 = 2305843022098595965
	kindSysmProcCmdUser uint64 = 2305843022098595966
	kindSysmProcCmdUid  uint64 = 2305843022098595967
	kindSysmProcCmdGid  uint64 = 2305843022098595968
)

// sysProcCmdSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysProcCmdSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// sysProcCmdSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysProcCmdSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// sysProcCmdU32ArrayAttrI is the InAttr-side view of the u32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysProcCmdU32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint32)
	EndAttributeP()
}

// sysProcCmdU32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysProcCmdU32ArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysProcCmdStringArrayAttrI is the InAttr-side view of the stringArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysProcCmdStringArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value string)
	EndAttributeP()
}

// sysProcCmdStringArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysProcCmdStringArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysProcCmdSymbolArrayAttrI is the InAttr-side view of the symbolArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type sysProcCmdSymbolArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value string)
	EndAttributeP()
}

// sysProcCmdSymbolArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type sysProcCmdSymbolArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// sysProcCmdEntityI is the entity-builder surface sysProcCmdAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type sysProcCmdEntityI[
	SymbolAttr sysProcCmdSymbolAttrI,
	SymbolSec sysProcCmdSymbolSecI[SymbolAttr, Ent],
	U32ArrayAttr sysProcCmdU32ArrayAttrI,
	U32ArraySec sysProcCmdU32ArraySecI[U32ArrayAttr, Ent],
	StringArrayAttr sysProcCmdStringArrayAttrI,
	StringArraySec sysProcCmdStringArraySecI[StringArrayAttr, Ent],
	SymbolArrayAttr sysProcCmdSymbolArrayAttrI,
	SymbolArraySec sysProcCmdSymbolArraySecI[SymbolArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionU32Array() U32ArraySec
	GetSectionStringArray() StringArraySec
	GetSectionSymbolArray() SymbolArraySec
}

// sysProcCmdAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func sysProcCmdAddSections[
	SymbolAttr sysProcCmdSymbolAttrI,
	SymbolSec sysProcCmdSymbolSecI[SymbolAttr, Ent],
	U32ArrayAttr sysProcCmdU32ArrayAttrI,
	U32ArraySec sysProcCmdU32ArraySecI[U32ArrayAttr, Ent],
	StringArrayAttr sysProcCmdStringArrayAttrI,
	StringArraySec sysProcCmdStringArraySecI[StringArrayAttr, Ent],
	SymbolArrayAttr sysProcCmdSymbolArrayAttrI,
	SymbolArraySec sysProcCmdSymbolArraySecI[SymbolArrayAttr, Ent],
	Ent any,
	DML sysProcCmdEntityI[
		SymbolAttr, SymbolSec,
		U32ArrayAttr, U32ArraySec,
		StringArrayAttr, StringArraySec,
		SymbolArrayAttr, SymbolArraySec,
		Ent,
	],
](dml DML, row SysProcCmd) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindSysmKindProcCmd)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Host := symbolSec.BeginAttribute(row.Host)
	symbolSecAttr_Host.AddMembershipLowCardRefP(kindSysmProcCmdHost)
	symbolSecAttr_Host.EndAttributeP()
	symbolSec.EndSection()
	// --- u32Array. ---
	u32ArraySec := dml.GetSectionU32Array()
	if len(row.Pid) > 0 {
		u32ArraySecAttr_Pid := u32ArraySec.BeginAttribute()
		for _, v := range row.Pid {
			u32ArraySecAttr_Pid.AddToContainerP(v)
		}
		u32ArraySecAttr_Pid.AddMembershipLowCardRefP(kindSysmProcCmdPid)
		u32ArraySecAttr_Pid.EndAttributeP()
	}
	if len(row.Uid) > 0 {
		u32ArraySecAttr_Uid := u32ArraySec.BeginAttribute()
		for _, v := range row.Uid {
			u32ArraySecAttr_Uid.AddToContainerP(v)
		}
		u32ArraySecAttr_Uid.AddMembershipLowCardRefP(kindSysmProcCmdUid)
		u32ArraySecAttr_Uid.EndAttributeP()
	}
	if len(row.Gid) > 0 {
		u32ArraySecAttr_Gid := u32ArraySec.BeginAttribute()
		for _, v := range row.Gid {
			u32ArraySecAttr_Gid.AddToContainerP(v)
		}
		u32ArraySecAttr_Gid.AddMembershipLowCardRefP(kindSysmProcCmdGid)
		u32ArraySecAttr_Gid.EndAttributeP()
	}
	u32ArraySec.EndSection()
	// --- stringArray. ---
	stringArraySec := dml.GetSectionStringArray()
	if len(row.Cmd) > 0 {
		stringArraySecAttr_Cmd := stringArraySec.BeginAttribute()
		for _, v := range row.Cmd {
			stringArraySecAttr_Cmd.AddToContainerP(v)
		}
		stringArraySecAttr_Cmd.AddMembershipLowCardRefP(kindSysmProcCmdLine)
		stringArraySecAttr_Cmd.EndAttributeP()
	}
	stringArraySec.EndSection()
	// --- symbolArray. ---
	symbolArraySec := dml.GetSectionSymbolArray()
	if len(row.User) > 0 {
		symbolArraySecAttr_User := symbolArraySec.BeginAttribute()
		for _, v := range row.User {
			symbolArraySecAttr_User.AddToContainerP(v)
		}
		symbolArraySecAttr_User.AddMembershipLowCardRefP(kindSysmProcCmdUser)
		symbolArraySecAttr_User.EndAttributeP()
	}
	symbolArraySec.EndSection()
	return
}

// sysProcCmdSymbolAttrsReadI is the Attributes-side view of the symbol section.
type sysProcCmdSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysProcCmdSymbolMembsReadI is the Memberships-side view of the symbol section.
type sysProcCmdSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysProcCmdU32ArrayAttrsReadI is the Attributes-side view of the u32Array section.
type sysProcCmdU32ArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint32]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysProcCmdU32ArrayMembsReadI is the Memberships-side view of the u32Array section.
type sysProcCmdU32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysProcCmdStringArrayAttrsReadI is the Attributes-side view of the stringArray section.
type sysProcCmdStringArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[string]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysProcCmdStringArrayMembsReadI is the Memberships-side view of the stringArray section.
type sysProcCmdStringArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysProcCmdSymbolArrayAttrsReadI is the Attributes-side view of the symbolArray section.
type sysProcCmdSymbolArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[string]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// sysProcCmdSymbolArrayMembsReadI is the Memberships-side view of the symbolArray section.
type sysProcCmdSymbolArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// sysProcCmdReadRow reads row i as one optional SysProcCmd component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func sysProcCmdReadRow[
	SymbolAttrs sysProcCmdSymbolAttrsReadI,
	SymbolMembs sysProcCmdSymbolMembsReadI,
	U32ArrayAttrs sysProcCmdU32ArrayAttrsReadI,
	U32ArrayMembs sysProcCmdU32ArrayMembsReadI,
	StringArrayAttrs sysProcCmdStringArrayAttrsReadI,
	StringArrayMembs sysProcCmdStringArrayMembsReadI,
	SymbolArrayAttrs sysProcCmdSymbolArrayAttrsReadI,
	SymbolArrayMembs sysProcCmdSymbolArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	u32ArrayAttrs U32ArrayAttrs,
	u32ArrayMembs U32ArrayMembs,
	stringArrayAttrs StringArrayAttrs,
	stringArrayMembs StringArrayMembs,
	symbolArrayAttrs SymbolArrayAttrs,
	symbolArrayMembs SymbolArrayMembs,
) (row SysProcCmd, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolHostVal string
	var symbolHostCount int
	var symbolHostLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmKindProcCmd:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindSysmProcCmdHost:
				if symbolHostLastAttr != attrJ+1 {
					symbolHostLastAttr = attrJ + 1
					symbolHostCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolHostVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmKindProcCmd").Int("got", symbolKindCount).Errorf("slot symbol@sysmKindProcCmd (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolHostCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "sysmProcCmdHost").Int("got", symbolHostCount).Errorf("slot symbol@sysmProcCmdHost (field Host) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolHostCount)
		return
	}
	if symbolHostCount == 1 {
		row.Host = symbolHostVal
		present = true
	}
	// --- u32Array. ---
	var u32ArrayPidSlice []uint32
	var u32ArrayPidCount int
	var u32ArrayPidLastAttr int64
	var u32ArrayUidSlice []uint32
	var u32ArrayUidCount int
	var u32ArrayUidLastAttr int64
	var u32ArrayGidSlice []uint32
	var u32ArrayGidCount int
	var u32ArrayGidLastAttr int64
	nu32Array := u32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu32Array; attrJ++ {
		for membID := range u32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmProcCmdPid:
				if u32ArrayPidLastAttr != attrJ+1 {
					u32ArrayPidLastAttr = attrJ + 1
					u32ArrayPidCount++
				}
				for v := range u32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u32ArrayPidSlice = append(u32ArrayPidSlice, v)
				}
			case kindSysmProcCmdUid:
				if u32ArrayUidLastAttr != attrJ+1 {
					u32ArrayUidLastAttr = attrJ + 1
					u32ArrayUidCount++
				}
				for v := range u32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u32ArrayUidSlice = append(u32ArrayUidSlice, v)
				}
			case kindSysmProcCmdGid:
				if u32ArrayGidLastAttr != attrJ+1 {
					u32ArrayGidLastAttr = attrJ + 1
					u32ArrayGidCount++
				}
				for v := range u32ArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					u32ArrayGidSlice = append(u32ArrayGidSlice, v)
				}
			}
		}
	}
	if u32ArrayPidCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "sysmProcCmdPid").Int("got", u32ArrayPidCount).Errorf("slot u32Array@sysmProcCmdPid (field Pid) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayPidCount)
		return
	}
	if u32ArrayPidSlice != nil {
		row.Pid = u32ArrayPidSlice
		present = true
	}
	if u32ArrayUidCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "sysmProcCmdUid").Int("got", u32ArrayUidCount).Errorf("slot u32Array@sysmProcCmdUid (field Uid) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayUidCount)
		return
	}
	if u32ArrayUidSlice != nil {
		row.Uid = u32ArrayUidSlice
		present = true
	}
	if u32ArrayGidCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "sysmProcCmdGid").Int("got", u32ArrayGidCount).Errorf("slot u32Array@sysmProcCmdGid (field Gid) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayGidCount)
		return
	}
	if u32ArrayGidSlice != nil {
		row.Gid = u32ArrayGidSlice
		present = true
	}
	// --- stringArray. ---
	var stringArrayCmdSlice []string
	var stringArrayCmdCount int
	var stringArrayCmdLastAttr int64
	nstringArray := stringArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstringArray; attrJ++ {
		for membID := range stringArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmProcCmdLine:
				if stringArrayCmdLastAttr != attrJ+1 {
					stringArrayCmdLastAttr = attrJ + 1
					stringArrayCmdCount++
				}
				for v := range stringArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					stringArrayCmdSlice = append(stringArrayCmdSlice, v)
				}
			}
		}
	}
	if stringArrayCmdCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "sysmProcCmdLine").Int("got", stringArrayCmdCount).Errorf("slot stringArray@sysmProcCmdLine (field Cmd) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayCmdCount)
		return
	}
	if stringArrayCmdSlice != nil {
		row.Cmd = stringArrayCmdSlice
		present = true
	}
	// --- symbolArray. ---
	var symbolArrayUserSlice []string
	var symbolArrayUserCount int
	var symbolArrayUserLastAttr int64
	nsymbolArray := symbolArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbolArray; attrJ++ {
		for membID := range symbolArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindSysmProcCmdUser:
				if symbolArrayUserLastAttr != attrJ+1 {
					symbolArrayUserLastAttr = attrJ + 1
					symbolArrayUserCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayUserSlice = append(symbolArrayUserSlice, v)
				}
			}
		}
	}
	if symbolArrayUserCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "sysmProcCmdUser").Int("got", symbolArrayUserCount).Errorf("slot symbolArray@sysmProcCmdUser (field User) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayUserCount)
		return
	}
	if symbolArrayUserSlice != nil {
		row.User = symbolArrayUserSlice
		present = true
	}
	return
}
