// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package meshdemo

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindMeshKindFleetSample uint64 = 1152921517491748864
	kindMeshHost            uint64 = 1152921517491748865
	kindMeshRegion          uint64 = 1152921517491748866
	kindMeshCpuPercent      uint64 = 1152921517491748867
	kindMeshUptimeSeconds   uint64 = 1152921517491748868
)

// fleetSampleSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type fleetSampleSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// fleetSampleSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type fleetSampleSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// fleetSampleU8ArrayAttrI is the InAttr-side view of the u8Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type fleetSampleU8ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// fleetSampleU8ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type fleetSampleU8ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint8) Attr
	EndSection() Ent
}

// fleetSampleU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type fleetSampleU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// fleetSampleU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type fleetSampleU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// fleetSampleEntityI is the entity-builder surface fleetSampleAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type fleetSampleEntityI[
	SymbolAttr fleetSampleSymbolAttrI,
	SymbolSec fleetSampleSymbolSecI[SymbolAttr, Ent],
	U8ArrayAttr fleetSampleU8ArrayAttrI,
	U8ArraySec fleetSampleU8ArraySecI[U8ArrayAttr, Ent],
	U64ArrayAttr fleetSampleU64ArrayAttrI,
	U64ArraySec fleetSampleU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionU8Array() U8ArraySec
	GetSectionU64Array() U64ArraySec
}

// fleetSampleEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func fleetSampleEmitSectionSymbol[
	SymbolAttr fleetSampleSymbolAttrI,
	SymbolSec fleetSampleSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row FleetSample) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindMeshKindFleetSample)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Host := symbolSec.BeginAttribute(row.Host)
	symbolSecAttr_Host.AddMembershipLowCardRefP(kindMeshHost)
	symbolSecAttr_Host.EndAttributeP()
	symbolSecAttr_Region := symbolSec.BeginAttribute(row.Region)
	symbolSecAttr_Region.AddMembershipLowCardRefP(kindMeshRegion)
	symbolSecAttr_Region.EndAttributeP()
	return
}

// fleetSampleEmitSectionU8Array writes this kind's u8Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func fleetSampleEmitSectionU8Array[
	U8ArrayAttr fleetSampleU8ArrayAttrI,
	U8ArraySec fleetSampleU8ArraySecI[U8ArrayAttr, Ent],
	Ent any,
](u8ArraySec U8ArraySec, row FleetSample) (err error) {
	u8ArraySecAttr_CpuPercent := u8ArraySec.BeginAttributeSingle(row.CpuPercent)
	u8ArraySecAttr_CpuPercent.AddMembershipLowCardRefP(kindMeshCpuPercent)
	u8ArraySecAttr_CpuPercent.EndAttributeP()
	return
}

// fleetSampleEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func fleetSampleEmitSectionU64Array[
	U64ArrayAttr fleetSampleU64ArrayAttrI,
	U64ArraySec fleetSampleU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row FleetSample) (err error) {
	u64ArraySecAttr_UptimeSeconds := u64ArraySec.BeginAttributeSingle(row.UptimeSeconds)
	u64ArraySecAttr_UptimeSeconds.AddMembershipLowCardRefP(kindMeshUptimeSeconds)
	u64ArraySecAttr_UptimeSeconds.EndAttributeP()
	return
}

// fleetSampleAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func fleetSampleAddSections[
	SymbolAttr fleetSampleSymbolAttrI,
	SymbolSec fleetSampleSymbolSecI[SymbolAttr, Ent],
	U8ArrayAttr fleetSampleU8ArrayAttrI,
	U8ArraySec fleetSampleU8ArraySecI[U8ArrayAttr, Ent],
	U64ArrayAttr fleetSampleU64ArrayAttrI,
	U64ArraySec fleetSampleU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
	DML fleetSampleEntityI[
		SymbolAttr, SymbolSec,
		U8ArrayAttr, U8ArraySec,
		U64ArrayAttr, U64ArraySec,
		Ent,
	],
](dml DML, row FleetSample) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = fleetSampleEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- u8Array. ---
	u8ArraySec := dml.GetSectionU8Array()
	err = fleetSampleEmitSectionU8Array(u8ArraySec, row)
	if err != nil {
		return
	}
	u8ArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = fleetSampleEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	return
}

// fleetSampleSymbolAttrsReadI is the Attributes-side view of the symbol section.
type fleetSampleSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// fleetSampleSymbolMembsReadI is the Memberships-side view of the symbol section.
type fleetSampleSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// fleetSampleU8ArrayAttrsReadI is the Attributes-side view of the u8Array section.
type fleetSampleU8ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint8, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// fleetSampleU8ArrayMembsReadI is the Memberships-side view of the u8Array section.
type fleetSampleU8ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// fleetSampleU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type fleetSampleU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// fleetSampleU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type fleetSampleU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// fleetSampleReadRow reads row i as one optional FleetSample component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func fleetSampleReadRow[
	SymbolAttrs fleetSampleSymbolAttrsReadI,
	SymbolMembs fleetSampleSymbolMembsReadI,
	U8ArrayAttrs fleetSampleU8ArrayAttrsReadI,
	U8ArrayMembs fleetSampleU8ArrayMembsReadI,
	U64ArrayAttrs fleetSampleU64ArrayAttrsReadI,
	U64ArrayMembs fleetSampleU64ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	u8ArrayAttrs U8ArrayAttrs,
	u8ArrayMembs U8ArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
) (row FleetSample, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolHostVal string
	var symbolHostCount int
	var symbolHostLastAttr int64
	var symbolRegionVal string
	var symbolRegionCount int
	var symbolRegionLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMeshKindFleetSample:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindMeshHost:
				if symbolHostLastAttr != attrJ+1 {
					symbolHostLastAttr = attrJ + 1
					symbolHostCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolHostVal = val
			case kindMeshRegion:
				if symbolRegionLastAttr != attrJ+1 {
					symbolRegionLastAttr = attrJ + 1
					symbolRegionCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolRegionVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "meshKindFleetSample").Int("got", symbolKindCount).Errorf("slot symbol@meshKindFleetSample (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolHostCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "meshHost").Int("got", symbolHostCount).Errorf("slot symbol@meshHost (field Host) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolHostCount)
		return
	}
	if symbolHostCount == 1 {
		row.Host = symbolHostVal
		present = true
	}
	if symbolRegionCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "meshRegion").Int("got", symbolRegionCount).Errorf("slot symbol@meshRegion (field Region) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolRegionCount)
		return
	}
	if symbolRegionCount == 1 {
		row.Region = symbolRegionVal
		present = true
	}
	// --- u8Array. ---
	var u8ArrayCpuPercentVal uint8
	var u8ArrayCpuPercentCount int
	var u8ArrayCpuPercentLastAttr int64
	nu8Array := u8ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu8Array; attrJ++ {
		for membID := range u8ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMeshCpuPercent:
				if u8ArrayCpuPercentLastAttr != attrJ+1 {
					u8ArrayCpuPercentLastAttr = attrJ + 1
					u8ArrayCpuPercentCount++
				}
				val, valErr := u8ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u8Array").Str("membership", "meshCpuPercent").Str("field", "CpuPercent").Errorf("slot u8Array@meshCpuPercent (field CpuPercent) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u8ArrayCpuPercentVal = val
			}
		}
	}
	if u8ArrayCpuPercentCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u8Array").Str("membership", "meshCpuPercent").Int("got", u8ArrayCpuPercentCount).Errorf("slot u8Array@meshCpuPercent (field CpuPercent) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u8ArrayCpuPercentCount)
		return
	}
	if u8ArrayCpuPercentCount == 1 {
		row.CpuPercent = u8ArrayCpuPercentVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayUptimeSecondsVal uint64
	var u64ArrayUptimeSecondsCount int
	var u64ArrayUptimeSecondsLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMeshUptimeSeconds:
				if u64ArrayUptimeSecondsLastAttr != attrJ+1 {
					u64ArrayUptimeSecondsLastAttr = attrJ + 1
					u64ArrayUptimeSecondsCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "meshUptimeSeconds").Str("field", "UptimeSeconds").Errorf("slot u64Array@meshUptimeSeconds (field UptimeSeconds) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayUptimeSecondsVal = val
			}
		}
	}
	if u64ArrayUptimeSecondsCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "meshUptimeSeconds").Int("got", u64ArrayUptimeSecondsCount).Errorf("slot u64Array@meshUptimeSeconds (field UptimeSeconds) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayUptimeSecondsCount)
		return
	}
	if u64ArrayUptimeSecondsCount == 1 {
		row.UptimeSeconds = u64ArrayUptimeSecondsVal
		present = true
	}
	return
}
