// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package persiststore

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindRuntimeApp              uint64 = 9223372049739677701
	kindRuntimeRun              uint64 = 9223372049739677716
	kindRuntimeLifecycleTileKey uint64 = 9223372049739677728
)

// ownerSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ownerSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ownerSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ownerSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// ownerU64AttrI is the InAttr-side view of the u64 section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ownerU64AttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ownerU64SecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ownerU64SecI[Attr any, Ent any] interface {
	BeginAttribute(value uint64) Attr
	EndSection() Ent
}

// ownerEntityI is the entity-builder surface ownerAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type ownerEntityI[
	SymbolAttr ownerSymbolAttrI,
	SymbolSec ownerSymbolSecI[SymbolAttr, Ent],
	U64Attr ownerU64AttrI,
	U64Sec ownerU64SecI[U64Attr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionU64() U64Sec
}

// ownerEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ownerEmitSectionSymbol[
	SymbolAttr ownerSymbolAttrI,
	SymbolSec ownerSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row Owner) (err error) {
	symbolSecAttr_AppId := symbolSec.BeginAttribute(row.AppId)
	symbolSecAttr_AppId.AddMembershipLowCardRefP(kindRuntimeApp)
	symbolSecAttr_AppId.EndAttributeP()
	symbolSecAttr_RunId := symbolSec.BeginAttribute(row.RunId)
	symbolSecAttr_RunId.AddMembershipLowCardRefP(kindRuntimeRun)
	symbolSecAttr_RunId.EndAttributeP()
	return
}

// ownerEmitSectionU64 writes this kind's u64 attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ownerEmitSectionU64[
	U64Attr ownerU64AttrI,
	U64Sec ownerU64SecI[U64Attr, Ent],
	Ent any,
](u64Sec U64Sec, row Owner) (err error) {
	u64SecAttr_InstanceKey := u64Sec.BeginAttribute(row.InstanceKey)
	u64SecAttr_InstanceKey.AddMembershipLowCardRefP(kindRuntimeLifecycleTileKey)
	u64SecAttr_InstanceKey.EndAttributeP()
	return
}

// ownerAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func ownerAddSections[
	SymbolAttr ownerSymbolAttrI,
	SymbolSec ownerSymbolSecI[SymbolAttr, Ent],
	U64Attr ownerU64AttrI,
	U64Sec ownerU64SecI[U64Attr, Ent],
	Ent any,
	DML ownerEntityI[
		SymbolAttr, SymbolSec,
		U64Attr, U64Sec,
		Ent,
	],
](dml DML, row Owner) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = ownerEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- u64. ---
	u64Sec := dml.GetSectionU64()
	err = ownerEmitSectionU64(u64Sec, row)
	if err != nil {
		return
	}
	u64Sec.EndSection()
	return
}

// ownerSymbolAttrsReadI is the Attributes-side view of the symbol section.
type ownerSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ownerSymbolMembsReadI is the Memberships-side view of the symbol section.
type ownerSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ownerU64AttrsReadI is the Attributes-side view of the u64 section.
type ownerU64AttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) uint64
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ownerU64MembsReadI is the Memberships-side view of the u64 section.
type ownerU64MembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ownerReadRow reads row i as one optional Owner component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func ownerReadRow[
	SymbolAttrs ownerSymbolAttrsReadI,
	SymbolMembs ownerSymbolMembsReadI,
	U64Attrs ownerU64AttrsReadI,
	U64Membs ownerU64MembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	u64Attrs U64Attrs,
	u64Membs U64Membs,
) (row Owner, present bool, err error) {
	// --- symbol. ---
	var symbolAppIdVal string
	var symbolAppIdCount int
	var symbolAppIdLastAttr int64
	var symbolRunIdVal string
	var symbolRunIdCount int
	var symbolRunIdLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeApp:
				if symbolAppIdLastAttr != attrJ+1 {
					symbolAppIdLastAttr = attrJ + 1
					symbolAppIdCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolAppIdVal = val
			case kindRuntimeRun:
				if symbolRunIdLastAttr != attrJ+1 {
					symbolRunIdLastAttr = attrJ + 1
					symbolRunIdCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolRunIdVal = val
			}
		}
	}
	if symbolAppIdCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "runtimeApp").Int("got", symbolAppIdCount).Errorf("slot symbol@runtimeApp (field AppId) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolAppIdCount)
		return
	}
	if symbolAppIdCount == 1 {
		row.AppId = symbolAppIdVal
		present = true
	}
	if symbolRunIdCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "runtimeRun").Int("got", symbolRunIdCount).Errorf("slot symbol@runtimeRun (field RunId) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolRunIdCount)
		return
	}
	if symbolRunIdCount == 1 {
		row.RunId = symbolRunIdVal
		present = true
	}
	// --- u64. ---
	var u64InstanceKeyVal uint64
	var u64InstanceKeyCount int
	var u64InstanceKeyLastAttr int64
	nu64 := u64Attrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64; attrJ++ {
		for membID := range u64Membs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeLifecycleTileKey:
				if u64InstanceKeyLastAttr != attrJ+1 {
					u64InstanceKeyLastAttr = attrJ + 1
					u64InstanceKeyCount++
				}
				val := u64Attrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				u64InstanceKeyVal = val
			}
		}
	}
	if u64InstanceKeyCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64").Str("membership", "runtimeLifecycleTileKey").Int("got", u64InstanceKeyCount).Errorf("slot u64@runtimeLifecycleTileKey (field InstanceKey) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64InstanceKeyCount)
		return
	}
	if u64InstanceKeyCount == 1 {
		row.InstanceKey = u64InstanceKeyVal
		present = true
	}
	return
}
