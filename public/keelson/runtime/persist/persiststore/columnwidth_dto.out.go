// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package persiststore

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindRuntimeColWidthTier      uint64 = 9223372049739677764
	kindRuntimeColWidthScope     uint64 = 9223372049739677765
	kindRuntimeColWidthColumnKey uint64 = 9223372049739677766
	kindRuntimeColWidthPoints    uint64 = 9223372049739677767
	kindRuntimeColWidthFontSize  uint64 = 9223372049739677768
)

// columnWidthSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type columnWidthSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// columnWidthSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type columnWidthSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// columnWidthStringAttrI is the InAttr-side view of the string section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type columnWidthStringAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// columnWidthStringSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type columnWidthStringSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// columnWidthF64AttrI is the InAttr-side view of the f64 section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type columnWidthF64AttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// columnWidthF64SecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type columnWidthF64SecI[Attr any, Ent any] interface {
	BeginAttribute(value float64) Attr
	EndSection() Ent
}

// columnWidthEntityI is the entity-builder surface columnWidthAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type columnWidthEntityI[
	SymbolAttr columnWidthSymbolAttrI,
	SymbolSec columnWidthSymbolSecI[SymbolAttr, Ent],
	StringAttr columnWidthStringAttrI,
	StringSec columnWidthStringSecI[StringAttr, Ent],
	F64Attr columnWidthF64AttrI,
	F64Sec columnWidthF64SecI[F64Attr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionString() StringSec
	GetSectionF64() F64Sec
}

// columnWidthEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func columnWidthEmitSectionSymbol[
	SymbolAttr columnWidthSymbolAttrI,
	SymbolSec columnWidthSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row ColumnWidth) (err error) {
	symbolSecAttr_Tier := symbolSec.BeginAttribute(row.Tier)
	symbolSecAttr_Tier.AddMembershipLowCardRefP(kindRuntimeColWidthTier)
	symbolSecAttr_Tier.EndAttributeP()
	return
}

// columnWidthEmitSectionString writes this kind's string attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func columnWidthEmitSectionString[
	StringAttr columnWidthStringAttrI,
	StringSec columnWidthStringSecI[StringAttr, Ent],
	Ent any,
](stringSec StringSec, row ColumnWidth) (err error) {
	stringSecAttr_Scope := stringSec.BeginAttribute(row.Scope)
	stringSecAttr_Scope.AddMembershipLowCardRefP(kindRuntimeColWidthScope)
	stringSecAttr_Scope.EndAttributeP()
	stringSecAttr_ColumnKey := stringSec.BeginAttribute(row.ColumnKey)
	stringSecAttr_ColumnKey.AddMembershipLowCardRefP(kindRuntimeColWidthColumnKey)
	stringSecAttr_ColumnKey.EndAttributeP()
	return
}

// columnWidthEmitSectionF64 writes this kind's f64 attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func columnWidthEmitSectionF64[
	F64Attr columnWidthF64AttrI,
	F64Sec columnWidthF64SecI[F64Attr, Ent],
	Ent any,
](f64Sec F64Sec, row ColumnWidth) (err error) {
	f64SecAttr_Points := f64Sec.BeginAttribute(row.Points)
	f64SecAttr_Points.AddMembershipLowCardRefP(kindRuntimeColWidthPoints)
	f64SecAttr_Points.EndAttributeP()
	f64SecAttr_FontSize := f64Sec.BeginAttribute(row.FontSize)
	f64SecAttr_FontSize.AddMembershipLowCardRefP(kindRuntimeColWidthFontSize)
	f64SecAttr_FontSize.EndAttributeP()
	return
}

// columnWidthAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func columnWidthAddSections[
	SymbolAttr columnWidthSymbolAttrI,
	SymbolSec columnWidthSymbolSecI[SymbolAttr, Ent],
	StringAttr columnWidthStringAttrI,
	StringSec columnWidthStringSecI[StringAttr, Ent],
	F64Attr columnWidthF64AttrI,
	F64Sec columnWidthF64SecI[F64Attr, Ent],
	Ent any,
	DML columnWidthEntityI[
		SymbolAttr, SymbolSec,
		StringAttr, StringSec,
		F64Attr, F64Sec,
		Ent,
	],
](dml DML, row ColumnWidth) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = columnWidthEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- string. ---
	stringSec := dml.GetSectionString()
	err = columnWidthEmitSectionString(stringSec, row)
	if err != nil {
		return
	}
	stringSec.EndSection()
	// --- f64. ---
	f64Sec := dml.GetSectionF64()
	err = columnWidthEmitSectionF64(f64Sec, row)
	if err != nil {
		return
	}
	f64Sec.EndSection()
	return
}

// columnWidthSymbolAttrsReadI is the Attributes-side view of the symbol section.
type columnWidthSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// columnWidthSymbolMembsReadI is the Memberships-side view of the symbol section.
type columnWidthSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// columnWidthStringAttrsReadI is the Attributes-side view of the string section.
type columnWidthStringAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// columnWidthStringMembsReadI is the Memberships-side view of the string section.
type columnWidthStringMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// columnWidthF64AttrsReadI is the Attributes-side view of the f64 section.
type columnWidthF64AttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) float64
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// columnWidthF64MembsReadI is the Memberships-side view of the f64 section.
type columnWidthF64MembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// columnWidthReadRow reads row i as one optional ColumnWidth component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func columnWidthReadRow[
	SymbolAttrs columnWidthSymbolAttrsReadI,
	SymbolMembs columnWidthSymbolMembsReadI,
	StringAttrs columnWidthStringAttrsReadI,
	StringMembs columnWidthStringMembsReadI,
	F64Attrs columnWidthF64AttrsReadI,
	F64Membs columnWidthF64MembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	stringAttrs StringAttrs,
	stringMembs StringMembs,
	f64Attrs F64Attrs,
	f64Membs F64Membs,
) (row ColumnWidth, present bool, err error) {
	// --- symbol. ---
	var symbolTierVal string
	var symbolTierCount int
	var symbolTierLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeColWidthTier:
				if symbolTierLastAttr != attrJ+1 {
					symbolTierLastAttr = attrJ + 1
					symbolTierCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolTierVal = val
			}
		}
	}
	if symbolTierCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "runtimeColWidthTier").Int("got", symbolTierCount).Errorf("slot symbol@runtimeColWidthTier (field Tier) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolTierCount)
		return
	}
	if symbolTierCount == 1 {
		row.Tier = symbolTierVal
		present = true
	}
	// --- string. ---
	var stringScopeVal string
	var stringScopeCount int
	var stringScopeLastAttr int64
	var stringColumnKeyVal string
	var stringColumnKeyCount int
	var stringColumnKeyLastAttr int64
	nstring := stringAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstring; attrJ++ {
		for membID := range stringMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeColWidthScope:
				if stringScopeLastAttr != attrJ+1 {
					stringScopeLastAttr = attrJ + 1
					stringScopeCount++
				}
				val := stringAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				stringScopeVal = val
			case kindRuntimeColWidthColumnKey:
				if stringColumnKeyLastAttr != attrJ+1 {
					stringColumnKeyLastAttr = attrJ + 1
					stringColumnKeyCount++
				}
				val := stringAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				stringColumnKeyVal = val
			}
		}
	}
	if stringScopeCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "string").Str("membership", "runtimeColWidthScope").Int("got", stringScopeCount).Errorf("slot string@runtimeColWidthScope (field Scope) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringScopeCount)
		return
	}
	if stringScopeCount == 1 {
		row.Scope = stringScopeVal
		present = true
	}
	if stringColumnKeyCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "string").Str("membership", "runtimeColWidthColumnKey").Int("got", stringColumnKeyCount).Errorf("slot string@runtimeColWidthColumnKey (field ColumnKey) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringColumnKeyCount)
		return
	}
	if stringColumnKeyCount == 1 {
		row.ColumnKey = stringColumnKeyVal
		present = true
	}
	// --- f64. ---
	var f64PointsVal float64
	var f64PointsCount int
	var f64PointsLastAttr int64
	var f64FontSizeVal float64
	var f64FontSizeCount int
	var f64FontSizeLastAttr int64
	nf64 := f64Attrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nf64; attrJ++ {
		for membID := range f64Membs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeColWidthPoints:
				if f64PointsLastAttr != attrJ+1 {
					f64PointsLastAttr = attrJ + 1
					f64PointsCount++
				}
				val := f64Attrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				f64PointsVal = val
			case kindRuntimeColWidthFontSize:
				if f64FontSizeLastAttr != attrJ+1 {
					f64FontSizeLastAttr = attrJ + 1
					f64FontSizeCount++
				}
				val := f64Attrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				f64FontSizeVal = val
			}
		}
	}
	if f64PointsCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "f64").Str("membership", "runtimeColWidthPoints").Int("got", f64PointsCount).Errorf("slot f64@runtimeColWidthPoints (field Points) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", f64PointsCount)
		return
	}
	if f64PointsCount == 1 {
		row.Points = f64PointsVal
		present = true
	}
	if f64FontSizeCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "f64").Str("membership", "runtimeColWidthFontSize").Int("got", f64FontSizeCount).Errorf("slot f64@runtimeColWidthFontSize (field FontSize) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", f64FontSizeCount)
		return
	}
	if f64FontSizeCount == 1 {
		row.FontSize = f64FontSizeVal
		present = true
	}
	return
}
