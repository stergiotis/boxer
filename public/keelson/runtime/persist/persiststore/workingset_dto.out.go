// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package persiststore

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindRuntimeWorkingsetName      uint64 = 9223372049739677762
	kindRuntimeLaunchConfigKind    uint64 = 9223372049739677759
	kindRuntimeLaunchConfig        uint64 = 9223372049739677760
	kindRuntimeLifecycleStopReason uint64 = 9223372049739677727
)

// workingsetSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type workingsetSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// workingsetSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type workingsetSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// workingsetBlobAttrI is the InAttr-side view of the blob section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type workingsetBlobAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// workingsetBlobSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type workingsetBlobSecI[Attr any, Ent any] interface {
	BeginAttribute(value []byte) Attr
	EndSection() Ent
}

// workingsetEntityI is the entity-builder surface workingsetAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type workingsetEntityI[
	SymbolAttr workingsetSymbolAttrI,
	SymbolSec workingsetSymbolSecI[SymbolAttr, Ent],
	BlobAttr workingsetBlobAttrI,
	BlobSec workingsetBlobSecI[BlobAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionBlob() BlobSec
}

// workingsetEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func workingsetEmitSectionSymbol[
	SymbolAttr workingsetSymbolAttrI,
	SymbolSec workingsetSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row Workingset) (err error) {
	symbolSecAttr_Name := symbolSec.BeginAttribute(row.Name)
	symbolSecAttr_Name.AddMembershipLowCardRefP(kindRuntimeWorkingsetName)
	symbolSecAttr_Name.EndAttributeP()
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindRuntimeLaunchConfigKind)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Reason := symbolSec.BeginAttribute(row.Reason)
	symbolSecAttr_Reason.AddMembershipLowCardRefP(kindRuntimeLifecycleStopReason)
	symbolSecAttr_Reason.EndAttributeP()
	return
}

// workingsetEmitSectionBlob writes this kind's blob attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func workingsetEmitSectionBlob[
	BlobAttr workingsetBlobAttrI,
	BlobSec workingsetBlobSecI[BlobAttr, Ent],
	Ent any,
](blobSec BlobSec, row Workingset) (err error) {
	blobSecAttr_Config := blobSec.BeginAttribute(row.Config)
	blobSecAttr_Config.AddMembershipLowCardRefP(kindRuntimeLaunchConfig)
	blobSecAttr_Config.EndAttributeP()
	return
}

// workingsetAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func workingsetAddSections[
	SymbolAttr workingsetSymbolAttrI,
	SymbolSec workingsetSymbolSecI[SymbolAttr, Ent],
	BlobAttr workingsetBlobAttrI,
	BlobSec workingsetBlobSecI[BlobAttr, Ent],
	Ent any,
	DML workingsetEntityI[
		SymbolAttr, SymbolSec,
		BlobAttr, BlobSec,
		Ent,
	],
](dml DML, row Workingset) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = workingsetEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- blob. ---
	blobSec := dml.GetSectionBlob()
	err = workingsetEmitSectionBlob(blobSec, row)
	if err != nil {
		return
	}
	blobSec.EndSection()
	return
}

// workingsetSymbolAttrsReadI is the Attributes-side view of the symbol section.
type workingsetSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// workingsetSymbolMembsReadI is the Memberships-side view of the symbol section.
type workingsetSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// workingsetBlobAttrsReadI is the Attributes-side view of the blob section.
type workingsetBlobAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) []byte
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// workingsetBlobMembsReadI is the Memberships-side view of the blob section.
type workingsetBlobMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// workingsetReadRow reads row i as one optional Workingset component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func workingsetReadRow[
	SymbolAttrs workingsetSymbolAttrsReadI,
	SymbolMembs workingsetSymbolMembsReadI,
	BlobAttrs workingsetBlobAttrsReadI,
	BlobMembs workingsetBlobMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	blobAttrs BlobAttrs,
	blobMembs BlobMembs,
) (row Workingset, present bool, err error) {
	// --- symbol. ---
	var symbolNameVal string
	var symbolNameCount int
	var symbolNameLastAttr int64
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolReasonVal string
	var symbolReasonCount int
	var symbolReasonLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeWorkingsetName:
				if symbolNameLastAttr != attrJ+1 {
					symbolNameLastAttr = attrJ + 1
					symbolNameCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolNameVal = val
			case kindRuntimeLaunchConfigKind:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindRuntimeLifecycleStopReason:
				if symbolReasonLastAttr != attrJ+1 {
					symbolReasonLastAttr = attrJ + 1
					symbolReasonCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolReasonVal = val
			}
		}
	}
	if symbolNameCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "runtimeWorkingsetName").Int("got", symbolNameCount).Errorf("slot symbol@runtimeWorkingsetName (field Name) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolNameCount)
		return
	}
	if symbolNameCount == 1 {
		row.Name = symbolNameVal
		present = true
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "runtimeLaunchConfigKind").Int("got", symbolKindCount).Errorf("slot symbol@runtimeLaunchConfigKind (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolReasonCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "runtimeLifecycleStopReason").Int("got", symbolReasonCount).Errorf("slot symbol@runtimeLifecycleStopReason (field Reason) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolReasonCount)
		return
	}
	if symbolReasonCount == 1 {
		row.Reason = symbolReasonVal
		present = true
	}
	// --- blob. ---
	var blobConfigVal []byte
	var blobConfigCount int
	var blobConfigLastAttr int64
	nblob := blobAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nblob; attrJ++ {
		for membID := range blobMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeLaunchConfig:
				if blobConfigLastAttr != attrJ+1 {
					blobConfigLastAttr = attrJ + 1
					blobConfigCount++
				}
				val := blobAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				cp := make([]byte, len(val))
				copy(cp, val)
				blobConfigVal = cp
			}
		}
	}
	if blobConfigCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blob").Str("membership", "runtimeLaunchConfig").Int("got", blobConfigCount).Errorf("slot blob@runtimeLaunchConfig (field Config) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobConfigCount)
		return
	}
	if blobConfigCount == 1 {
		row.Config = blobConfigVal
		present = true
	}
	return
}
