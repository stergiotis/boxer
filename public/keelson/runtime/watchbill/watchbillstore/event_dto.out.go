// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package watchbillstore

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindWatchbillEventState     uint64 = 9223372049739677786
	kindWatchbillEventAttempt   uint64 = 9223372049739677787
	kindWatchbillEventWorkerRun uint64 = 9223372049739677788
	kindWatchbillEventError     uint64 = 9223372049739677789
	kindWatchbillEventNote      uint64 = 9223372049739677790
)

// eventEventStateAttrI is the InAttr-side view of the eventState section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type eventEventStateAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// eventEventStateSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type eventEventStateSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// eventEventAttemptAttrI is the InAttr-side view of the eventAttempt section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type eventEventAttemptAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// eventEventAttemptSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type eventEventAttemptSecI[Attr any, Ent any] interface {
	BeginAttribute(value uint32) Attr
	EndSection() Ent
}

// eventEventWorkerRunAttrI is the InAttr-side view of the eventWorkerRun section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type eventEventWorkerRunAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// eventEventWorkerRunSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type eventEventWorkerRunSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// eventEventErrorAttrI is the InAttr-side view of the eventError section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type eventEventErrorAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// eventEventErrorSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type eventEventErrorSecI[Attr any, Ent any] interface {
	BeginAttribute(value []byte) Attr
	EndSection() Ent
}

// eventEventNoteAttrI is the InAttr-side view of the eventNote section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type eventEventNoteAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// eventEventNoteSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type eventEventNoteSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// eventEntityI is the entity-builder surface eventAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type eventEntityI[
	EventStateAttr eventEventStateAttrI,
	EventStateSec eventEventStateSecI[EventStateAttr, Ent],
	EventAttemptAttr eventEventAttemptAttrI,
	EventAttemptSec eventEventAttemptSecI[EventAttemptAttr, Ent],
	EventWorkerRunAttr eventEventWorkerRunAttrI,
	EventWorkerRunSec eventEventWorkerRunSecI[EventWorkerRunAttr, Ent],
	EventErrorAttr eventEventErrorAttrI,
	EventErrorSec eventEventErrorSecI[EventErrorAttr, Ent],
	EventNoteAttr eventEventNoteAttrI,
	EventNoteSec eventEventNoteSecI[EventNoteAttr, Ent],
	Ent any,
] interface {
	GetSectionEventState() EventStateSec
	GetSectionEventAttempt() EventAttemptSec
	GetSectionEventWorkerRun() EventWorkerRunSec
	GetSectionEventError() EventErrorSec
	GetSectionEventNote() EventNoteSec
}

// eventEmitSectionEventState writes this kind's eventState attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func eventEmitSectionEventState[
	EventStateAttr eventEventStateAttrI,
	EventStateSec eventEventStateSecI[EventStateAttr, Ent],
	Ent any,
](eventStateSec EventStateSec, row Event) (err error) {
	eventStateSecAttr_State := eventStateSec.BeginAttribute(row.State)
	eventStateSecAttr_State.AddMembershipLowCardRefP(kindWatchbillEventState)
	eventStateSecAttr_State.EndAttributeP()
	return
}

// eventEmitSectionEventAttempt writes this kind's eventAttempt attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func eventEmitSectionEventAttempt[
	EventAttemptAttr eventEventAttemptAttrI,
	EventAttemptSec eventEventAttemptSecI[EventAttemptAttr, Ent],
	Ent any,
](eventAttemptSec EventAttemptSec, row Event) (err error) {
	eventAttemptSecAttr_Attempt := eventAttemptSec.BeginAttribute(row.Attempt)
	eventAttemptSecAttr_Attempt.AddMembershipLowCardRefP(kindWatchbillEventAttempt)
	eventAttemptSecAttr_Attempt.EndAttributeP()
	return
}

// eventEmitSectionEventWorkerRun writes this kind's eventWorkerRun attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func eventEmitSectionEventWorkerRun[
	EventWorkerRunAttr eventEventWorkerRunAttrI,
	EventWorkerRunSec eventEventWorkerRunSecI[EventWorkerRunAttr, Ent],
	Ent any,
](eventWorkerRunSec EventWorkerRunSec, row Event) (err error) {
	eventWorkerRunSecAttr_WorkerRun := eventWorkerRunSec.BeginAttribute(row.WorkerRun)
	eventWorkerRunSecAttr_WorkerRun.AddMembershipLowCardRefP(kindWatchbillEventWorkerRun)
	eventWorkerRunSecAttr_WorkerRun.EndAttributeP()
	return
}

// eventEmitSectionEventError writes this kind's eventError attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func eventEmitSectionEventError[
	EventErrorAttr eventEventErrorAttrI,
	EventErrorSec eventEventErrorSecI[EventErrorAttr, Ent],
	Ent any,
](eventErrorSec EventErrorSec, row Event) (err error) {
	eventErrorSecAttr_Error := eventErrorSec.BeginAttribute(row.Error)
	eventErrorSecAttr_Error.AddMembershipLowCardRefP(kindWatchbillEventError)
	eventErrorSecAttr_Error.EndAttributeP()
	return
}

// eventEmitSectionEventNote writes this kind's eventNote attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func eventEmitSectionEventNote[
	EventNoteAttr eventEventNoteAttrI,
	EventNoteSec eventEventNoteSecI[EventNoteAttr, Ent],
	Ent any,
](eventNoteSec EventNoteSec, row Event) (err error) {
	eventNoteSecAttr_Note := eventNoteSec.BeginAttribute(row.Note)
	eventNoteSecAttr_Note.AddMembershipLowCardRefP(kindWatchbillEventNote)
	eventNoteSecAttr_Note.EndAttributeP()
	return
}

// eventAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func eventAddSections[
	EventStateAttr eventEventStateAttrI,
	EventStateSec eventEventStateSecI[EventStateAttr, Ent],
	EventAttemptAttr eventEventAttemptAttrI,
	EventAttemptSec eventEventAttemptSecI[EventAttemptAttr, Ent],
	EventWorkerRunAttr eventEventWorkerRunAttrI,
	EventWorkerRunSec eventEventWorkerRunSecI[EventWorkerRunAttr, Ent],
	EventErrorAttr eventEventErrorAttrI,
	EventErrorSec eventEventErrorSecI[EventErrorAttr, Ent],
	EventNoteAttr eventEventNoteAttrI,
	EventNoteSec eventEventNoteSecI[EventNoteAttr, Ent],
	Ent any,
	DML eventEntityI[
		EventStateAttr, EventStateSec,
		EventAttemptAttr, EventAttemptSec,
		EventWorkerRunAttr, EventWorkerRunSec,
		EventErrorAttr, EventErrorSec,
		EventNoteAttr, EventNoteSec,
		Ent,
	],
](dml DML, row Event) (err error) {
	// --- eventState. ---
	eventStateSec := dml.GetSectionEventState()
	err = eventEmitSectionEventState(eventStateSec, row)
	if err != nil {
		return
	}
	eventStateSec.EndSection()
	// --- eventAttempt. ---
	eventAttemptSec := dml.GetSectionEventAttempt()
	err = eventEmitSectionEventAttempt(eventAttemptSec, row)
	if err != nil {
		return
	}
	eventAttemptSec.EndSection()
	// --- eventWorkerRun. ---
	eventWorkerRunSec := dml.GetSectionEventWorkerRun()
	err = eventEmitSectionEventWorkerRun(eventWorkerRunSec, row)
	if err != nil {
		return
	}
	eventWorkerRunSec.EndSection()
	// --- eventError. ---
	eventErrorSec := dml.GetSectionEventError()
	err = eventEmitSectionEventError(eventErrorSec, row)
	if err != nil {
		return
	}
	eventErrorSec.EndSection()
	// --- eventNote. ---
	eventNoteSec := dml.GetSectionEventNote()
	err = eventEmitSectionEventNote(eventNoteSec, row)
	if err != nil {
		return
	}
	eventNoteSec.EndSection()
	return
}

// eventEventStateAttrsReadI is the Attributes-side view of the eventState section.
type eventEventStateAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// eventEventStateMembsReadI is the Memberships-side view of the eventState section.
type eventEventStateMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// eventEventAttemptAttrsReadI is the Attributes-side view of the eventAttempt section.
type eventEventAttemptAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) uint32
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// eventEventAttemptMembsReadI is the Memberships-side view of the eventAttempt section.
type eventEventAttemptMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// eventEventWorkerRunAttrsReadI is the Attributes-side view of the eventWorkerRun section.
type eventEventWorkerRunAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// eventEventWorkerRunMembsReadI is the Memberships-side view of the eventWorkerRun section.
type eventEventWorkerRunMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// eventEventErrorAttrsReadI is the Attributes-side view of the eventError section.
type eventEventErrorAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) []byte
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// eventEventErrorMembsReadI is the Memberships-side view of the eventError section.
type eventEventErrorMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// eventEventNoteAttrsReadI is the Attributes-side view of the eventNote section.
type eventEventNoteAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// eventEventNoteMembsReadI is the Memberships-side view of the eventNote section.
type eventEventNoteMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// eventReadRow reads row i as one optional Event component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func eventReadRow[
	EventStateAttrs eventEventStateAttrsReadI,
	EventStateMembs eventEventStateMembsReadI,
	EventAttemptAttrs eventEventAttemptAttrsReadI,
	EventAttemptMembs eventEventAttemptMembsReadI,
	EventWorkerRunAttrs eventEventWorkerRunAttrsReadI,
	EventWorkerRunMembs eventEventWorkerRunMembsReadI,
	EventErrorAttrs eventEventErrorAttrsReadI,
	EventErrorMembs eventEventErrorMembsReadI,
	EventNoteAttrs eventEventNoteAttrsReadI,
	EventNoteMembs eventEventNoteMembsReadI,
](
	i int,
	eventStateAttrs EventStateAttrs,
	eventStateMembs EventStateMembs,
	eventAttemptAttrs EventAttemptAttrs,
	eventAttemptMembs EventAttemptMembs,
	eventWorkerRunAttrs EventWorkerRunAttrs,
	eventWorkerRunMembs EventWorkerRunMembs,
	eventErrorAttrs EventErrorAttrs,
	eventErrorMembs EventErrorMembs,
	eventNoteAttrs EventNoteAttrs,
	eventNoteMembs EventNoteMembs,
) (row Event, present bool, err error) {
	// --- eventState. ---
	var eventStateStateVal string
	var eventStateStateCount int
	var eventStateStateLastAttr int64
	neventState := eventStateAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < neventState; attrJ++ {
		for membID := range eventStateMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindWatchbillEventState:
				if eventStateStateLastAttr != attrJ+1 {
					eventStateStateLastAttr = attrJ + 1
					eventStateStateCount++
				}
				val := eventStateAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				eventStateStateVal = val
			}
		}
	}
	if eventStateStateCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "eventState").Str("membership", "watchbillEventState").Int("got", eventStateStateCount).Errorf("slot eventState@watchbillEventState (field State) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", eventStateStateCount)
		return
	}
	if eventStateStateCount == 1 {
		row.State = eventStateStateVal
		present = true
	}
	// --- eventAttempt. ---
	var eventAttemptAttemptVal uint32
	var eventAttemptAttemptCount int
	var eventAttemptAttemptLastAttr int64
	neventAttempt := eventAttemptAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < neventAttempt; attrJ++ {
		for membID := range eventAttemptMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindWatchbillEventAttempt:
				if eventAttemptAttemptLastAttr != attrJ+1 {
					eventAttemptAttemptLastAttr = attrJ + 1
					eventAttemptAttemptCount++
				}
				val := eventAttemptAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				eventAttemptAttemptVal = val
			}
		}
	}
	if eventAttemptAttemptCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "eventAttempt").Str("membership", "watchbillEventAttempt").Int("got", eventAttemptAttemptCount).Errorf("slot eventAttempt@watchbillEventAttempt (field Attempt) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", eventAttemptAttemptCount)
		return
	}
	if eventAttemptAttemptCount == 1 {
		row.Attempt = eventAttemptAttemptVal
		present = true
	}
	// --- eventWorkerRun. ---
	var eventWorkerRunWorkerRunVal string
	var eventWorkerRunWorkerRunCount int
	var eventWorkerRunWorkerRunLastAttr int64
	neventWorkerRun := eventWorkerRunAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < neventWorkerRun; attrJ++ {
		for membID := range eventWorkerRunMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindWatchbillEventWorkerRun:
				if eventWorkerRunWorkerRunLastAttr != attrJ+1 {
					eventWorkerRunWorkerRunLastAttr = attrJ + 1
					eventWorkerRunWorkerRunCount++
				}
				val := eventWorkerRunAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				eventWorkerRunWorkerRunVal = val
			}
		}
	}
	if eventWorkerRunWorkerRunCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "eventWorkerRun").Str("membership", "watchbillEventWorkerRun").Int("got", eventWorkerRunWorkerRunCount).Errorf("slot eventWorkerRun@watchbillEventWorkerRun (field WorkerRun) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", eventWorkerRunWorkerRunCount)
		return
	}
	if eventWorkerRunWorkerRunCount == 1 {
		row.WorkerRun = eventWorkerRunWorkerRunVal
		present = true
	}
	// --- eventError. ---
	var eventErrorErrorVal []byte
	var eventErrorErrorCount int
	var eventErrorErrorLastAttr int64
	neventError := eventErrorAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < neventError; attrJ++ {
		for membID := range eventErrorMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindWatchbillEventError:
				if eventErrorErrorLastAttr != attrJ+1 {
					eventErrorErrorLastAttr = attrJ + 1
					eventErrorErrorCount++
				}
				val := eventErrorAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				cp := make([]byte, len(val))
				copy(cp, val)
				eventErrorErrorVal = cp
			}
		}
	}
	if eventErrorErrorCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "eventError").Str("membership", "watchbillEventError").Int("got", eventErrorErrorCount).Errorf("slot eventError@watchbillEventError (field Error) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", eventErrorErrorCount)
		return
	}
	if eventErrorErrorCount == 1 {
		row.Error = eventErrorErrorVal
		present = true
	}
	// --- eventNote. ---
	var eventNoteNoteVal string
	var eventNoteNoteCount int
	var eventNoteNoteLastAttr int64
	neventNote := eventNoteAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < neventNote; attrJ++ {
		for membID := range eventNoteMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindWatchbillEventNote:
				if eventNoteNoteLastAttr != attrJ+1 {
					eventNoteNoteLastAttr = attrJ + 1
					eventNoteNoteCount++
				}
				val := eventNoteAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				eventNoteNoteVal = val
			}
		}
	}
	if eventNoteNoteCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "eventNote").Str("membership", "watchbillEventNote").Int("got", eventNoteNoteCount).Errorf("slot eventNote@watchbillEventNote (field Note) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", eventNoteNoteCount)
		return
	}
	if eventNoteNoteCount == 1 {
		row.Note = eventNoteNoteVal
		present = true
	}
	return
}
