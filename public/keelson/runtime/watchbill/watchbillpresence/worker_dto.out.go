// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package watchbillpresence

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindRuntimeKindWatchbillWorker uint64 = 9223372049739677791
	kindWatchbillWorkerRunId       uint64 = 9223372049739677792
	kindWatchbillWorkerHost        uint64 = 9223372049739677793
	kindWatchbillWorkerPhase       uint64 = 9223372049739677794
	kindWatchbillWorkerKinds       uint64 = 9223372049739677795
	kindWatchbillWorkerQueues      uint64 = 9223372049739677796
	kindWatchbillWorkerMaxWorkers  uint64 = 9223372049739677797
)

// workerPresenceSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type workerPresenceSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// workerPresenceSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type workerPresenceSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// workerPresenceSymbolArrayAttrI is the InAttr-side view of the symbolArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type workerPresenceSymbolArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value string)
	EndAttributeP()
}

// workerPresenceSymbolArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type workerPresenceSymbolArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// workerPresenceU32ArrayAttrI is the InAttr-side view of the u32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type workerPresenceU32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// workerPresenceU32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type workerPresenceU32ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint32) Attr
	EndSection() Ent
}

// workerPresenceEntityI is the entity-builder surface workerPresenceAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type workerPresenceEntityI[
	SymbolAttr workerPresenceSymbolAttrI,
	SymbolSec workerPresenceSymbolSecI[SymbolAttr, Ent],
	SymbolArrayAttr workerPresenceSymbolArrayAttrI,
	SymbolArraySec workerPresenceSymbolArraySecI[SymbolArrayAttr, Ent],
	U32ArrayAttr workerPresenceU32ArrayAttrI,
	U32ArraySec workerPresenceU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionSymbolArray() SymbolArraySec
	GetSectionU32Array() U32ArraySec
}

// workerPresenceEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func workerPresenceEmitSectionSymbol[
	SymbolAttr workerPresenceSymbolAttrI,
	SymbolSec workerPresenceSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row WorkerPresence) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindRuntimeKindWatchbillWorker)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_RunId := symbolSec.BeginAttribute(row.RunId)
	symbolSecAttr_RunId.AddMembershipLowCardRefP(kindWatchbillWorkerRunId)
	symbolSecAttr_RunId.EndAttributeP()
	symbolSecAttr_Host := symbolSec.BeginAttribute(row.Host)
	symbolSecAttr_Host.AddMembershipLowCardRefP(kindWatchbillWorkerHost)
	symbolSecAttr_Host.EndAttributeP()
	symbolSecAttr_Phase := symbolSec.BeginAttribute(row.Phase)
	symbolSecAttr_Phase.AddMembershipLowCardRefP(kindWatchbillWorkerPhase)
	symbolSecAttr_Phase.EndAttributeP()
	return
}

// workerPresenceEmitSectionSymbolArray writes this kind's symbolArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func workerPresenceEmitSectionSymbolArray[
	SymbolArrayAttr workerPresenceSymbolArrayAttrI,
	SymbolArraySec workerPresenceSymbolArraySecI[SymbolArrayAttr, Ent],
	Ent any,
](symbolArraySec SymbolArraySec, row WorkerPresence) (err error) {
	if len(row.Kinds) > 0 {
		symbolArraySecAttr_Kinds := symbolArraySec.BeginAttribute()
		for _, v := range row.Kinds {
			symbolArraySecAttr_Kinds.AddToContainerP(v)
		}
		symbolArraySecAttr_Kinds.AddMembershipLowCardRefP(kindWatchbillWorkerKinds)
		symbolArraySecAttr_Kinds.EndAttributeP()
	}
	if len(row.Queues) > 0 {
		symbolArraySecAttr_Queues := symbolArraySec.BeginAttribute()
		for _, v := range row.Queues {
			symbolArraySecAttr_Queues.AddToContainerP(v)
		}
		symbolArraySecAttr_Queues.AddMembershipLowCardRefP(kindWatchbillWorkerQueues)
		symbolArraySecAttr_Queues.EndAttributeP()
	}
	return
}

// workerPresenceEmitSectionU32Array writes this kind's u32Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func workerPresenceEmitSectionU32Array[
	U32ArrayAttr workerPresenceU32ArrayAttrI,
	U32ArraySec workerPresenceU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
](u32ArraySec U32ArraySec, row WorkerPresence) (err error) {
	u32ArraySecAttr_MaxWorkers := u32ArraySec.BeginAttributeSingle(row.MaxWorkers)
	u32ArraySecAttr_MaxWorkers.AddMembershipLowCardRefP(kindWatchbillWorkerMaxWorkers)
	u32ArraySecAttr_MaxWorkers.EndAttributeP()
	return
}

// workerPresenceAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func workerPresenceAddSections[
	SymbolAttr workerPresenceSymbolAttrI,
	SymbolSec workerPresenceSymbolSecI[SymbolAttr, Ent],
	SymbolArrayAttr workerPresenceSymbolArrayAttrI,
	SymbolArraySec workerPresenceSymbolArraySecI[SymbolArrayAttr, Ent],
	U32ArrayAttr workerPresenceU32ArrayAttrI,
	U32ArraySec workerPresenceU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
	DML workerPresenceEntityI[
		SymbolAttr, SymbolSec,
		SymbolArrayAttr, SymbolArraySec,
		U32ArrayAttr, U32ArraySec,
		Ent,
	],
](dml DML, row WorkerPresence) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = workerPresenceEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- symbolArray. ---
	symbolArraySec := dml.GetSectionSymbolArray()
	err = workerPresenceEmitSectionSymbolArray(symbolArraySec, row)
	if err != nil {
		return
	}
	symbolArraySec.EndSection()
	// --- u32Array. ---
	u32ArraySec := dml.GetSectionU32Array()
	err = workerPresenceEmitSectionU32Array(u32ArraySec, row)
	if err != nil {
		return
	}
	u32ArraySec.EndSection()
	return
}

// workerPresenceSymbolAttrsReadI is the Attributes-side view of the symbol section.
type workerPresenceSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// workerPresenceSymbolMembsReadI is the Memberships-side view of the symbol section.
type workerPresenceSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// workerPresenceSymbolArrayAttrsReadI is the Attributes-side view of the symbolArray section.
type workerPresenceSymbolArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[string]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// workerPresenceSymbolArrayMembsReadI is the Memberships-side view of the symbolArray section.
type workerPresenceSymbolArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// workerPresenceU32ArrayAttrsReadI is the Attributes-side view of the u32Array section.
type workerPresenceU32ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint32, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// workerPresenceU32ArrayMembsReadI is the Memberships-side view of the u32Array section.
type workerPresenceU32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// workerPresenceReadRow reads row i as one optional WorkerPresence component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func workerPresenceReadRow[
	SymbolAttrs workerPresenceSymbolAttrsReadI,
	SymbolMembs workerPresenceSymbolMembsReadI,
	SymbolArrayAttrs workerPresenceSymbolArrayAttrsReadI,
	SymbolArrayMembs workerPresenceSymbolArrayMembsReadI,
	U32ArrayAttrs workerPresenceU32ArrayAttrsReadI,
	U32ArrayMembs workerPresenceU32ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	symbolArrayAttrs SymbolArrayAttrs,
	symbolArrayMembs SymbolArrayMembs,
	u32ArrayAttrs U32ArrayAttrs,
	u32ArrayMembs U32ArrayMembs,
) (row WorkerPresence, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolRunIdVal string
	var symbolRunIdCount int
	var symbolRunIdLastAttr int64
	var symbolHostVal string
	var symbolHostCount int
	var symbolHostLastAttr int64
	var symbolPhaseVal string
	var symbolPhaseCount int
	var symbolPhaseLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindRuntimeKindWatchbillWorker:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindWatchbillWorkerRunId:
				if symbolRunIdLastAttr != attrJ+1 {
					symbolRunIdLastAttr = attrJ + 1
					symbolRunIdCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolRunIdVal = val
			case kindWatchbillWorkerHost:
				if symbolHostLastAttr != attrJ+1 {
					symbolHostLastAttr = attrJ + 1
					symbolHostCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolHostVal = val
			case kindWatchbillWorkerPhase:
				if symbolPhaseLastAttr != attrJ+1 {
					symbolPhaseLastAttr = attrJ + 1
					symbolPhaseCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolPhaseVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "runtimeKindWatchbillWorker").Int("got", symbolKindCount).Errorf("slot symbol@runtimeKindWatchbillWorker (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolRunIdCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "watchbillWorkerRunId").Int("got", symbolRunIdCount).Errorf("slot symbol@watchbillWorkerRunId (field RunId) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolRunIdCount)
		return
	}
	if symbolRunIdCount == 1 {
		row.RunId = symbolRunIdVal
		present = true
	}
	if symbolHostCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "watchbillWorkerHost").Int("got", symbolHostCount).Errorf("slot symbol@watchbillWorkerHost (field Host) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolHostCount)
		return
	}
	if symbolHostCount == 1 {
		row.Host = symbolHostVal
		present = true
	}
	if symbolPhaseCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "watchbillWorkerPhase").Int("got", symbolPhaseCount).Errorf("slot symbol@watchbillWorkerPhase (field Phase) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolPhaseCount)
		return
	}
	if symbolPhaseCount == 1 {
		row.Phase = symbolPhaseVal
		present = true
	}
	// --- symbolArray. ---
	var symbolArrayKindsSlice []string
	var symbolArrayKindsCount int
	var symbolArrayKindsLastAttr int64
	var symbolArrayQueuesSlice []string
	var symbolArrayQueuesCount int
	var symbolArrayQueuesLastAttr int64
	nsymbolArray := symbolArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbolArray; attrJ++ {
		for membID := range symbolArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindWatchbillWorkerKinds:
				if symbolArrayKindsLastAttr != attrJ+1 {
					symbolArrayKindsLastAttr = attrJ + 1
					symbolArrayKindsCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayKindsSlice = append(symbolArrayKindsSlice, v)
				}
			case kindWatchbillWorkerQueues:
				if symbolArrayQueuesLastAttr != attrJ+1 {
					symbolArrayQueuesLastAttr = attrJ + 1
					symbolArrayQueuesCount++
				}
				for v := range symbolArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					symbolArrayQueuesSlice = append(symbolArrayQueuesSlice, v)
				}
			}
		}
	}
	if symbolArrayKindsCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "watchbillWorkerKinds").Int("got", symbolArrayKindsCount).Errorf("slot symbolArray@watchbillWorkerKinds (field Kinds) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayKindsCount)
		return
	}
	if symbolArrayKindsSlice != nil {
		row.Kinds = symbolArrayKindsSlice
		present = true
	}
	if symbolArrayQueuesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbolArray").Str("membership", "watchbillWorkerQueues").Int("got", symbolArrayQueuesCount).Errorf("slot symbolArray@watchbillWorkerQueues (field Queues) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolArrayQueuesCount)
		return
	}
	if symbolArrayQueuesSlice != nil {
		row.Queues = symbolArrayQueuesSlice
		present = true
	}
	// --- u32Array. ---
	var u32ArrayMaxWorkersVal uint32
	var u32ArrayMaxWorkersCount int
	var u32ArrayMaxWorkersLastAttr int64
	nu32Array := u32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu32Array; attrJ++ {
		for membID := range u32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindWatchbillWorkerMaxWorkers:
				if u32ArrayMaxWorkersLastAttr != attrJ+1 {
					u32ArrayMaxWorkersLastAttr = attrJ + 1
					u32ArrayMaxWorkersCount++
				}
				val, valErr := u32ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "watchbillWorkerMaxWorkers").Str("field", "MaxWorkers").Errorf("slot u32Array@watchbillWorkerMaxWorkers (field MaxWorkers) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u32ArrayMaxWorkersVal = val
			}
		}
	}
	if u32ArrayMaxWorkersCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "watchbillWorkerMaxWorkers").Int("got", u32ArrayMaxWorkersCount).Errorf("slot u32Array@watchbillWorkerMaxWorkers (field MaxWorkers) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayMaxWorkersCount)
		return
	}
	if u32ArrayMaxWorkersCount == 1 {
		row.MaxWorkers = u32ArrayMaxWorkersVal
		present = true
	}
	return
}
