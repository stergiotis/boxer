// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package ladingmeta

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindLadingKindSnapshot uint64 = 10376293554346524673
	kindLadingSnapEntries  uint64 = 10376293554346524687
	kindLadingSnapBytes    uint64 = 10376293554346524688
	kindLadingTtlClass     uint64 = 10376293554346524689
	kindLadingTextRule     uint64 = 10376293554346524690
	kindLadingInlineMax    uint64 = 10376293554346524691
)

// ladingSnapshotSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingSnapshotSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingSnapshotSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingSnapshotSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// ladingSnapshotU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingSnapshotU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingSnapshotU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingSnapshotU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// ladingSnapshotEntityI is the entity-builder surface ladingSnapshotAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type ladingSnapshotEntityI[
	SymbolAttr ladingSnapshotSymbolAttrI,
	SymbolSec ladingSnapshotSymbolSecI[SymbolAttr, Ent],
	U64ArrayAttr ladingSnapshotU64ArrayAttrI,
	U64ArraySec ladingSnapshotU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionU64Array() U64ArraySec
}

// ladingSnapshotEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingSnapshotEmitSectionSymbol[
	SymbolAttr ladingSnapshotSymbolAttrI,
	SymbolSec ladingSnapshotSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row LadingSnapshot) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindLadingKindSnapshot)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_TtlClass := symbolSec.BeginAttribute(row.TtlClass)
	symbolSecAttr_TtlClass.AddMembershipLowCardRefP(kindLadingTtlClass)
	symbolSecAttr_TtlClass.EndAttributeP()
	symbolSecAttr_TextRule := symbolSec.BeginAttribute(row.TextRule)
	symbolSecAttr_TextRule.AddMembershipLowCardRefP(kindLadingTextRule)
	symbolSecAttr_TextRule.EndAttributeP()
	return
}

// ladingSnapshotEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingSnapshotEmitSectionU64Array[
	U64ArrayAttr ladingSnapshotU64ArrayAttrI,
	U64ArraySec ladingSnapshotU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row LadingSnapshot) (err error) {
	u64ArraySecAttr_Entries := u64ArraySec.BeginAttributeSingle(row.Entries)
	u64ArraySecAttr_Entries.AddMembershipLowCardRefP(kindLadingSnapEntries)
	u64ArraySecAttr_Entries.EndAttributeP()
	u64ArraySecAttr_Bytes := u64ArraySec.BeginAttributeSingle(row.Bytes)
	u64ArraySecAttr_Bytes.AddMembershipLowCardRefP(kindLadingSnapBytes)
	u64ArraySecAttr_Bytes.EndAttributeP()
	u64ArraySecAttr_InlineMax := u64ArraySec.BeginAttributeSingle(row.InlineMax)
	u64ArraySecAttr_InlineMax.AddMembershipLowCardRefP(kindLadingInlineMax)
	u64ArraySecAttr_InlineMax.EndAttributeP()
	return
}

// ladingSnapshotAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func ladingSnapshotAddSections[
	SymbolAttr ladingSnapshotSymbolAttrI,
	SymbolSec ladingSnapshotSymbolSecI[SymbolAttr, Ent],
	U64ArrayAttr ladingSnapshotU64ArrayAttrI,
	U64ArraySec ladingSnapshotU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
	DML ladingSnapshotEntityI[
		SymbolAttr, SymbolSec,
		U64ArrayAttr, U64ArraySec,
		Ent,
	],
](dml DML, row LadingSnapshot) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = ladingSnapshotEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = ladingSnapshotEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	return
}

// ladingSnapshotSymbolAttrsReadI is the Attributes-side view of the symbol section.
type ladingSnapshotSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingSnapshotSymbolMembsReadI is the Memberships-side view of the symbol section.
type ladingSnapshotSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingSnapshotU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type ladingSnapshotU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingSnapshotU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type ladingSnapshotU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingSnapshotReadRow reads row i as one optional LadingSnapshot component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func ladingSnapshotReadRow[
	SymbolAttrs ladingSnapshotSymbolAttrsReadI,
	SymbolMembs ladingSnapshotSymbolMembsReadI,
	U64ArrayAttrs ladingSnapshotU64ArrayAttrsReadI,
	U64ArrayMembs ladingSnapshotU64ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
) (row LadingSnapshot, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolTtlClassVal string
	var symbolTtlClassCount int
	var symbolTtlClassLastAttr int64
	var symbolTextRuleVal string
	var symbolTextRuleCount int
	var symbolTextRuleLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingKindSnapshot:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindLadingTtlClass:
				if symbolTtlClassLastAttr != attrJ+1 {
					symbolTtlClassLastAttr = attrJ + 1
					symbolTtlClassCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolTtlClassVal = val
			case kindLadingTextRule:
				if symbolTextRuleLastAttr != attrJ+1 {
					symbolTextRuleLastAttr = attrJ + 1
					symbolTextRuleCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolTextRuleVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "ladingKindSnapshot").Int("got", symbolKindCount).Errorf("slot symbol@ladingKindSnapshot (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolTtlClassCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "ladingTtlClass").Int("got", symbolTtlClassCount).Errorf("slot symbol@ladingTtlClass (field TtlClass) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolTtlClassCount)
		return
	}
	if symbolTtlClassCount == 1 {
		row.TtlClass = symbolTtlClassVal
		present = true
	}
	if symbolTextRuleCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "ladingTextRule").Int("got", symbolTextRuleCount).Errorf("slot symbol@ladingTextRule (field TextRule) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolTextRuleCount)
		return
	}
	if symbolTextRuleCount == 1 {
		row.TextRule = symbolTextRuleVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayEntriesVal uint64
	var u64ArrayEntriesCount int
	var u64ArrayEntriesLastAttr int64
	var u64ArrayBytesVal uint64
	var u64ArrayBytesCount int
	var u64ArrayBytesLastAttr int64
	var u64ArrayInlineMaxVal uint64
	var u64ArrayInlineMaxCount int
	var u64ArrayInlineMaxLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingSnapEntries:
				if u64ArrayEntriesLastAttr != attrJ+1 {
					u64ArrayEntriesLastAttr = attrJ + 1
					u64ArrayEntriesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingSnapEntries").Str("field", "Entries").Errorf("slot u64Array@ladingSnapEntries (field Entries) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayEntriesVal = val
			case kindLadingSnapBytes:
				if u64ArrayBytesLastAttr != attrJ+1 {
					u64ArrayBytesLastAttr = attrJ + 1
					u64ArrayBytesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingSnapBytes").Str("field", "Bytes").Errorf("slot u64Array@ladingSnapBytes (field Bytes) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayBytesVal = val
			case kindLadingInlineMax:
				if u64ArrayInlineMaxLastAttr != attrJ+1 {
					u64ArrayInlineMaxLastAttr = attrJ + 1
					u64ArrayInlineMaxCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingInlineMax").Str("field", "InlineMax").Errorf("slot u64Array@ladingInlineMax (field InlineMax) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayInlineMaxVal = val
			}
		}
	}
	if u64ArrayEntriesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingSnapEntries").Int("got", u64ArrayEntriesCount).Errorf("slot u64Array@ladingSnapEntries (field Entries) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayEntriesCount)
		return
	}
	if u64ArrayEntriesCount == 1 {
		row.Entries = u64ArrayEntriesVal
		present = true
	}
	if u64ArrayBytesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingSnapBytes").Int("got", u64ArrayBytesCount).Errorf("slot u64Array@ladingSnapBytes (field Bytes) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayBytesCount)
		return
	}
	if u64ArrayBytesCount == 1 {
		row.Bytes = u64ArrayBytesVal
		present = true
	}
	if u64ArrayInlineMaxCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingInlineMax").Int("got", u64ArrayInlineMaxCount).Errorf("slot u64Array@ladingInlineMax (field InlineMax) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayInlineMaxCount)
		return
	}
	if u64ArrayInlineMaxCount == 1 {
		row.InlineMax = u64ArrayInlineMaxVal
		present = true
	}
	return
}
