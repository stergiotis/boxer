// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package ladingmeta

import (
	"iter"
	"time"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindLadingKindEntry   uint64 = 10376293554346524672
	kindLadingNodeKind    uint64 = 10376293554346524676
	kindLadingContent     uint64 = 10376293554346524677
	kindLadingMode        uint64 = 10376293554346524678
	kindLadingBlockSize   uint64 = 10376293554346524683
	kindLadingBlocks      uint64 = 10376293554346524684
	kindLadingSize        uint64 = 10376293554346524679
	kindLadingMtime       uint64 = 10376293554346524680
	kindLadingLinkTarget  uint64 = 10376293554346524681
	kindLadingErr         uint64 = 10376293554346524686
	kindLadingContentHash uint64 = 10376293554346524682
	kindLadingText        uint64 = 10376293554346524685
)

// ladingEntrySymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingEntrySymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingEntrySymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingEntrySymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// ladingEntryU32ArrayAttrI is the InAttr-side view of the u32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingEntryU32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingEntryU32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingEntryU32ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint32) Attr
	EndSection() Ent
}

// ladingEntryU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingEntryU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingEntryU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingEntryU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// ladingEntryTimeArrayAttrI is the InAttr-side view of the timeArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingEntryTimeArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingEntryTimeArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingEntryTimeArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value time.Time) Attr
	EndSection() Ent
}

// ladingEntryStringArrayAttrI is the InAttr-side view of the stringArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingEntryStringArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingEntryStringArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingEntryStringArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// ladingEntryBlobArrayAttrI is the InAttr-side view of the blobArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingEntryBlobArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingEntryBlobArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingEntryBlobArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value []byte) Attr
	EndSection() Ent
}

// ladingEntryBoolAttrI is the InAttr-side view of the bool section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingEntryBoolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingEntryBoolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingEntryBoolSecI[Attr any, Ent any] interface {
	BeginAttribute(value bool) Attr
	EndSection() Ent
}

// ladingEntryEntityI is the entity-builder surface ladingEntryAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type ladingEntryEntityI[
	SymbolAttr ladingEntrySymbolAttrI,
	SymbolSec ladingEntrySymbolSecI[SymbolAttr, Ent],
	U32ArrayAttr ladingEntryU32ArrayAttrI,
	U32ArraySec ladingEntryU32ArraySecI[U32ArrayAttr, Ent],
	U64ArrayAttr ladingEntryU64ArrayAttrI,
	U64ArraySec ladingEntryU64ArraySecI[U64ArrayAttr, Ent],
	TimeArrayAttr ladingEntryTimeArrayAttrI,
	TimeArraySec ladingEntryTimeArraySecI[TimeArrayAttr, Ent],
	StringArrayAttr ladingEntryStringArrayAttrI,
	StringArraySec ladingEntryStringArraySecI[StringArrayAttr, Ent],
	BlobArrayAttr ladingEntryBlobArrayAttrI,
	BlobArraySec ladingEntryBlobArraySecI[BlobArrayAttr, Ent],
	BoolAttr ladingEntryBoolAttrI,
	BoolSec ladingEntryBoolSecI[BoolAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionU32Array() U32ArraySec
	GetSectionU64Array() U64ArraySec
	GetSectionTimeArray() TimeArraySec
	GetSectionStringArray() StringArraySec
	GetSectionBlobArray() BlobArraySec
	GetSectionBool() BoolSec
}

// ladingEntryEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingEntryEmitSectionSymbol[
	SymbolAttr ladingEntrySymbolAttrI,
	SymbolSec ladingEntrySymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row LadingEntry) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindLadingKindEntry)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_NodeKind := symbolSec.BeginAttribute(row.NodeKind)
	symbolSecAttr_NodeKind.AddMembershipLowCardRefP(kindLadingNodeKind)
	symbolSecAttr_NodeKind.EndAttributeP()
	symbolSecAttr_Content := symbolSec.BeginAttribute(row.Content)
	symbolSecAttr_Content.AddMembershipLowCardRefP(kindLadingContent)
	symbolSecAttr_Content.EndAttributeP()
	return
}

// ladingEntryEmitSectionU32Array writes this kind's u32Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingEntryEmitSectionU32Array[
	U32ArrayAttr ladingEntryU32ArrayAttrI,
	U32ArraySec ladingEntryU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
](u32ArraySec U32ArraySec, row LadingEntry) (err error) {
	u32ArraySecAttr_Mode := u32ArraySec.BeginAttributeSingle(row.Mode)
	u32ArraySecAttr_Mode.AddMembershipLowCardRefP(kindLadingMode)
	u32ArraySecAttr_Mode.EndAttributeP()
	u32ArraySecAttr_BlockSize := u32ArraySec.BeginAttributeSingle(row.BlockSize)
	u32ArraySecAttr_BlockSize.AddMembershipLowCardRefP(kindLadingBlockSize)
	u32ArraySecAttr_BlockSize.EndAttributeP()
	u32ArraySecAttr_Blocks := u32ArraySec.BeginAttributeSingle(row.Blocks)
	u32ArraySecAttr_Blocks.AddMembershipLowCardRefP(kindLadingBlocks)
	u32ArraySecAttr_Blocks.EndAttributeP()
	return
}

// ladingEntryEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingEntryEmitSectionU64Array[
	U64ArrayAttr ladingEntryU64ArrayAttrI,
	U64ArraySec ladingEntryU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row LadingEntry) (err error) {
	u64ArraySecAttr_Size := u64ArraySec.BeginAttributeSingle(row.Size)
	u64ArraySecAttr_Size.AddMembershipLowCardRefP(kindLadingSize)
	u64ArraySecAttr_Size.EndAttributeP()
	return
}

// ladingEntryEmitSectionTimeArray writes this kind's timeArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingEntryEmitSectionTimeArray[
	TimeArrayAttr ladingEntryTimeArrayAttrI,
	TimeArraySec ladingEntryTimeArraySecI[TimeArrayAttr, Ent],
	Ent any,
](timeArraySec TimeArraySec, row LadingEntry) (err error) {
	timeArraySecAttr_Mtime := timeArraySec.BeginAttributeSingle(row.Mtime)
	timeArraySecAttr_Mtime.AddMembershipLowCardRefP(kindLadingMtime)
	timeArraySecAttr_Mtime.EndAttributeP()
	return
}

// ladingEntryEmitSectionStringArray writes this kind's stringArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingEntryEmitSectionStringArray[
	StringArrayAttr ladingEntryStringArrayAttrI,
	StringArraySec ladingEntryStringArraySecI[StringArrayAttr, Ent],
	Ent any,
](stringArraySec StringArraySec, row LadingEntry) (err error) {
	stringArraySecAttr_LinkTarget := stringArraySec.BeginAttributeSingle(row.LinkTarget)
	stringArraySecAttr_LinkTarget.AddMembershipLowCardRefP(kindLadingLinkTarget)
	stringArraySecAttr_LinkTarget.EndAttributeP()
	stringArraySecAttr_Err := stringArraySec.BeginAttributeSingle(row.Err)
	stringArraySecAttr_Err.AddMembershipLowCardRefP(kindLadingErr)
	stringArraySecAttr_Err.EndAttributeP()
	return
}

// ladingEntryEmitSectionBlobArray writes this kind's blobArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingEntryEmitSectionBlobArray[
	BlobArrayAttr ladingEntryBlobArrayAttrI,
	BlobArraySec ladingEntryBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
](blobArraySec BlobArraySec, row LadingEntry) (err error) {
	blobArraySecAttr_ContentHash := blobArraySec.BeginAttributeSingle(row.ContentHash)
	blobArraySecAttr_ContentHash.AddMembershipLowCardRefP(kindLadingContentHash)
	blobArraySecAttr_ContentHash.EndAttributeP()
	return
}

// ladingEntryEmitSectionBool writes this kind's bool attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingEntryEmitSectionBool[
	BoolAttr ladingEntryBoolAttrI,
	BoolSec ladingEntryBoolSecI[BoolAttr, Ent],
	Ent any,
](boolSec BoolSec, row LadingEntry) (err error) {
	boolSecAttr_Text := boolSec.BeginAttribute(row.Text)
	boolSecAttr_Text.AddMembershipLowCardRefP(kindLadingText)
	boolSecAttr_Text.EndAttributeP()
	return
}

// ladingEntryAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func ladingEntryAddSections[
	SymbolAttr ladingEntrySymbolAttrI,
	SymbolSec ladingEntrySymbolSecI[SymbolAttr, Ent],
	U32ArrayAttr ladingEntryU32ArrayAttrI,
	U32ArraySec ladingEntryU32ArraySecI[U32ArrayAttr, Ent],
	U64ArrayAttr ladingEntryU64ArrayAttrI,
	U64ArraySec ladingEntryU64ArraySecI[U64ArrayAttr, Ent],
	TimeArrayAttr ladingEntryTimeArrayAttrI,
	TimeArraySec ladingEntryTimeArraySecI[TimeArrayAttr, Ent],
	StringArrayAttr ladingEntryStringArrayAttrI,
	StringArraySec ladingEntryStringArraySecI[StringArrayAttr, Ent],
	BlobArrayAttr ladingEntryBlobArrayAttrI,
	BlobArraySec ladingEntryBlobArraySecI[BlobArrayAttr, Ent],
	BoolAttr ladingEntryBoolAttrI,
	BoolSec ladingEntryBoolSecI[BoolAttr, Ent],
	Ent any,
	DML ladingEntryEntityI[
		SymbolAttr, SymbolSec,
		U32ArrayAttr, U32ArraySec,
		U64ArrayAttr, U64ArraySec,
		TimeArrayAttr, TimeArraySec,
		StringArrayAttr, StringArraySec,
		BlobArrayAttr, BlobArraySec,
		BoolAttr, BoolSec,
		Ent,
	],
](dml DML, row LadingEntry) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = ladingEntryEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- u32Array. ---
	u32ArraySec := dml.GetSectionU32Array()
	err = ladingEntryEmitSectionU32Array(u32ArraySec, row)
	if err != nil {
		return
	}
	u32ArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = ladingEntryEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	// --- timeArray. ---
	timeArraySec := dml.GetSectionTimeArray()
	err = ladingEntryEmitSectionTimeArray(timeArraySec, row)
	if err != nil {
		return
	}
	timeArraySec.EndSection()
	// --- stringArray. ---
	stringArraySec := dml.GetSectionStringArray()
	err = ladingEntryEmitSectionStringArray(stringArraySec, row)
	if err != nil {
		return
	}
	stringArraySec.EndSection()
	// --- blobArray. ---
	blobArraySec := dml.GetSectionBlobArray()
	err = ladingEntryEmitSectionBlobArray(blobArraySec, row)
	if err != nil {
		return
	}
	blobArraySec.EndSection()
	// --- bool. ---
	boolSec := dml.GetSectionBool()
	err = ladingEntryEmitSectionBool(boolSec, row)
	if err != nil {
		return
	}
	boolSec.EndSection()
	return
}

// ladingEntrySymbolAttrsReadI is the Attributes-side view of the symbol section.
type ladingEntrySymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingEntrySymbolMembsReadI is the Memberships-side view of the symbol section.
type ladingEntrySymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingEntryU32ArrayAttrsReadI is the Attributes-side view of the u32Array section.
type ladingEntryU32ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint32, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingEntryU32ArrayMembsReadI is the Memberships-side view of the u32Array section.
type ladingEntryU32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingEntryU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type ladingEntryU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingEntryU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type ladingEntryU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingEntryTimeArrayAttrsReadI is the Attributes-side view of the timeArray section.
type ladingEntryTimeArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (time.Time, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingEntryTimeArrayMembsReadI is the Memberships-side view of the timeArray section.
type ladingEntryTimeArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingEntryStringArrayAttrsReadI is the Attributes-side view of the stringArray section.
type ladingEntryStringArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingEntryStringArrayMembsReadI is the Memberships-side view of the stringArray section.
type ladingEntryStringArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingEntryBlobArrayAttrsReadI is the Attributes-side view of the blobArray section.
type ladingEntryBlobArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) ([]byte, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingEntryBlobArrayMembsReadI is the Memberships-side view of the blobArray section.
type ladingEntryBlobArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingEntryBoolAttrsReadI is the Attributes-side view of the bool section.
type ladingEntryBoolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) bool
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingEntryBoolMembsReadI is the Memberships-side view of the bool section.
type ladingEntryBoolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingEntryReadRow reads row i as one optional LadingEntry component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func ladingEntryReadRow[
	SymbolAttrs ladingEntrySymbolAttrsReadI,
	SymbolMembs ladingEntrySymbolMembsReadI,
	U32ArrayAttrs ladingEntryU32ArrayAttrsReadI,
	U32ArrayMembs ladingEntryU32ArrayMembsReadI,
	U64ArrayAttrs ladingEntryU64ArrayAttrsReadI,
	U64ArrayMembs ladingEntryU64ArrayMembsReadI,
	TimeArrayAttrs ladingEntryTimeArrayAttrsReadI,
	TimeArrayMembs ladingEntryTimeArrayMembsReadI,
	StringArrayAttrs ladingEntryStringArrayAttrsReadI,
	StringArrayMembs ladingEntryStringArrayMembsReadI,
	BlobArrayAttrs ladingEntryBlobArrayAttrsReadI,
	BlobArrayMembs ladingEntryBlobArrayMembsReadI,
	BoolAttrs ladingEntryBoolAttrsReadI,
	BoolMembs ladingEntryBoolMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	u32ArrayAttrs U32ArrayAttrs,
	u32ArrayMembs U32ArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
	timeArrayAttrs TimeArrayAttrs,
	timeArrayMembs TimeArrayMembs,
	stringArrayAttrs StringArrayAttrs,
	stringArrayMembs StringArrayMembs,
	blobArrayAttrs BlobArrayAttrs,
	blobArrayMembs BlobArrayMembs,
	boolAttrs BoolAttrs,
	boolMembs BoolMembs,
) (row LadingEntry, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolNodeKindVal string
	var symbolNodeKindCount int
	var symbolNodeKindLastAttr int64
	var symbolContentVal string
	var symbolContentCount int
	var symbolContentLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingKindEntry:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindLadingNodeKind:
				if symbolNodeKindLastAttr != attrJ+1 {
					symbolNodeKindLastAttr = attrJ + 1
					symbolNodeKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolNodeKindVal = val
			case kindLadingContent:
				if symbolContentLastAttr != attrJ+1 {
					symbolContentLastAttr = attrJ + 1
					symbolContentCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolContentVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "ladingKindEntry").Int("got", symbolKindCount).Errorf("slot symbol@ladingKindEntry (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolNodeKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "ladingNodeKind").Int("got", symbolNodeKindCount).Errorf("slot symbol@ladingNodeKind (field NodeKind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolNodeKindCount)
		return
	}
	if symbolNodeKindCount == 1 {
		row.NodeKind = symbolNodeKindVal
		present = true
	}
	if symbolContentCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "ladingContent").Int("got", symbolContentCount).Errorf("slot symbol@ladingContent (field Content) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolContentCount)
		return
	}
	if symbolContentCount == 1 {
		row.Content = symbolContentVal
		present = true
	}
	// --- u32Array. ---
	var u32ArrayModeVal uint32
	var u32ArrayModeCount int
	var u32ArrayModeLastAttr int64
	var u32ArrayBlockSizeVal uint32
	var u32ArrayBlockSizeCount int
	var u32ArrayBlockSizeLastAttr int64
	var u32ArrayBlocksVal uint32
	var u32ArrayBlocksCount int
	var u32ArrayBlocksLastAttr int64
	nu32Array := u32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu32Array; attrJ++ {
		for membID := range u32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingMode:
				if u32ArrayModeLastAttr != attrJ+1 {
					u32ArrayModeLastAttr = attrJ + 1
					u32ArrayModeCount++
				}
				val, valErr := u32ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingMode").Str("field", "Mode").Errorf("slot u32Array@ladingMode (field Mode) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u32ArrayModeVal = val
			case kindLadingBlockSize:
				if u32ArrayBlockSizeLastAttr != attrJ+1 {
					u32ArrayBlockSizeLastAttr = attrJ + 1
					u32ArrayBlockSizeCount++
				}
				val, valErr := u32ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingBlockSize").Str("field", "BlockSize").Errorf("slot u32Array@ladingBlockSize (field BlockSize) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u32ArrayBlockSizeVal = val
			case kindLadingBlocks:
				if u32ArrayBlocksLastAttr != attrJ+1 {
					u32ArrayBlocksLastAttr = attrJ + 1
					u32ArrayBlocksCount++
				}
				val, valErr := u32ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingBlocks").Str("field", "Blocks").Errorf("slot u32Array@ladingBlocks (field Blocks) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u32ArrayBlocksVal = val
			}
		}
	}
	if u32ArrayModeCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingMode").Int("got", u32ArrayModeCount).Errorf("slot u32Array@ladingMode (field Mode) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayModeCount)
		return
	}
	if u32ArrayModeCount == 1 {
		row.Mode = u32ArrayModeVal
		present = true
	}
	if u32ArrayBlockSizeCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingBlockSize").Int("got", u32ArrayBlockSizeCount).Errorf("slot u32Array@ladingBlockSize (field BlockSize) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayBlockSizeCount)
		return
	}
	if u32ArrayBlockSizeCount == 1 {
		row.BlockSize = u32ArrayBlockSizeVal
		present = true
	}
	if u32ArrayBlocksCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingBlocks").Int("got", u32ArrayBlocksCount).Errorf("slot u32Array@ladingBlocks (field Blocks) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayBlocksCount)
		return
	}
	if u32ArrayBlocksCount == 1 {
		row.Blocks = u32ArrayBlocksVal
		present = true
	}
	// --- u64Array. ---
	var u64ArraySizeVal uint64
	var u64ArraySizeCount int
	var u64ArraySizeLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingSize:
				if u64ArraySizeLastAttr != attrJ+1 {
					u64ArraySizeLastAttr = attrJ + 1
					u64ArraySizeCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingSize").Str("field", "Size").Errorf("slot u64Array@ladingSize (field Size) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArraySizeVal = val
			}
		}
	}
	if u64ArraySizeCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "ladingSize").Int("got", u64ArraySizeCount).Errorf("slot u64Array@ladingSize (field Size) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArraySizeCount)
		return
	}
	if u64ArraySizeCount == 1 {
		row.Size = u64ArraySizeVal
		present = true
	}
	// --- timeArray. ---
	var timeArrayMtimeVal time.Time
	var timeArrayMtimeCount int
	var timeArrayMtimeLastAttr int64
	ntimeArray := timeArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < ntimeArray; attrJ++ {
		for membID := range timeArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingMtime:
				if timeArrayMtimeLastAttr != attrJ+1 {
					timeArrayMtimeLastAttr = attrJ + 1
					timeArrayMtimeCount++
				}
				val, valErr := timeArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "timeArray").Str("membership", "ladingMtime").Str("field", "Mtime").Errorf("slot timeArray@ladingMtime (field Mtime) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				timeArrayMtimeVal = val
			}
		}
	}
	if timeArrayMtimeCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "timeArray").Str("membership", "ladingMtime").Int("got", timeArrayMtimeCount).Errorf("slot timeArray@ladingMtime (field Mtime) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", timeArrayMtimeCount)
		return
	}
	if timeArrayMtimeCount == 1 {
		row.Mtime = timeArrayMtimeVal
		present = true
	}
	// --- stringArray. ---
	var stringArrayLinkTargetVal string
	var stringArrayLinkTargetCount int
	var stringArrayLinkTargetLastAttr int64
	var stringArrayErrVal string
	var stringArrayErrCount int
	var stringArrayErrLastAttr int64
	nstringArray := stringArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstringArray; attrJ++ {
		for membID := range stringArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingLinkTarget:
				if stringArrayLinkTargetLastAttr != attrJ+1 {
					stringArrayLinkTargetLastAttr = attrJ + 1
					stringArrayLinkTargetCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "ladingLinkTarget").Str("field", "LinkTarget").Errorf("slot stringArray@ladingLinkTarget (field LinkTarget) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayLinkTargetVal = val
			case kindLadingErr:
				if stringArrayErrLastAttr != attrJ+1 {
					stringArrayErrLastAttr = attrJ + 1
					stringArrayErrCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "ladingErr").Str("field", "Err").Errorf("slot stringArray@ladingErr (field Err) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayErrVal = val
			}
		}
	}
	if stringArrayLinkTargetCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "ladingLinkTarget").Int("got", stringArrayLinkTargetCount).Errorf("slot stringArray@ladingLinkTarget (field LinkTarget) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayLinkTargetCount)
		return
	}
	if stringArrayLinkTargetCount == 1 {
		row.LinkTarget = stringArrayLinkTargetVal
		present = true
	}
	if stringArrayErrCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "ladingErr").Int("got", stringArrayErrCount).Errorf("slot stringArray@ladingErr (field Err) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayErrCount)
		return
	}
	if stringArrayErrCount == 1 {
		row.Err = stringArrayErrVal
		present = true
	}
	// --- blobArray. ---
	var blobArrayContentHashVal []byte
	var blobArrayContentHashCount int
	var blobArrayContentHashLastAttr int64
	nblobArray := blobArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nblobArray; attrJ++ {
		for membID := range blobArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingContentHash:
				if blobArrayContentHashLastAttr != attrJ+1 {
					blobArrayContentHashLastAttr = attrJ + 1
					blobArrayContentHashCount++
				}
				val, valErr := blobArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "ladingContentHash").Str("field", "ContentHash").Errorf("slot blobArray@ladingContentHash (field ContentHash) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				cp := make([]byte, len(val))
				copy(cp, val)
				blobArrayContentHashVal = cp
			}
		}
	}
	if blobArrayContentHashCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "ladingContentHash").Int("got", blobArrayContentHashCount).Errorf("slot blobArray@ladingContentHash (field ContentHash) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobArrayContentHashCount)
		return
	}
	if blobArrayContentHashCount == 1 {
		row.ContentHash = blobArrayContentHashVal
		present = true
	}
	// --- bool. ---
	var boolTextVal bool
	var boolTextCount int
	var boolTextLastAttr int64
	nbool := boolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nbool; attrJ++ {
		for membID := range boolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingText:
				if boolTextLastAttr != attrJ+1 {
					boolTextLastAttr = attrJ + 1
					boolTextCount++
				}
				val := boolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				boolTextVal = val
			}
		}
	}
	if boolTextCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "bool").Str("membership", "ladingText").Int("got", boolTextCount).Errorf("slot bool@ladingText (field Text) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", boolTextCount)
		return
	}
	if boolTextCount == 1 {
		row.Text = boolTextVal
		present = true
	}
	return
}
