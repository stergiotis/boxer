// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package ladingdata

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindLadingKindBlock uint64 = 10376293554346524674
	kindLadingData      uint64 = 10376293554346524692
	kindLadingBlockHash uint64 = 10376293554346524693
	kindLadingLine0     uint64 = 10376293554346524694
)

// ladingBlockSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingBlockSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingBlockSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingBlockSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// ladingBlockBlobArrayAttrI is the InAttr-side view of the blobArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingBlockBlobArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingBlockBlobArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingBlockBlobArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value []byte) Attr
	EndSection() Ent
}

// ladingBlockU32ArrayAttrI is the InAttr-side view of the u32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type ladingBlockU32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// ladingBlockU32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type ladingBlockU32ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint32) Attr
	EndSection() Ent
}

// ladingBlockEntityI is the entity-builder surface ladingBlockAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type ladingBlockEntityI[
	SymbolAttr ladingBlockSymbolAttrI,
	SymbolSec ladingBlockSymbolSecI[SymbolAttr, Ent],
	BlobArrayAttr ladingBlockBlobArrayAttrI,
	BlobArraySec ladingBlockBlobArraySecI[BlobArrayAttr, Ent],
	U32ArrayAttr ladingBlockU32ArrayAttrI,
	U32ArraySec ladingBlockU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionBlobArray() BlobArraySec
	GetSectionU32Array() U32ArraySec
}

// ladingBlockEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingBlockEmitSectionSymbol[
	SymbolAttr ladingBlockSymbolAttrI,
	SymbolSec ladingBlockSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row LadingBlock) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindLadingKindBlock)
	symbolSecAttr_Kind.EndAttributeP()
	return
}

// ladingBlockEmitSectionBlobArray writes this kind's blobArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingBlockEmitSectionBlobArray[
	BlobArrayAttr ladingBlockBlobArrayAttrI,
	BlobArraySec ladingBlockBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
](blobArraySec BlobArraySec, row LadingBlock) (err error) {
	blobArraySecAttr_Data := blobArraySec.BeginAttributeSingle(row.Data)
	blobArraySecAttr_Data.AddMembershipLowCardRefP(kindLadingData)
	blobArraySecAttr_Data.EndAttributeP()
	blobArraySecAttr_Hash := blobArraySec.BeginAttributeSingle(row.Hash)
	blobArraySecAttr_Hash.AddMembershipLowCardRefP(kindLadingBlockHash)
	blobArraySecAttr_Hash.EndAttributeP()
	return
}

// ladingBlockEmitSectionU32Array writes this kind's u32Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func ladingBlockEmitSectionU32Array[
	U32ArrayAttr ladingBlockU32ArrayAttrI,
	U32ArraySec ladingBlockU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
](u32ArraySec U32ArraySec, row LadingBlock) (err error) {
	u32ArraySecAttr_Line0 := u32ArraySec.BeginAttributeSingle(row.Line0)
	u32ArraySecAttr_Line0.AddMembershipLowCardRefP(kindLadingLine0)
	u32ArraySecAttr_Line0.EndAttributeP()
	return
}

// ladingBlockAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func ladingBlockAddSections[
	SymbolAttr ladingBlockSymbolAttrI,
	SymbolSec ladingBlockSymbolSecI[SymbolAttr, Ent],
	BlobArrayAttr ladingBlockBlobArrayAttrI,
	BlobArraySec ladingBlockBlobArraySecI[BlobArrayAttr, Ent],
	U32ArrayAttr ladingBlockU32ArrayAttrI,
	U32ArraySec ladingBlockU32ArraySecI[U32ArrayAttr, Ent],
	Ent any,
	DML ladingBlockEntityI[
		SymbolAttr, SymbolSec,
		BlobArrayAttr, BlobArraySec,
		U32ArrayAttr, U32ArraySec,
		Ent,
	],
](dml DML, row LadingBlock) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = ladingBlockEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- blobArray. ---
	blobArraySec := dml.GetSectionBlobArray()
	err = ladingBlockEmitSectionBlobArray(blobArraySec, row)
	if err != nil {
		return
	}
	blobArraySec.EndSection()
	// --- u32Array. ---
	u32ArraySec := dml.GetSectionU32Array()
	err = ladingBlockEmitSectionU32Array(u32ArraySec, row)
	if err != nil {
		return
	}
	u32ArraySec.EndSection()
	return
}

// ladingBlockSymbolAttrsReadI is the Attributes-side view of the symbol section.
type ladingBlockSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingBlockSymbolMembsReadI is the Memberships-side view of the symbol section.
type ladingBlockSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingBlockBlobArrayAttrsReadI is the Attributes-side view of the blobArray section.
type ladingBlockBlobArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) ([]byte, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingBlockBlobArrayMembsReadI is the Memberships-side view of the blobArray section.
type ladingBlockBlobArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingBlockU32ArrayAttrsReadI is the Attributes-side view of the u32Array section.
type ladingBlockU32ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint32, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// ladingBlockU32ArrayMembsReadI is the Memberships-side view of the u32Array section.
type ladingBlockU32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// ladingBlockReadRow reads row i as one optional LadingBlock component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func ladingBlockReadRow[
	SymbolAttrs ladingBlockSymbolAttrsReadI,
	SymbolMembs ladingBlockSymbolMembsReadI,
	BlobArrayAttrs ladingBlockBlobArrayAttrsReadI,
	BlobArrayMembs ladingBlockBlobArrayMembsReadI,
	U32ArrayAttrs ladingBlockU32ArrayAttrsReadI,
	U32ArrayMembs ladingBlockU32ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	blobArrayAttrs BlobArrayAttrs,
	blobArrayMembs BlobArrayMembs,
	u32ArrayAttrs U32ArrayAttrs,
	u32ArrayMembs U32ArrayMembs,
) (row LadingBlock, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingKindBlock:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "ladingKindBlock").Int("got", symbolKindCount).Errorf("slot symbol@ladingKindBlock (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	// --- blobArray. ---
	var blobArrayDataVal []byte
	var blobArrayDataCount int
	var blobArrayDataLastAttr int64
	var blobArrayHashVal []byte
	var blobArrayHashCount int
	var blobArrayHashLastAttr int64
	nblobArray := blobArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nblobArray; attrJ++ {
		for membID := range blobArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingData:
				if blobArrayDataLastAttr != attrJ+1 {
					blobArrayDataLastAttr = attrJ + 1
					blobArrayDataCount++
				}
				val, valErr := blobArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "ladingData").Str("field", "Data").Errorf("slot blobArray@ladingData (field Data) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				cp := make([]byte, len(val))
				copy(cp, val)
				blobArrayDataVal = cp
			case kindLadingBlockHash:
				if blobArrayHashLastAttr != attrJ+1 {
					blobArrayHashLastAttr = attrJ + 1
					blobArrayHashCount++
				}
				val, valErr := blobArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "ladingBlockHash").Str("field", "Hash").Errorf("slot blobArray@ladingBlockHash (field Hash) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				cp := make([]byte, len(val))
				copy(cp, val)
				blobArrayHashVal = cp
			}
		}
	}
	if blobArrayDataCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "ladingData").Int("got", blobArrayDataCount).Errorf("slot blobArray@ladingData (field Data) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobArrayDataCount)
		return
	}
	if blobArrayDataCount == 1 {
		row.Data = blobArrayDataVal
		present = true
	}
	if blobArrayHashCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "ladingBlockHash").Int("got", blobArrayHashCount).Errorf("slot blobArray@ladingBlockHash (field Hash) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobArrayHashCount)
		return
	}
	if blobArrayHashCount == 1 {
		row.Hash = blobArrayHashVal
		present = true
	}
	// --- u32Array. ---
	var u32ArrayLine0Val uint32
	var u32ArrayLine0Count int
	var u32ArrayLine0LastAttr int64
	nu32Array := u32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu32Array; attrJ++ {
		for membID := range u32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindLadingLine0:
				if u32ArrayLine0LastAttr != attrJ+1 {
					u32ArrayLine0LastAttr = attrJ + 1
					u32ArrayLine0Count++
				}
				val, valErr := u32ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingLine0").Str("field", "Line0").Errorf("slot u32Array@ladingLine0 (field Line0) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u32ArrayLine0Val = val
			}
		}
	}
	if u32ArrayLine0Count > 1 {
		err = eb.Build().Int("row", i).Str("section", "u32Array").Str("membership", "ladingLine0").Int("got", u32ArrayLine0Count).Errorf("slot u32Array@ladingLine0 (field Line0) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u32ArrayLine0Count)
		return
	}
	if u32ArrayLine0Count == 1 {
		row.Line0 = u32ArrayLine0Val
		present = true
	}
	return
}
