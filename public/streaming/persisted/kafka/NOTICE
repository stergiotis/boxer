public/streaming/persisted/kafka — NOTICE
==========================================

This package is a derivative work distributed under the Apache License,
Version 2.0. The license text is reproduced verbatim under section 1.4
of <THIRD_PARTY_NOTICES.md> at the repository root, and online at
<http://www.apache.org/licenses/LICENSE-2.0>.

----------------------------------------------------------------------
ORIGIN
----------------------------------------------------------------------

Derived from:
  github.com/redpanda-data/connect/internal/impl/kafka
  Pinned commit: 50aa034a668cc7d03d6acdcf63791fc36906a21c (2026-04-24)
  https://github.com/redpanda-data/connect/tree/50aa034a668cc7d03d6acdcf63791fc36906a21c/internal/impl/kafka

  Copyright 2024 Redpanda Data, Inc.
  Licensed under the Apache License, Version 2.0.

Decision record:
  doc/adr/0005-streaming-persisted-kafka-from-connect.md

----------------------------------------------------------------------
MODIFICATIONS APPLIED DURING PORT
----------------------------------------------------------------------

  * Restricted scope to the franz-go (kgo) consumer and producer. The
    sarama variant, the Redpanda-specific input/output/cache wrappers,
    the schema-registry input/output, and the AWS MSK IAM helpers were
    not ported.

  * Removed all dependencies on
    github.com/redpanda-data/benthos/v4/public/service. The Benthos
    Message / MessageBatch / AckFunc / Resources / ConfigField surface
    was replaced with package-local interfaces (ConsumerI, ProducerI,
    BatchI, AckFnI, RecordEnvelopeI). Configuration moved from
    service.ParsedConfig to plain Go option structs.

  * Refactored to the boxer coding standards (see CODINGSTANDARDS.md
    in github.com/stergiotis/boxer): receiver naming convention,
    interface suffix "I", enum suffix "E", named return values,
    error builder usage (eh.Errorf / eb.Build), sized integer fields
    on structs, compile-time interface assertions.

  * Replaced the service.Logger adapter with zerolog.

  * Replaced github.com/Jeffail/shutdown lifecycle helpers with
    context-cancellation primitives where the simplification was
    safe; otherwise retained the dependency.

  * Did not port the
    github.com/redpanda-data/connect/internal/impl/kafka/enterprise/
    subdirectory, which is licensed under the Redpanda Community
    License (RCL), not Apache-2.0.

  * Did not port scram.go. The XDGSCRAMClient type wraps
    github.com/xdg-go/scram for the sarama path; the franz-go path
    uses github.com/twmb/franz-go/pkg/sasl/scram natively, so the
    wrapper is unnecessary in this port.

  * Dropped the AWS_MSK_IAM and REDPANDA_CLOUD_SERVICE_ACCOUNT SASL
    mechanisms from sasl.go. AWS_MSK_IAM is supplied upstream via an
    indirect plug-point (AWSSASLFromConfigFn) populated when
    components/aws is imported; pebble2impl does not import that
    layer. REDPANDA_CLOUD_SERVICE_ACCOUNT depends on
    github.com/redpanda-data/connect/v4/internal/serviceaccount,
    a Connect-internal package not exposed for re-use. The four
    portable franz-go mechanisms (PLAIN, OAUTHBEARER, SCRAM-SHA-256,
    SCRAM-SHA-512) remain.

  * Dropped AddHeaders and ExtractHeaders from franz_headers.go (and
    the kafkaHeaders metadata-key constant they shared). Those
    functions converted between []kgo.RecordHeader and
    service.Message metadata; this port exposes *kgo.Record directly
    to application code, so the conversion is unneeded. The pure
    helpers GetHeaderValue and SetHeaderValue were retained.

  * franz_reader_ordered.go: dropped the per-message
    dispatch.CtxOnTriggerSignal mechanism. Upstream used the trigger
    to clear pendingDispatch[batchID] when the Benthos pipeline took
    a batch's records into the processor chain (i.e. before ack),
    enabling pipelined throughput on a single partition. This port
    has no processor-chain notion, so pendingDispatch[batchID] is
    set on pop and cleared only on ack, yielding strictly serial
    batches per partition. Applications that want pipelined
    throughput can wrap Read into a producer/consumer goroutine pair
    externally. The companion test (TestPartitionCacheOrdering),
    which existed solely to exercise the dispatch trigger under
    tangled multi-worker dispatch, was dropped as well; the
    chunking-focused TestPartitionCacheBatching was retained.

  * franz_reader_ordered.go: dropped the ConsumerLag wiring (the
    kafka_lag per-message metadata, the lag-refresh goroutine, and
    the redpanda_lag gauge metric). Phase 8 may re-introduce
    consumer-lag observability via a metrics seam.

  * franz_reader_ordered.go: dropped ConnectionTest. NewFranzClient
    already calls Ping on construction; callers that want a
    "test connection" path can call NewFranzClient + Close.

  * franz_reader_unordered.go: dropped the service.Batcher
    integration. Upstream's partitionTracker held a service.Batcher
    per partition and a dedicated goroutine driving size-based and
    time-based batch flushes. This port emits one *kgo.Record per
    Batch (single-record batches), simplifying partitionTracker to a
    checkpointer + lock with no internal goroutine. Applications
    that want size or time batching wrap Read in their own batcher
    externally.

  * franz_reader_unordered.go: dropped the franzRecordToMsgFn
    indirection (which defaulted to FranzRecordToMessageV0 / V1
    depending on the multi-header config flag) and the multi_header
    field. *kgo.Record is exposed directly per ADR-0005 /
    EXPLANATION.md; callers read record.Headers themselves.

  * franz_reader_unordered.go: dropped ConsumerLag wiring and
    ConnectionTest (same scope decisions as the ordered reader).
    Upstream's "Deprecated: use toggled" markers on the config-DSL
    helpers do not carry over: the deprecation targeted those
    helpers (which are dropped here anyway), not the
    FranzReaderUnordered struct itself, which the toggled wrapper
    composes rather than supersedes.

  * franz_reader_toggled.go: dropped the entire Benthos config-DSL
    surface (krtField* constants, FranzReaderToggledConfigFields,
    NewFranzReaderToggledFromConfig). Upstream's "toggled" wrapper
    existed primarily to expose a single YAML schema that switched
    between ordered and unordered via a nested
    `unordered_processing.enabled` flag. Without YAML config, the
    wrapper reduces to a constructor that picks the underlying
    reader from a Go-struct field; the upstream return type
    service.BatchInput is replaced with the package-local
    ConsumerI.

  * franz_writer.go: dropped the entire producer-side config-DSL
    surface (kfwField* constants for both producer-limit and
    per-record fields, FranzProducerLimitsFields,
    FranzProducerFields, FranzProducerLimitsOptsFromConfig,
    FranzProducerOptsFromConfig, FranzWriterConfigFields,
    FranzWriterConfigLints, NewFranzWriterFromConfig).

  * franz_writer.go: dropped the per-record interpolation surface
    (Topic / Key / Partition / Timestamp / MetaFilter as
    service.InterpolatedString / service.MetadataFilter). The port
    takes *kgo.Record values directly; applications populate Topic,
    Key, Value, Headers, Partition, and Timestamp on each record
    themselves.

  * franz_writer.go: dropped the franzWriterHooks indirection
    (accessClientFn / yieldClientFn / FranzSharedClientUseFn) and
    chose not to port franz_shared_client.go at all. The hooks +
    registry pattern existed so Benthos input and output components
    — instantiated independently from YAML — could share a
    *kgo.Client through a name-keyed registry on *service.Resources.
    In this port, the application constructs the *kgo.Client
    (typically via NewFranzClient) and passes it to NewFranzWriter
    directly; ownership stays with the caller and Close does not
    tear it down. A reader and writer that want to share one client
    can both receive the same pointer at construction time. (Note:
    today the readers still create their own client internally
    via clientOpts; sharing across reader+writer would require an
    additional NewFranzReaderFromClient constructor variant —
    deferred until a use case appears.)

  * franz_writer.go: dropped MessageBatchToFranzRecords,
    DecorateRecord, and the SkipRecord sentinel (all hooks for the
    migrator-style record-rewriting path). Upstream's
    dispatch.TriggerSignal calls go away with them.

  * franz_writer.go: dropped ConnectionTest. Connect calls Ping
    directly; callers that want a deeper "test connection" path
    own the *kgo.Client and can issue admin requests against it.

  * franz_writer.go: replaced the upstream manual Produce +
    sync.WaitGroup fan-out with kgo.Client.ProduceSync, which is
    functionally identical (waits for all produces; surfaces
    per-record errors via FirstErr) and roughly half the code.

  * Did not port output_kafka_franz.go (the Benthos output-plugin
    registration shim). It existed solely to wire FranzWriter into
    the Benthos plugin registry; without that registry the file has
    no role.

  * lag.go ported as consumer_lag.go. The *service.MetricGauge sink
    is replaced with a LagSinkFn callback (the package commits to
    no metrics interface; callers wire whatever backend they want).
    The asyncroutine.Periodic dependency (a Connect-internal package)
    is replaced with stdlib time.Ticker + a context-cancelled
    goroutine — identical refresh-on-tick semantics. The
    *service.Logger becomes *zerolog.Logger (nil-safe). The lag
    cache key shape ("topic_partition"), max(lag, 0) clamping, and
    kadm.Client usage are preserved verbatim. Integration into the
    readers (per-message kafka_lag metadata) is NOT restored —
    *kgo.Record is exposed directly per ADR-0005, so attaching
    metadata is the application's job; ConsumerLag is a standalone
    observability utility that callers compose with the reader
    using the reader's Client field.

Per Apache-2.0 §4.b, every modified source file in this package
carries a "Modifications applied during port" line in its header
referencing this NOTICE.

----------------------------------------------------------------------
THIRD-PARTY DEPENDENCIES OF THIS PACKAGE
----------------------------------------------------------------------

The aggregate index lives at THIRD_PARTY_NOTICES.md (section 1.4 for
this package; section 3 for module-level Go deps). Direct
dependencies of this package:

  github.com/twmb/franz-go/pkg/kgo
    BSD-3-Clause
    Copyright 2020, Travis Bischel.

  github.com/twmb/franz-go/pkg/sasl/{plain,scram,oauth}
    BSD-3-Clause (same module as kgo)

  github.com/twmb/franz-go/pkg/kadm
    BSD-3-Clause (same module as kgo)

  github.com/Jeffail/checkpoint
    MIT
    Copyright (c) 2024 Ashley Jeffs

  github.com/Jeffail/shutdown
    MIT (only if retained after Phase 5)
    Copyright (c) 2024 Ashley Jeffs

  github.com/cenkalti/backoff/v4
    MIT

  github.com/dustin/go-humanize
    MIT (cosmetic; may be dropped during port)

  github.com/testcontainers/testcontainers-go (and ./modules/redpanda)
    MIT
    Copyright (c) 2017-2019 Gianluca Arbezzano (and contributors)
    Test-only — gated behind the `integration` build tag in
    integration_test.go; required to spin up a Redpanda container
    for the produce/consume round-trip tests.

----------------------------------------------------------------------
RE-PINNING UPSTREAM
----------------------------------------------------------------------

When this package is re-synced against a newer upstream commit:

  1. Update the "Pinned commit" line in the ORIGIN section above.
  2. Append a dated entry under MODIFICATIONS APPLIED DURING PORT
     summarising the diff applied during the resync.
  3. Update doc/adr/0005-streaming-persisted-kafka-from-connect.md
     References section with the new SHA and date.
  4. Update THIRD_PARTY_NOTICES.md if any new third-party deps
     were introduced upstream.
