// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package stevedorefacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindStevedoreKindDeadLetter uint64 = 576460765188325408
	kindStevedoreDeadRef        uint64 = 576460765188325409
	kindStevedoreDeadOrigin     uint64 = 576460765188325410
	kindStevedoreDeadOrdinal    uint64 = 576460765188325411
	kindStevedoreDeadClass      uint64 = 576460765188325412
	kindStevedoreDeadError      uint64 = 576460765188325413
	kindStevedoreDeadTopic      uint64 = 576460765188325414
	kindStevedoreDeadPartition  uint64 = 576460765188325415
	kindStevedoreDeadOffset     uint64 = 576460765188325416
	kindStevedoreDeadMessage    uint64 = 576460765188325417
)

// deadLetterSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type deadLetterSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// deadLetterSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type deadLetterSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// deadLetterU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type deadLetterU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// deadLetterU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type deadLetterU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// deadLetterStringArrayAttrI is the InAttr-side view of the stringArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type deadLetterStringArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// deadLetterStringArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type deadLetterStringArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// deadLetterI32ArrayAttrI is the InAttr-side view of the i32Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type deadLetterI32ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// deadLetterI32ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type deadLetterI32ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value int32) Attr
	EndSection() Ent
}

// deadLetterI64ArrayAttrI is the InAttr-side view of the i64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type deadLetterI64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// deadLetterI64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type deadLetterI64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value int64) Attr
	EndSection() Ent
}

// deadLetterBlobArrayAttrI is the InAttr-side view of the blobArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type deadLetterBlobArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// deadLetterBlobArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type deadLetterBlobArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value []byte) Attr
	EndSection() Ent
}

// deadLetterEntityI is the entity-builder surface deadLetterAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type deadLetterEntityI[
	SymbolAttr deadLetterSymbolAttrI,
	SymbolSec deadLetterSymbolSecI[SymbolAttr, Ent],
	U64ArrayAttr deadLetterU64ArrayAttrI,
	U64ArraySec deadLetterU64ArraySecI[U64ArrayAttr, Ent],
	StringArrayAttr deadLetterStringArrayAttrI,
	StringArraySec deadLetterStringArraySecI[StringArrayAttr, Ent],
	I32ArrayAttr deadLetterI32ArrayAttrI,
	I32ArraySec deadLetterI32ArraySecI[I32ArrayAttr, Ent],
	I64ArrayAttr deadLetterI64ArrayAttrI,
	I64ArraySec deadLetterI64ArraySecI[I64ArrayAttr, Ent],
	BlobArrayAttr deadLetterBlobArrayAttrI,
	BlobArraySec deadLetterBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionU64Array() U64ArraySec
	GetSectionStringArray() StringArraySec
	GetSectionI32Array() I32ArraySec
	GetSectionI64Array() I64ArraySec
	GetSectionBlobArray() BlobArraySec
}

// deadLetterEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func deadLetterEmitSectionSymbol[
	SymbolAttr deadLetterSymbolAttrI,
	SymbolSec deadLetterSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row DeadLetter) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindStevedoreKindDeadLetter)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Class := symbolSec.BeginAttribute(row.Class)
	symbolSecAttr_Class.AddMembershipLowCardRefP(kindStevedoreDeadClass)
	symbolSecAttr_Class.EndAttributeP()
	symbolSecAttr_Topic := symbolSec.BeginAttribute(row.Topic)
	symbolSecAttr_Topic.AddMembershipLowCardRefP(kindStevedoreDeadTopic)
	symbolSecAttr_Topic.EndAttributeP()
	return
}

// deadLetterEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func deadLetterEmitSectionU64Array[
	U64ArrayAttr deadLetterU64ArrayAttrI,
	U64ArraySec deadLetterU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row DeadLetter) (err error) {
	u64ArraySecAttr_Ref := u64ArraySec.BeginAttributeSingle(row.Ref)
	u64ArraySecAttr_Ref.AddMembershipLowCardRefP(kindStevedoreDeadRef)
	u64ArraySecAttr_Ref.EndAttributeP()
	u64ArraySecAttr_Ordinal := u64ArraySec.BeginAttributeSingle(row.Ordinal)
	u64ArraySecAttr_Ordinal.AddMembershipLowCardRefP(kindStevedoreDeadOrdinal)
	u64ArraySecAttr_Ordinal.EndAttributeP()
	return
}

// deadLetterEmitSectionStringArray writes this kind's stringArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func deadLetterEmitSectionStringArray[
	StringArrayAttr deadLetterStringArrayAttrI,
	StringArraySec deadLetterStringArraySecI[StringArrayAttr, Ent],
	Ent any,
](stringArraySec StringArraySec, row DeadLetter) (err error) {
	stringArraySecAttr_Origin := stringArraySec.BeginAttributeSingle(row.Origin)
	stringArraySecAttr_Origin.AddMembershipLowCardRefP(kindStevedoreDeadOrigin)
	stringArraySecAttr_Origin.EndAttributeP()
	stringArraySecAttr_Error := stringArraySec.BeginAttributeSingle(row.Error)
	stringArraySecAttr_Error.AddMembershipLowCardRefP(kindStevedoreDeadError)
	stringArraySecAttr_Error.EndAttributeP()
	return
}

// deadLetterEmitSectionI32Array writes this kind's i32Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func deadLetterEmitSectionI32Array[
	I32ArrayAttr deadLetterI32ArrayAttrI,
	I32ArraySec deadLetterI32ArraySecI[I32ArrayAttr, Ent],
	Ent any,
](i32ArraySec I32ArraySec, row DeadLetter) (err error) {
	i32ArraySecAttr_Partition := i32ArraySec.BeginAttributeSingle(row.Partition)
	i32ArraySecAttr_Partition.AddMembershipLowCardRefP(kindStevedoreDeadPartition)
	i32ArraySecAttr_Partition.EndAttributeP()
	return
}

// deadLetterEmitSectionI64Array writes this kind's i64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func deadLetterEmitSectionI64Array[
	I64ArrayAttr deadLetterI64ArrayAttrI,
	I64ArraySec deadLetterI64ArraySecI[I64ArrayAttr, Ent],
	Ent any,
](i64ArraySec I64ArraySec, row DeadLetter) (err error) {
	i64ArraySecAttr_Offset := i64ArraySec.BeginAttributeSingle(row.Offset)
	i64ArraySecAttr_Offset.AddMembershipLowCardRefP(kindStevedoreDeadOffset)
	i64ArraySecAttr_Offset.EndAttributeP()
	return
}

// deadLetterEmitSectionBlobArray writes this kind's blobArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func deadLetterEmitSectionBlobArray[
	BlobArrayAttr deadLetterBlobArrayAttrI,
	BlobArraySec deadLetterBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
](blobArraySec BlobArraySec, row DeadLetter) (err error) {
	blobArraySecAttr_Message := blobArraySec.BeginAttributeSingle(row.Message)
	blobArraySecAttr_Message.AddMembershipLowCardRefP(kindStevedoreDeadMessage)
	blobArraySecAttr_Message.EndAttributeP()
	return
}

// deadLetterAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func deadLetterAddSections[
	SymbolAttr deadLetterSymbolAttrI,
	SymbolSec deadLetterSymbolSecI[SymbolAttr, Ent],
	U64ArrayAttr deadLetterU64ArrayAttrI,
	U64ArraySec deadLetterU64ArraySecI[U64ArrayAttr, Ent],
	StringArrayAttr deadLetterStringArrayAttrI,
	StringArraySec deadLetterStringArraySecI[StringArrayAttr, Ent],
	I32ArrayAttr deadLetterI32ArrayAttrI,
	I32ArraySec deadLetterI32ArraySecI[I32ArrayAttr, Ent],
	I64ArrayAttr deadLetterI64ArrayAttrI,
	I64ArraySec deadLetterI64ArraySecI[I64ArrayAttr, Ent],
	BlobArrayAttr deadLetterBlobArrayAttrI,
	BlobArraySec deadLetterBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
	DML deadLetterEntityI[
		SymbolAttr, SymbolSec,
		U64ArrayAttr, U64ArraySec,
		StringArrayAttr, StringArraySec,
		I32ArrayAttr, I32ArraySec,
		I64ArrayAttr, I64ArraySec,
		BlobArrayAttr, BlobArraySec,
		Ent,
	],
](dml DML, row DeadLetter) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = deadLetterEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = deadLetterEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	// --- stringArray. ---
	stringArraySec := dml.GetSectionStringArray()
	err = deadLetterEmitSectionStringArray(stringArraySec, row)
	if err != nil {
		return
	}
	stringArraySec.EndSection()
	// --- i32Array. ---
	i32ArraySec := dml.GetSectionI32Array()
	err = deadLetterEmitSectionI32Array(i32ArraySec, row)
	if err != nil {
		return
	}
	i32ArraySec.EndSection()
	// --- i64Array. ---
	i64ArraySec := dml.GetSectionI64Array()
	err = deadLetterEmitSectionI64Array(i64ArraySec, row)
	if err != nil {
		return
	}
	i64ArraySec.EndSection()
	// --- blobArray. ---
	blobArraySec := dml.GetSectionBlobArray()
	err = deadLetterEmitSectionBlobArray(blobArraySec, row)
	if err != nil {
		return
	}
	blobArraySec.EndSection()
	return
}

// deadLetterSymbolAttrsReadI is the Attributes-side view of the symbol section.
type deadLetterSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// deadLetterSymbolMembsReadI is the Memberships-side view of the symbol section.
type deadLetterSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// deadLetterU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type deadLetterU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// deadLetterU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type deadLetterU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// deadLetterStringArrayAttrsReadI is the Attributes-side view of the stringArray section.
type deadLetterStringArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// deadLetterStringArrayMembsReadI is the Memberships-side view of the stringArray section.
type deadLetterStringArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// deadLetterI32ArrayAttrsReadI is the Attributes-side view of the i32Array section.
type deadLetterI32ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (int32, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// deadLetterI32ArrayMembsReadI is the Memberships-side view of the i32Array section.
type deadLetterI32ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// deadLetterI64ArrayAttrsReadI is the Attributes-side view of the i64Array section.
type deadLetterI64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (int64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// deadLetterI64ArrayMembsReadI is the Memberships-side view of the i64Array section.
type deadLetterI64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// deadLetterBlobArrayAttrsReadI is the Attributes-side view of the blobArray section.
type deadLetterBlobArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) ([]byte, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// deadLetterBlobArrayMembsReadI is the Memberships-side view of the blobArray section.
type deadLetterBlobArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// deadLetterReadRow reads row i as one optional DeadLetter component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func deadLetterReadRow[
	SymbolAttrs deadLetterSymbolAttrsReadI,
	SymbolMembs deadLetterSymbolMembsReadI,
	U64ArrayAttrs deadLetterU64ArrayAttrsReadI,
	U64ArrayMembs deadLetterU64ArrayMembsReadI,
	StringArrayAttrs deadLetterStringArrayAttrsReadI,
	StringArrayMembs deadLetterStringArrayMembsReadI,
	I32ArrayAttrs deadLetterI32ArrayAttrsReadI,
	I32ArrayMembs deadLetterI32ArrayMembsReadI,
	I64ArrayAttrs deadLetterI64ArrayAttrsReadI,
	I64ArrayMembs deadLetterI64ArrayMembsReadI,
	BlobArrayAttrs deadLetterBlobArrayAttrsReadI,
	BlobArrayMembs deadLetterBlobArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
	stringArrayAttrs StringArrayAttrs,
	stringArrayMembs StringArrayMembs,
	i32ArrayAttrs I32ArrayAttrs,
	i32ArrayMembs I32ArrayMembs,
	i64ArrayAttrs I64ArrayAttrs,
	i64ArrayMembs I64ArrayMembs,
	blobArrayAttrs BlobArrayAttrs,
	blobArrayMembs BlobArrayMembs,
) (row DeadLetter, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolClassVal string
	var symbolClassCount int
	var symbolClassLastAttr int64
	var symbolTopicVal string
	var symbolTopicCount int
	var symbolTopicLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindStevedoreKindDeadLetter:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindStevedoreDeadClass:
				if symbolClassLastAttr != attrJ+1 {
					symbolClassLastAttr = attrJ + 1
					symbolClassCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolClassVal = val
			case kindStevedoreDeadTopic:
				if symbolTopicLastAttr != attrJ+1 {
					symbolTopicLastAttr = attrJ + 1
					symbolTopicCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolTopicVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "stevedoreKindDeadLetter").Int("got", symbolKindCount).Errorf("slot symbol@stevedoreKindDeadLetter (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolClassCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "stevedoreDeadClass").Int("got", symbolClassCount).Errorf("slot symbol@stevedoreDeadClass (field Class) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolClassCount)
		return
	}
	if symbolClassCount == 1 {
		row.Class = symbolClassVal
		present = true
	}
	if symbolTopicCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "stevedoreDeadTopic").Int("got", symbolTopicCount).Errorf("slot symbol@stevedoreDeadTopic (field Topic) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolTopicCount)
		return
	}
	if symbolTopicCount == 1 {
		row.Topic = symbolTopicVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayRefVal uint64
	var u64ArrayRefCount int
	var u64ArrayRefLastAttr int64
	var u64ArrayOrdinalVal uint64
	var u64ArrayOrdinalCount int
	var u64ArrayOrdinalLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindStevedoreDeadRef:
				if u64ArrayRefLastAttr != attrJ+1 {
					u64ArrayRefLastAttr = attrJ + 1
					u64ArrayRefCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "stevedoreDeadRef").Str("field", "Ref").Errorf("slot u64Array@stevedoreDeadRef (field Ref) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayRefVal = val
			case kindStevedoreDeadOrdinal:
				if u64ArrayOrdinalLastAttr != attrJ+1 {
					u64ArrayOrdinalLastAttr = attrJ + 1
					u64ArrayOrdinalCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "stevedoreDeadOrdinal").Str("field", "Ordinal").Errorf("slot u64Array@stevedoreDeadOrdinal (field Ordinal) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayOrdinalVal = val
			}
		}
	}
	if u64ArrayRefCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "stevedoreDeadRef").Int("got", u64ArrayRefCount).Errorf("slot u64Array@stevedoreDeadRef (field Ref) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayRefCount)
		return
	}
	if u64ArrayRefCount == 1 {
		row.Ref = u64ArrayRefVal
		present = true
	}
	if u64ArrayOrdinalCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "stevedoreDeadOrdinal").Int("got", u64ArrayOrdinalCount).Errorf("slot u64Array@stevedoreDeadOrdinal (field Ordinal) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayOrdinalCount)
		return
	}
	if u64ArrayOrdinalCount == 1 {
		row.Ordinal = u64ArrayOrdinalVal
		present = true
	}
	// --- stringArray. ---
	var stringArrayOriginVal string
	var stringArrayOriginCount int
	var stringArrayOriginLastAttr int64
	var stringArrayErrorVal string
	var stringArrayErrorCount int
	var stringArrayErrorLastAttr int64
	nstringArray := stringArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstringArray; attrJ++ {
		for membID := range stringArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindStevedoreDeadOrigin:
				if stringArrayOriginLastAttr != attrJ+1 {
					stringArrayOriginLastAttr = attrJ + 1
					stringArrayOriginCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "stevedoreDeadOrigin").Str("field", "Origin").Errorf("slot stringArray@stevedoreDeadOrigin (field Origin) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayOriginVal = val
			case kindStevedoreDeadError:
				if stringArrayErrorLastAttr != attrJ+1 {
					stringArrayErrorLastAttr = attrJ + 1
					stringArrayErrorCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "stevedoreDeadError").Str("field", "Error").Errorf("slot stringArray@stevedoreDeadError (field Error) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayErrorVal = val
			}
		}
	}
	if stringArrayOriginCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "stevedoreDeadOrigin").Int("got", stringArrayOriginCount).Errorf("slot stringArray@stevedoreDeadOrigin (field Origin) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayOriginCount)
		return
	}
	if stringArrayOriginCount == 1 {
		row.Origin = stringArrayOriginVal
		present = true
	}
	if stringArrayErrorCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "stevedoreDeadError").Int("got", stringArrayErrorCount).Errorf("slot stringArray@stevedoreDeadError (field Error) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayErrorCount)
		return
	}
	if stringArrayErrorCount == 1 {
		row.Error = stringArrayErrorVal
		present = true
	}
	// --- i32Array. ---
	var i32ArrayPartitionVal int32
	var i32ArrayPartitionCount int
	var i32ArrayPartitionLastAttr int64
	ni32Array := i32ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < ni32Array; attrJ++ {
		for membID := range i32ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindStevedoreDeadPartition:
				if i32ArrayPartitionLastAttr != attrJ+1 {
					i32ArrayPartitionLastAttr = attrJ + 1
					i32ArrayPartitionCount++
				}
				val, valErr := i32ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "i32Array").Str("membership", "stevedoreDeadPartition").Str("field", "Partition").Errorf("slot i32Array@stevedoreDeadPartition (field Partition) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				i32ArrayPartitionVal = val
			}
		}
	}
	if i32ArrayPartitionCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "i32Array").Str("membership", "stevedoreDeadPartition").Int("got", i32ArrayPartitionCount).Errorf("slot i32Array@stevedoreDeadPartition (field Partition) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", i32ArrayPartitionCount)
		return
	}
	if i32ArrayPartitionCount == 1 {
		row.Partition = i32ArrayPartitionVal
		present = true
	}
	// --- i64Array. ---
	var i64ArrayOffsetVal int64
	var i64ArrayOffsetCount int
	var i64ArrayOffsetLastAttr int64
	ni64Array := i64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < ni64Array; attrJ++ {
		for membID := range i64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindStevedoreDeadOffset:
				if i64ArrayOffsetLastAttr != attrJ+1 {
					i64ArrayOffsetLastAttr = attrJ + 1
					i64ArrayOffsetCount++
				}
				val, valErr := i64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "i64Array").Str("membership", "stevedoreDeadOffset").Str("field", "Offset").Errorf("slot i64Array@stevedoreDeadOffset (field Offset) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				i64ArrayOffsetVal = val
			}
		}
	}
	if i64ArrayOffsetCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "i64Array").Str("membership", "stevedoreDeadOffset").Int("got", i64ArrayOffsetCount).Errorf("slot i64Array@stevedoreDeadOffset (field Offset) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", i64ArrayOffsetCount)
		return
	}
	if i64ArrayOffsetCount == 1 {
		row.Offset = i64ArrayOffsetVal
		present = true
	}
	// --- blobArray. ---
	var blobArrayMessageVal []byte
	var blobArrayMessageCount int
	var blobArrayMessageLastAttr int64
	nblobArray := blobArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nblobArray; attrJ++ {
		for membID := range blobArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindStevedoreDeadMessage:
				if blobArrayMessageLastAttr != attrJ+1 {
					blobArrayMessageLastAttr = attrJ + 1
					blobArrayMessageCount++
				}
				val, valErr := blobArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "stevedoreDeadMessage").Str("field", "Message").Errorf("slot blobArray@stevedoreDeadMessage (field Message) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				cp := make([]byte, len(val))
				copy(cp, val)
				blobArrayMessageVal = cp
			}
		}
	}
	if blobArrayMessageCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "stevedoreDeadMessage").Int("got", blobArrayMessageCount).Errorf("slot blobArray@stevedoreDeadMessage (field Message) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobArrayMessageCount)
		return
	}
	if blobArrayMessageCount == 1 {
		row.Message = blobArrayMessageVal
		present = true
	}
	return
}
