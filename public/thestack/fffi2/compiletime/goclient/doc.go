//go:build llm_generated_opus47

// Package goclient generates Go-side fluent builder code for FFFI2 IDL
// definitions. Each widget method, factory, and enum becomes a typed Go
// surface that marshals into the binary FFFI2 wire format consumed by
// the matching Rust dispatch table.
package goclient
