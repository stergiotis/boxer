//go:build llm_generated_opus47

// Package runtime is the FFFI2 wire-protocol runtime — binary marshalling,
// channel I/O, deferred-block capture, and procedure dispatch. Forms the
// transport substrate beneath the typed wrappers in fffi2/typed/.
package runtime
