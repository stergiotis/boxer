//go:build llm_generated_opus47

// Package driver wires the Rust child-process side of imzero2: spawns the
// `main_rust` viewport, manages the FFFI2 byte channel, and orchestrates
// the egui2 codegen build via the embedded compiletime plugins.
package driver
