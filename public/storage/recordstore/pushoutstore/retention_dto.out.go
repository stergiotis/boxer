// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package pushoutstore

import (
	"iter"

	"github.com/apache/arrow-go/v18/arrow/array"

	"github.com/stergiotis/boxer/public/observability/eh"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Package-local membership ids (schema-agnostic target). ---

const (
	kindPushoutRetHash uint64 = 1
	kindPushoutRetIdx  uint64 = 2
	kindPushoutRetTime uint64 = 3
)

// --- SoA columns + AoS Append adapter. ---

// RetentionColumns is the SoA storage for batches of Retention rows.
// All slices grow in lockstep — Len returns the row count.
type RetentionColumns struct {
	ID []string

	Hashes  [][]string
	Indices [][]uint64
	Times   [][]int64
}

// Len returns the number of rows currently in the batch.
func (c *RetentionColumns) Len() int { return len(c.ID) }

// Append pushes one AoS record into the SoA buffers.
//
// Aliasing: slice and pointer fields (`[]T`, `*roaring.Bitmap`) are
// stored by reference, not copied. Callers must not mutate
// row.<F> after Append unless they want Marshal to read the
// mutation. Scalar fields (T, Option[T]) are copied by value.
func (c *RetentionColumns) Append(row Retention) {
	c.ID = append(c.ID, row.ID)
	c.Hashes = append(c.Hashes, row.Hashes)
	c.Indices = append(c.Indices, row.Indices)
	c.Times = append(c.Times, row.Times)
}

// Row reconstructs entity i as an AoS Retention record. Inverse of
// Append: slice / pointer fields are shared by reference (no
// defensive copy); scalar fields and Option[T] are copied.
func (c *RetentionColumns) Row(i int) (row Retention) {
	row.ID = c.ID[i]
	row.Hashes = c.Hashes[i]
	row.Indices = c.Indices[i]
	row.Times = c.Times[i]
	return
}

// --- Composed-interface BuildEntities helper (schema-agnostic). ---
//
// RetentionBuildEntities walks the SoA columns and emits one entity per
// row through dml.BeginEntity / per-section BeginAttribute* /
// AddMembershipLowCardRefP / AddToContainerP / EndAttributeP /
// EndSection / CommitEntity. dml is generic — any leeway-DML class
// whose method shapes satisfy the derived interfaces qualifies;
// Go's type inference binds the type parameters at the call site.
//
// Callers drain via dml.TransferRecords (or schema-specific
// equivalents) — left outside the helper because the record type
// varies by target.

// RetentionRetHashAttrI is the InAttr-side view of the retHash section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type RetentionRetHashAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value string)
	EndAttributeP()
}

// RetentionRetHashSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type RetentionRetHashSecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// RetentionRetIndexAttrI is the InAttr-side view of the retIndex section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type RetentionRetIndexAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value uint64)
	EndAttributeP()
}

// RetentionRetIndexSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type RetentionRetIndexSecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// RetentionRetTimeAttrI is the InAttr-side view of the retTime section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type RetentionRetTimeAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value int64)
	EndAttributeP()
}

// RetentionRetTimeSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type RetentionRetTimeSecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	EndSection() Ent
}

// RetentionEntityI lists exactly the entity-level methods Retention uses.
// Type parameters compose the per-section Attr + Sec interfaces; Ent
// is the entity type itself (return type of BeginEntity / SetId /
// SetTimestamp / SetLifecycle — usually the DML pointer).
type RetentionEntityI[
	RetHashAttr RetentionRetHashAttrI,
	RetHashSec RetentionRetHashSecI[RetHashAttr, Ent],
	RetIndexAttr RetentionRetIndexAttrI,
	RetIndexSec RetentionRetIndexSecI[RetIndexAttr, Ent],
	RetTimeAttr RetentionRetTimeAttrI,
	RetTimeSec RetentionRetTimeSecI[RetTimeAttr, Ent],
	Ent any,
] interface {
	BeginEntity() Ent
	SetId(id string) Ent
	GetSectionRetHash() RetHashSec
	GetSectionRetIndex() RetIndexSec
	GetSectionRetTime() RetTimeSec
	CommitEntity() (err error)
}

// RetentionBuildEntities walks c row-by-row, drives dml's entity / section
// chain, and returns once every row has been committed. The dml
// argument's concrete type binds every type parameter via Go's
// type inference at the call site.
func RetentionBuildEntities[
	RetHashAttr RetentionRetHashAttrI,
	RetHashSec RetentionRetHashSecI[RetHashAttr, Ent],
	RetIndexAttr RetentionRetIndexAttrI,
	RetIndexSec RetentionRetIndexSecI[RetIndexAttr, Ent],
	RetTimeAttr RetentionRetTimeAttrI,
	RetTimeSec RetentionRetTimeSecI[RetTimeAttr, Ent],
	Ent any,
	DML RetentionEntityI[
		RetHashAttr, RetHashSec,
		RetIndexAttr, RetIndexSec,
		RetTimeAttr, RetTimeSec,
		Ent,
	],
](dml DML, c *RetentionColumns) (err error) {
	n := c.Len()
	for i := 0; i < n; i++ {
		dml.BeginEntity()
		dml.SetId(c.ID[i])
		// --- retHash. ---
		retHashSec := dml.GetSectionRetHash()
		if len(c.Hashes[i]) > 0 {
			retHashSecAttr_Hashes := retHashSec.BeginAttribute()
			for _, v := range c.Hashes[i] {
				retHashSecAttr_Hashes.AddToContainerP(v)
			}
			retHashSecAttr_Hashes.AddMembershipLowCardRefP(kindPushoutRetHash)
			retHashSecAttr_Hashes.EndAttributeP()
		}
		retHashSec.EndSection()
		// --- retIndex. ---
		retIndexSec := dml.GetSectionRetIndex()
		if len(c.Indices[i]) > 0 {
			retIndexSecAttr_Indices := retIndexSec.BeginAttribute()
			for _, v := range c.Indices[i] {
				retIndexSecAttr_Indices.AddToContainerP(v)
			}
			retIndexSecAttr_Indices.AddMembershipLowCardRefP(kindPushoutRetIdx)
			retIndexSecAttr_Indices.EndAttributeP()
		}
		retIndexSec.EndSection()
		// --- retTime. ---
		retTimeSec := dml.GetSectionRetTime()
		if len(c.Times[i]) > 0 {
			retTimeSecAttr_Times := retTimeSec.BeginAttribute()
			for _, v := range c.Times[i] {
				retTimeSecAttr_Times.AddToContainerP(v)
			}
			retTimeSecAttr_Times.AddMembershipLowCardRefP(kindPushoutRetTime)
			retTimeSecAttr_Times.EndAttributeP()
		}
		retTimeSec.EndSection()
		err = dml.CommitEntity()
		if err != nil {
			err = eh.Errorf("commit row %d: %w", i, err)
			return
		}
	}
	return
}

// RetentionAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func RetentionAddSections[
	RetHashAttr RetentionRetHashAttrI,
	RetHashSec RetentionRetHashSecI[RetHashAttr, Ent],
	RetIndexAttr RetentionRetIndexAttrI,
	RetIndexSec RetentionRetIndexSecI[RetIndexAttr, Ent],
	RetTimeAttr RetentionRetTimeAttrI,
	RetTimeSec RetentionRetTimeSecI[RetTimeAttr, Ent],
	Ent any,
	DML RetentionEntityI[
		RetHashAttr, RetHashSec,
		RetIndexAttr, RetIndexSec,
		RetTimeAttr, RetTimeSec,
		Ent,
	],
](dml DML, row Retention) (err error) {
	// --- retHash. ---
	retHashSec := dml.GetSectionRetHash()
	if len(row.Hashes) > 0 {
		retHashSecAttr_Hashes := retHashSec.BeginAttribute()
		for _, v := range row.Hashes {
			retHashSecAttr_Hashes.AddToContainerP(v)
		}
		retHashSecAttr_Hashes.AddMembershipLowCardRefP(kindPushoutRetHash)
		retHashSecAttr_Hashes.EndAttributeP()
	}
	retHashSec.EndSection()
	// --- retIndex. ---
	retIndexSec := dml.GetSectionRetIndex()
	if len(row.Indices) > 0 {
		retIndexSecAttr_Indices := retIndexSec.BeginAttribute()
		for _, v := range row.Indices {
			retIndexSecAttr_Indices.AddToContainerP(v)
		}
		retIndexSecAttr_Indices.AddMembershipLowCardRefP(kindPushoutRetIdx)
		retIndexSecAttr_Indices.EndAttributeP()
	}
	retIndexSec.EndSection()
	// --- retTime. ---
	retTimeSec := dml.GetSectionRetTime()
	if len(row.Times) > 0 {
		retTimeSecAttr_Times := retTimeSec.BeginAttribute()
		for _, v := range row.Times {
			retTimeSecAttr_Times.AddToContainerP(v)
		}
		retTimeSecAttr_Times.AddMembershipLowCardRefP(kindPushoutRetTime)
		retTimeSecAttr_Times.EndAttributeP()
	}
	retTimeSec.EndSection()
	return
}

// --- Composed-interface FillFromArrow helper (schema-agnostic). ---
//
// RetentionFillFromArrow walks the Arrow record row-by-row and appends
// each entity's plain + tagged-section values into c. Plain columns
// enter as concrete Arrow accessors (uniform across schemas);
// per-section Attrs + Membs bind through type-parameter interfaces.

// RetentionRetHashAttrsReadI is the Attributes-side view of the retHash section.
type RetentionRetHashAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[string]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// RetentionRetHashMembsReadI is the Memberships-side view of the retHash section.
type RetentionRetHashMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// RetentionRetIndexAttrsReadI is the Attributes-side view of the retIndex section.
type RetentionRetIndexAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// RetentionRetIndexMembsReadI is the Memberships-side view of the retIndex section.
type RetentionRetIndexMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// RetentionRetTimeAttrsReadI is the Attributes-side view of the retTime section.
type RetentionRetTimeAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[int64]
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// RetentionRetTimeMembsReadI is the Memberships-side view of the retTime section.
type RetentionRetTimeMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// RetentionFillFromArrow walks rec row-by-row and appends each entity's
// plain + tagged-section values into c. Plain columns enter as
// concrete Arrow accessors; per-section Attrs + Membs bind through
// type-parameter interfaces.
func RetentionFillFromArrow[
	RetHashAttrs RetentionRetHashAttrsReadI,
	RetHashMembs RetentionRetHashMembsReadI,
	RetIndexAttrs RetentionRetIndexAttrsReadI,
	RetIndexMembs RetentionRetIndexMembsReadI,
	RetTimeAttrs RetentionRetTimeAttrsReadI,
	RetTimeMembs RetentionRetTimeMembsReadI,
](
	c *RetentionColumns,
	n int,
	idCol *array.String,
	retHashAttrs RetHashAttrs,
	retHashMembs RetHashMembs,
	retIndexAttrs RetIndexAttrs,
	retIndexMembs RetIndexMembs,
	retTimeAttrs RetTimeAttrs,
	retTimeMembs RetTimeMembs,
) (err error) {
	for i := 0; i < n; i++ {
		c.ID = append(c.ID, idCol.Value(i))
		// --- retHash. ---
		var retHashHashesSlice []string
		nretHash := retHashAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
		for attrJ := int64(0); attrJ < nretHash; attrJ++ {
			for membID := range retHashMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
				switch membID {
				case kindPushoutRetHash:
					for v := range retHashAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
						retHashHashesSlice = append(retHashHashesSlice, v)
					}
				}
			}
		}
		c.Hashes = append(c.Hashes, retHashHashesSlice)
		// --- retIndex. ---
		var retIndexIndicesSlice []uint64
		nretIndex := retIndexAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
		for attrJ := int64(0); attrJ < nretIndex; attrJ++ {
			for membID := range retIndexMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
				switch membID {
				case kindPushoutRetIdx:
					for v := range retIndexAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
						retIndexIndicesSlice = append(retIndexIndicesSlice, v)
					}
				}
			}
		}
		c.Indices = append(c.Indices, retIndexIndicesSlice)
		// --- retTime. ---
		var retTimeTimesSlice []int64
		nretTime := retTimeAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
		for attrJ := int64(0); attrJ < nretTime; attrJ++ {
			for membID := range retTimeMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
				switch membID {
				case kindPushoutRetTime:
					for v := range retTimeAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
						retTimeTimesSlice = append(retTimeTimesSlice, v)
					}
				}
			}
		}
		c.Times = append(c.Times, retTimeTimesSlice)
	}
	return
}

// RetentionReadRow reads row i as one optional Retention component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched, erroring only when a field
// occurs more than once. Plain-bound fields stay zero — the caller
// owns the envelope. The Attrs/Membs readers bind by type inference
// at the call site, as with FillFromArrow.
func RetentionReadRow[
	RetHashAttrs RetentionRetHashAttrsReadI,
	RetHashMembs RetentionRetHashMembsReadI,
	RetIndexAttrs RetentionRetIndexAttrsReadI,
	RetIndexMembs RetentionRetIndexMembsReadI,
	RetTimeAttrs RetentionRetTimeAttrsReadI,
	RetTimeMembs RetentionRetTimeMembsReadI,
](
	i int,
	retHashAttrs RetHashAttrs,
	retHashMembs RetHashMembs,
	retIndexAttrs RetIndexAttrs,
	retIndexMembs RetIndexMembs,
	retTimeAttrs RetTimeAttrs,
	retTimeMembs RetTimeMembs,
) (row Retention, present bool, err error) {
	// --- retHash. ---
	var retHashHashesSlice []string
	nretHash := retHashAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nretHash; attrJ++ {
		for membID := range retHashMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindPushoutRetHash:
				for v := range retHashAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					retHashHashesSlice = append(retHashHashesSlice, v)
				}
			}
		}
	}
	if retHashHashesSlice != nil {
		row.Hashes = retHashHashesSlice
		present = true
	}
	// --- retIndex. ---
	var retIndexIndicesSlice []uint64
	nretIndex := retIndexAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nretIndex; attrJ++ {
		for membID := range retIndexMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindPushoutRetIdx:
				for v := range retIndexAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					retIndexIndicesSlice = append(retIndexIndicesSlice, v)
				}
			}
		}
	}
	if retIndexIndicesSlice != nil {
		row.Indices = retIndexIndicesSlice
		present = true
	}
	// --- retTime. ---
	var retTimeTimesSlice []int64
	nretTime := retTimeAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nretTime; attrJ++ {
		for membID := range retTimeMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindPushoutRetTime:
				for v := range retTimeAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					retTimeTimesSlice = append(retTimeTimesSlice, v)
				}
			}
		}
	}
	if retTimeTimesSlice != nil {
		row.Times = retTimeTimesSlice
		present = true
	}
	return
}
