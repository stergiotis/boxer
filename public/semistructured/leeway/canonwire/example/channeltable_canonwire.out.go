// Code generated by Leeway canonwire (github.com/stergiotis/boxer/public/semistructured/leeway/canonwire.test); DO NOT EDIT.

package example

import (
	"io"
	"time"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	cwruntime "github.com/stergiotis/boxer/public/semistructured/leeway/canonwire/runtime"
	"github.com/stergiotis/boxer/public/semistructured/leeway/common"
	"github.com/stergiotis/boxer/public/semistructured/leeway/mappingplan"
	"github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// The imports a table without plain sections, without memberships or without a
// temporal column would otherwise leave unused. The generated code is one file
// per table and the set it touches depends on the table's shape.
var (
	_ = time.Time{}
	_ = common.PlainItemTypeNone
	_ = mappingplan.MembershipChannelLowCardRef
	_ = eb.Build
)

///////////////////////////////////////////////////////////////////
// code generator
// canonwire.(*GoClassBuilder).ComposeCodec
// ./public/semistructured/leeway/canonwire/lw_cw_generator.go:209

// The slot keys of ADR-0210 SD2: a tagged section's CT group, or a co-section
// group's CT signature. They key the entity item's tagged map and are the only
// thing on the wire that comes from channel-table's description: section names, column
// names, aspects and hints are not.

// CanonWireSignatureChannelTableHparam keys the slot of section hparam.
const CanonWireSignatureChannelTableHparam = "f64"

// CanonWireSignatureChannelTableHref keys the slot of section href.
const CanonWireSignatureChannelTableHref = "i64"

// CanonWireSignatureChannelTableHverb keys the slot of section hverb.
const CanonWireSignatureChannelTableHverb = "s"

// CanonWireSignatureChannelTableMref keys the slot of section mref.
const CanonWireSignatureChannelTableMref = "u64"

// CanonWireSignatureChannelTableLparam keys the slot of section lparam.
const CanonWireSignatureChannelTableLparam = "y"

// CanonWireSlotChannelTableE names the table's tagged slots.
//
// They are in slot-table order — by signature, then by first section index —
// so the ordinals do not depend on map iteration.
type CanonWireSlotChannelTableE uint8

const (
	CanonWireSlotChannelTableHparam CanonWireSlotChannelTableE = iota
	CanonWireSlotChannelTableHref
	CanonWireSlotChannelTableHverb
	CanonWireSlotChannelTableMref
	CanonWireSlotChannelTableLparam
)

func (inst CanonWireSlotChannelTableE) String() string {
	switch inst {
	case CanonWireSlotChannelTableHparam:
		return "hparam"
	case CanonWireSlotChannelTableHref:
		return "href"
	case CanonWireSlotChannelTableHverb:
		return "hverb"
	case CanonWireSlotChannelTableMref:
		return "mref"
	case CanonWireSlotChannelTableLparam:
		return "lparam"
	}
	return "<invalid>"
}

// Signature returns the slot's CT signature — the key it rides under in an
// entity's tagged map.
func (inst CanonWireSlotChannelTableE) Signature() string {
	switch inst {
	case CanonWireSlotChannelTableHparam:
		return CanonWireSignatureChannelTableHparam
	case CanonWireSlotChannelTableHref:
		return CanonWireSignatureChannelTableHref
	case CanonWireSlotChannelTableHverb:
		return CanonWireSignatureChannelTableHverb
	case CanonWireSlotChannelTableMref:
		return CanonWireSignatureChannelTableMref
	case CanonWireSlotChannelTableLparam:
		return CanonWireSignatureChannelTableLparam
	}
	return ""
}

// Ambiguous reports whether the slot's signature is carried by more than one
// slot of this table. Those are the slots ADR-0210 SD5's dispatch has to
// resolve: the encoder consults its tagger for every attribute of one, and for
// no attribute of any other.
func (inst CanonWireSlotChannelTableE) Ambiguous() bool {
	_ = inst
	return false
}

// CanonWireTaggerChannelTableI is the encoder half of ADR-0210 SD5's dispatch pair.
//
// It is consulted for every attribute of a slot whose signature is ambiguous
// in this table, and its small opaque integer rides as the attribute item's
// trailing element for the decoding side's DispatcherI to read. Attributes of
// an unambiguous slot never reach it and never carry a discriminator.
type CanonWireTaggerChannelTableI interface {
	Tag(slot CanonWireSlotChannelTableE, entityIdx runtime.EntityIdx, attrIdx runtime.AttributeIdx) uint64
}

// CanonWireOrdinalTaggerChannelTable is the built-in tagger of ADR-0210 SD5.
//
// It returns the slot's ordinal within its ambiguity set, which round-trips
// between two tables that declare the same sections in the same order and is
// explicitly that declaration-order coupling — a consumer that wants
// section-name independence supplies its own pair.
type CanonWireOrdinalTaggerChannelTable struct{}

var _ CanonWireTaggerChannelTableI = CanonWireOrdinalTaggerChannelTable{}

func (inst CanonWireOrdinalTaggerChannelTable) Tag(slot CanonWireSlotChannelTableE, entityIdx runtime.EntityIdx, attrIdx runtime.AttributeIdx) uint64 {
	_ = slot
	return 0
}

// CanonWireEncoderChannelTable writes entities of a loaded ReadAccessChannelTable as the leeway canonical wire (ADR-0210).
//
// It reads the generated readaccess accessors directly: no reflection, no
// arrow.Array type switch, one typed runtime call per column. The buffers are
// held on the encoder and reused across entities, so it is not goroutine-safe;
// one encoder per encoding goroutine.
type CanonWireEncoderChannelTable struct {
	tagger               CanonWireTaggerChannelTableI
	ra                   *ReadAccessChannelTable
	ew                   *cwruntime.EntityWriter
	awChannelTableHparam *cwruntime.AttributeWriter
	awChannelTableHref   *cwruntime.AttributeWriter
	awChannelTableHverb  *cwruntime.AttributeWriter
	awChannelTableMref   *cwruntime.AttributeWriter
	awChannelTableLparam *cwruntime.AttributeWriter
}

// NewCanonWireEncoderChannelTable binds an encoder to ra, which must already be loaded from a batch.
//
// tagger may be nil: no attribute then carries an SD5 discriminator, and the
// decoding side has to resolve an ambiguous signature from content alone.
func NewCanonWireEncoderChannelTable(ra *ReadAccessChannelTable, tagger CanonWireTaggerChannelTableI) (inst *CanonWireEncoderChannelTable, err error) {
	if ra == nil {
		err = eb.Build().Str("tableName", "channel-table").Errorf("no read access to encode from")
		return
	}
	inst = &CanonWireEncoderChannelTable{ra: ra, tagger: tagger}
	inst.ew, err = cwruntime.NewEntityWriter()
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "channel-table").Errorf("unable to create the entity writer: %w", err)
		return
	}
	inst.awChannelTableHparam, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "channel-table").Str("slot", "hparam").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awChannelTableHref, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "channel-table").Str("slot", "href").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awChannelTableHverb, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "channel-table").Str("slot", "hverb").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awChannelTableMref, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "channel-table").Str("slot", "mref").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awChannelTableLparam, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "channel-table").Str("slot", "lparam").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	return
}

// EncodeEntity writes one entity item to w: the plain sections under their item
// types, then the tagged slots under their CT signatures, both in the order the
// form fixes (ADR-0210 SD1–SD3). The attributes are held back only until the
// item is complete, which is what lets the writer sort them into canonical
// order.
func (inst *CanonWireEncoderChannelTable) EncodeEntity(entityIdx runtime.EntityIdx, w io.Writer) (err error) {
	ew := inst.ew
	ew.Begin()
	{ // plain section entity-id — u64
		cw := ew.BeginPlain(common.PlainItemTypeEntityId, 1)
		{
			v := inst.ra.EntityId.GetAttrValueId(entityIdx)
			cw.WriteUint(uint64(v))
		}
		ew.EndPlain()
	}
	{ // slot hparam — "f64"
		sw := ew.Slot(CanonWireSignatureChannelTableHparam)
		aw := inst.awChannelTableHparam
		n := inst.ra.Hparam.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v := range inst.ra.Hparam.Memberships.GetMembValueHighCardRefParametrized(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelHighCardRefParametrized, 0, nil, v)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Hparam.Attributes.GetAttrValueE(entityIdx, attrIdx)
				cw.WriteF64(v)
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "hparam").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot href — "i64"
		sw := ew.Slot(CanonWireSignatureChannelTableHref)
		aw := inst.awChannelTableHref
		n := inst.ra.Href.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v := range inst.ra.Href.Memberships.GetMembValueHighCardRef(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelHighCardRef, v, nil, nil)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Href.Attributes.GetAttrValueB(entityIdx, attrIdx)
				cw.WriteInt(int64(v))
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "href").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot hverb — "s"
		sw := ew.Slot(CanonWireSignatureChannelTableHverb)
		aw := inst.awChannelTableHverb
		n := inst.ra.Hverb.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v := range inst.ra.Hverb.Memberships.GetMembValueHighCardVerbatim(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelHighCardVerbatim, 0, v, nil)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Hverb.Attributes.GetAttrValueC(entityIdx, attrIdx)
				cw.WriteTextString(v)
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "hverb").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot mref — "u64"
		sw := ew.Slot(CanonWireSignatureChannelTableMref)
		aw := inst.awChannelTableMref
		n := inst.ra.Mref.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.Mref.Memberships.GetMembValueLowCardRefHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardRef, v1, nil, v2)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Mref.Attributes.GetAttrValueA(entityIdx, attrIdx)
				cw.WriteUint(uint64(v))
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "mref").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot lparam — "y"
		sw := ew.Slot(CanonWireSignatureChannelTableLparam)
		aw := inst.awChannelTableLparam
		n := inst.ra.Lparam.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v := range inst.ra.Lparam.Memberships.GetMembValueLowCardRefParametrized(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelLowCardRefParametrized, 0, nil, v)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Lparam.Attributes.GetAttrValueD(entityIdx, attrIdx)
				cw.WriteBytes(v)
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "lparam").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	err = ew.Flush(w)
	if err != nil {
		err = eb.Build().Str("tableName", "channel-table").Int("entityIdx", int(entityIdx)).Errorf("unable to flush the entity: %w", err)
	}
	return
}

// EncodeAll writes every entity of the loaded batch to w as a CBOR sequence
// (RFC 8742): one entity item after another, with no framing and no header.
func (inst *CanonWireEncoderChannelTable) EncodeAll(w io.Writer) (err error) {
	n := inst.ra.GetNumberOfEntities()
	for i := range n {
		err = inst.EncodeEntity(runtime.EntityIdx(i), w)
		if err != nil {
			err = eb.Build().Str("tableName", "channel-table").Int("entityIdx", i).Errorf("unable to encode the entity: %w", err)
			return
		}
	}
	return
}

// canonWireAmbiguousChannelTable reports whether any signature of channel-table is carried by more than one slot.
// It is what the decoder's constructor checks: an ambiguous table cannot be
// decoded without a dispatcher, and the refusal is at construction rather than
// at the first attribute that needs one (ADR-0210 SD5).
const canonWireAmbiguousChannelTable = false

// canonWireAcceptMasksChannelTable holds, per slot in slot order, the membership channels each member
// section accepts — one bitmask per section in signature order, over the
// mappingplan channel ordinals. It is the input to the narrowing step of
// ADR-0210 SD5: a section that cannot store the memberships an attribute
// carries is not that attribute's target.
var canonWireAcceptMasksChannelTable = [][]uint32{
	{0x80}, // hparam
	{0x04}, // href
	{0x08}, // hverb
	{0x10}, // mref
	{0x40}, // lparam
}

// CanonWireDispatcherChannelTableI is the decoder half of ADR-0210 SD5's dispatch pair.
//
// It is consulted only for an attribute whose signature more than one slot of
// this table carries *and* whose memberships more than one of those slots can
// store: candidates holds the survivors of the narrowing step, in slot order,
// and is never empty and never of length one — those two cases are decided
// without a call. attr is the attribute as the wire carried it, with nothing
// of either endpoint's table description in it.
//
// The returned slot must be one of the candidates; anything else fails the
// entity.
type CanonWireDispatcherChannelTableI interface {
	Dispatch(candidates []CanonWireSlotChannelTableE, attr *cwruntime.AttributeView) (slot CanonWireSlotChannelTableE, err error)
}

// CanonWireOrdinalDispatcherChannelTable is the built-in dispatcher of ADR-0210 SD5 and the mirror of CanonWireOrdinalTaggerChannelTable: it reads
// the attribute's discriminator as the slot's ordinal within the full
// ambiguity set. It round-trips between two tables that declare the same
// sections in the same order, and is explicitly that declaration-order
// coupling — a consumer that wants section-name independence supplies its own
// pair.
type CanonWireOrdinalDispatcherChannelTable struct{}

var _ CanonWireDispatcherChannelTableI = CanonWireOrdinalDispatcherChannelTable{}

func (inst CanonWireOrdinalDispatcherChannelTable) Dispatch(candidates []CanonWireSlotChannelTableE, attr *cwruntime.AttributeView) (slot CanonWireSlotChannelTableE, err error) {
	_ = candidates
	_ = attr
	err = eb.Build().Str("tableName", "channel-table").Errorf("the table has no ambiguous signature, so no attribute is dispatched: %w", cwruntime.ErrDispatch)
	return
}

// CanonWireDecoderChannelTable decodes leeway canonical wire entities (ADR-0210) into a InEntityChannelTable.
//
// It drives the generated dml builders directly: the plain setters, then one
// BeginAttribute / container append / AddMembership… / EndAttribute per wire
// attribute, then CommitEntity. No reflection, one typed runtime call per
// column. The buffers are held on the decoder and reused across entities, so
// it is not goroutine-safe; one decoder per decoding goroutine.
//
// A plain section the wire does not carry is simply not set — the entity gets
// the dml's zero for it, which is the entity a writer that never called that
// setter would have produced. Attributes come back in the form's canonical
// order, not the order they were written in.
type CanonWireDecoderChannelTable struct {
	dispatcher CanonWireDispatcherChannelTableI
	dml        *InEntityChannelTable
	r          *cwruntime.CborReader
	er         *cwruntime.EntityReader
	groups     [][]cwruntime.MembershipView
	values     [][]byte
	cands      []CanonWireSlotChannelTableE
	view       cwruntime.AttributeView
}

// NewCanonWireDecoderChannelTable binds a decoder to dml, which it drives one entity at a time.
//
// dispatcher may be nil only for a table with no ambiguous signature — canonWireAmbiguousChannelTable says
// which this is. A table that has one cannot resolve an attribute's slot from
// the key alone, so it is refused here rather than at the first attribute that
// would have needed the hook (ADR-0210 SD5).
func NewCanonWireDecoderChannelTable(dml *InEntityChannelTable, dispatcher CanonWireDispatcherChannelTableI) (inst *CanonWireDecoderChannelTable, err error) {
	if dml == nil {
		err = eb.Build().Str("tableName", "channel-table").Errorf("no dml builder to decode into")
		return
	}
	if dispatcher == nil && canonWireAmbiguousChannelTable {
		err = eb.Build().Str("tableName", "channel-table").Errorf("the table carries a signature more than one slot claims, which needs a dispatcher: %w", cwruntime.ErrDispatch)
		return
	}
	inst = &CanonWireDecoderChannelTable{dml: dml, dispatcher: dispatcher}
	inst.r = cwruntime.NewCborReader(nil)
	inst.er = cwruntime.NewEntityReader(inst.r)
	inst.groups = make([][]cwruntime.MembershipView, 1)
	inst.values = make([][]byte, 1)
	return
}

// slotAcceptsChannels is the narrowing step of ADR-0210 SD5: every membership
// the attribute carries must land on a channel the member section at that
// group's position declares. It is also the check an unambiguous slot goes
// through, because a signature match does not imply the carriage matches.
func (inst *CanonWireDecoderChannelTable) slotAcceptsChannels(slot CanonWireSlotChannelTableE) (accepts bool) {
	if int(slot) >= len(canonWireAcceptMasksChannelTable) {
		return false
	}
	masks := canonWireAcceptMasksChannelTable[slot]
	if len(masks) != len(inst.view.Groups) {
		return false
	}
	for g, mask := range masks {
		for _, m := range inst.view.Groups[g] {
			if uint(m.Channel) >= 32 || mask&(uint32(1)<<uint(m.Channel)) == 0 {
				return false
			}
		}
	}
	return true
}

// DecodeEntity decodes the one entity item at the start of b and writes it
// through the dml, returning how many bytes it consumed. On any failure the
// entity is rolled back, so the builder is left where it was and the next
// entity can still be decoded into it.
func (inst *CanonWireDecoderChannelTable) DecodeEntity(b []byte) (n int, err error) {
	inst.er.Reset(b)
	inst.dml.BeginEntity()
	err = inst.decodeEntity()
	if err == nil {
		err = inst.dml.CheckErrors()
		if err != nil {
			err = eb.Build().Str("tableName", "channel-table").Errorf("the dml builder rejected the decoded entity: %w", err)
		}
	}
	if err != nil {
		if rerr := inst.dml.RollbackEntity(); rerr != nil {
			err = eb.Build().Str("tableName", "channel-table").Str("rollbackError", rerr.Error()).Errorf("unable to roll the entity back: %w", err)
		}
		return 0, err
	}
	err = inst.dml.CommitEntity()
	if err != nil {
		err = eb.Build().Str("tableName", "channel-table").Errorf("unable to commit the decoded entity: %w", err)
		return 0, err
	}
	return inst.r.Pos(), nil
}

// DecodeAll decodes a CBOR sequence (RFC 8742) of entity items — what
// EncodeAll writes — and returns how many entities it wrote.
func (inst *CanonWireDecoderChannelTable) DecodeAll(b []byte) (nEntities int, err error) {
	for len(b) > 0 {
		var n int
		n, err = inst.DecodeEntity(b)
		if err != nil {
			err = eb.Build().Str("tableName", "channel-table").Int("entityIdx", nEntities).Errorf("unable to decode the entity: %w", err)
			return
		}
		if n <= 0 {
			err = eb.Build().Str("tableName", "channel-table").Int("entityIdx", nEntities).Errorf("the decoder consumed no bytes: %w", cwruntime.ErrOutOfRange)
			return
		}
		b = b[n:]
		nEntities++
	}
	return
}

// decodeEntity walks one entity item: the plain sections under their item
// types, then the tagged slots under their CT signatures. It leaves the dml's
// entity open — DecodeEntity commits or rolls it back.
func (inst *CanonWireDecoderChannelTable) decodeEntity() (err error) {
	er := inst.er
	r := inst.r
	er.Begin()
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "channel-table").Errorf("unable to begin the entity: %w", err)
		return
	}
	for {
		itemType, nCols, ok := er.NextPlain()
		if !ok {
			break
		}
		switch itemType {
		case common.PlainItemTypeEntityId:
			if nCols != 1 {
				err = eb.Build().Stringer("plainItemType", itemType).Int("columns", nCols).Int("want", 1).Errorf("the plain section does not carry the number of columns the table declares: %w", cwruntime.ErrOutOfRange)
				return
			}
			dp00 := r.ReadU64()
			if err = r.Err(); err != nil {
				err = eb.Build().Stringer("plainItemType", itemType).Errorf("unable to read the plain section: %w", err)
				return
			}
			inst.dml.SetId(dp00)
		default:
			err = eb.Build().Stringer("plainItemType", itemType).Errorf("the table declares no plain section of this item type: %w", cwruntime.ErrUnknownPlain)
			return
		}
	}
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "channel-table").Errorf("unable to read the entity's plain sections: %w", err)
		return
	}
	for {
		sig, nAttrs, ok := er.NextSlot()
		if !ok {
			break
		}
		switch string(sig) {
		case CanonWireSignatureChannelTableHparam: // hparam
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 float64
				{
					p := r.Pos()
					dv00 = r.ReadF64()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "hparam").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotChannelTableHparam) {
					err = eb.Build().Str("slot", "hparam").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section hparam
					at := inst.dml.GetSectionHparam().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelHighCardRefParametrized:
							at.AddMembershipHighCardRefParametrizedP(m.Params)
						default:
							err = eb.Build().Str("section", "hparam").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureChannelTableHref: // href
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 int64
				{
					p := r.Pos()
					dv00 = r.ReadI64()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "href").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotChannelTableHref) {
					err = eb.Build().Str("slot", "href").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section href
					at := inst.dml.GetSectionHref().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelHighCardRef:
							at.AddMembershipHighCardRefP(m.Ref)
						default:
							err = eb.Build().Str("section", "href").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureChannelTableHverb: // hverb
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 string
				{
					p := r.Pos()
					dv00 = r.ReadTextString()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "hverb").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotChannelTableHverb) {
					err = eb.Build().Str("slot", "hverb").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section hverb
					at := inst.dml.GetSectionHverb().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelHighCardVerbatim:
							at.AddMembershipHighCardVerbatimP(m.Verbatim)
						default:
							err = eb.Build().Str("section", "hverb").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureChannelTableMref: // mref
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 uint64
				{
					p := r.Pos()
					dv00 = r.ReadU64()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "mref").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotChannelTableMref) {
					err = eb.Build().Str("slot", "mref").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section mref
					at := inst.dml.GetSectionMref().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelMixedLowCardRef:
							at.AddMembershipMixedLowCardRefP(m.Ref, m.Params)
						default:
							err = eb.Build().Str("section", "mref").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureChannelTableLparam: // lparam
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 []byte
				{
					p := r.Pos()
					dv00 = r.ReadBytes()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "lparam").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotChannelTableLparam) {
					err = eb.Build().Str("slot", "lparam").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section lparam
					at := inst.dml.GetSectionLparam().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelLowCardRefParametrized:
							at.AddMembershipLowCardRefParametrizedP(m.Params)
						default:
							err = eb.Build().Str("section", "lparam").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		default:
			err = eb.Build().Bytes("signature", sig).Errorf("the table has no slot for this canonical type signature: %w", cwruntime.ErrUnknownSlot)
			return
		}
	}
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "channel-table").Errorf("unable to read the entity's tagged slots: %w", err)
		return
	}
	er.End()
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "channel-table").Errorf("unable to close the entity: %w", err)
		return
	}
	return
}
