// Code generated by Leeway canonwire (github.com/stergiotis/boxer/public/semistructured/leeway/canonwire.test); DO NOT EDIT.

package example

import (
	"io"
	"time"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	cwruntime "github.com/stergiotis/boxer/public/semistructured/leeway/canonwire/runtime"
	"github.com/stergiotis/boxer/public/semistructured/leeway/common"
	"github.com/stergiotis/boxer/public/semistructured/leeway/mappingplan"
	"github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// The imports a table without plain sections, without memberships or without a
// temporal column would otherwise leave unused. The generated code is one file
// per table and the set it touches depends on the table's shape.
var (
	_ = time.Time{}
	_ = common.PlainItemTypeNone
	_ = mappingplan.MembershipChannelLowCardRef
	_ = eb.Build
)

///////////////////////////////////////////////////////////////////
// code generator
// canonwire.(*GoClassBuilder).ComposeCodec
// ./public/semistructured/leeway/canonwire/lw_cw_generator.go:209

// The slot keys of ADR-0210 SD2: a tagged section's CT group, or a co-section
// group's CT signature. They key the entity item's tagged map and are the only
// thing on the wire that comes from fixed-table's description: section names, column
// names, aspects and hints are not.

// CanonWireSignatureFixedTableCodes keys the slot of section codes.
const CanonWireSignatureFixedTableCodes = "sx3h"

// CanonWireSignatureFixedTableCode keys the slot of section code.
const CanonWireSignatureFixedTableCode = "sx4"

// CanonWireSignatureFixedTableHash keys the slot of section hash.
const CanonWireSignatureFixedTableHash = "yx4"

// CanonWireSlotFixedTableE names the table's tagged slots.
//
// They are in slot-table order — by signature, then by first section index —
// so the ordinals do not depend on map iteration.
type CanonWireSlotFixedTableE uint8

const (
	CanonWireSlotFixedTableCodes CanonWireSlotFixedTableE = iota
	CanonWireSlotFixedTableCode
	CanonWireSlotFixedTableHash
)

func (inst CanonWireSlotFixedTableE) String() string {
	switch inst {
	case CanonWireSlotFixedTableCodes:
		return "codes"
	case CanonWireSlotFixedTableCode:
		return "code"
	case CanonWireSlotFixedTableHash:
		return "hash"
	}
	return "<invalid>"
}

// Signature returns the slot's CT signature — the key it rides under in an
// entity's tagged map.
func (inst CanonWireSlotFixedTableE) Signature() string {
	switch inst {
	case CanonWireSlotFixedTableCodes:
		return CanonWireSignatureFixedTableCodes
	case CanonWireSlotFixedTableCode:
		return CanonWireSignatureFixedTableCode
	case CanonWireSlotFixedTableHash:
		return CanonWireSignatureFixedTableHash
	}
	return ""
}

// Ambiguous reports whether the slot's signature is carried by more than one
// slot of this table. Those are the slots ADR-0210 SD5's dispatch has to
// resolve: the encoder consults its tagger for every attribute of one, and for
// no attribute of any other.
func (inst CanonWireSlotFixedTableE) Ambiguous() bool {
	_ = inst
	return false
}

// CanonWireTaggerFixedTableI is the encoder half of ADR-0210 SD5's dispatch pair.
//
// It is consulted for every attribute of a slot whose signature is ambiguous
// in this table, and its small opaque integer rides as the attribute item's
// trailing element for the decoding side's DispatcherI to read. Attributes of
// an unambiguous slot never reach it and never carry a discriminator.
type CanonWireTaggerFixedTableI interface {
	Tag(slot CanonWireSlotFixedTableE, entityIdx runtime.EntityIdx, attrIdx runtime.AttributeIdx) uint64
}

// CanonWireOrdinalTaggerFixedTable is the built-in tagger of ADR-0210 SD5.
//
// It returns the slot's ordinal within its ambiguity set, which round-trips
// between two tables that declare the same sections in the same order and is
// explicitly that declaration-order coupling — a consumer that wants
// section-name independence supplies its own pair.
type CanonWireOrdinalTaggerFixedTable struct{}

var _ CanonWireTaggerFixedTableI = CanonWireOrdinalTaggerFixedTable{}

func (inst CanonWireOrdinalTaggerFixedTable) Tag(slot CanonWireSlotFixedTableE, entityIdx runtime.EntityIdx, attrIdx runtime.AttributeIdx) uint64 {
	_ = slot
	return 0
}

// CanonWireEncoderFixedTable writes entities of a loaded ReadAccessFixedTable as the leeway canonical wire (ADR-0210).
//
// It reads the generated readaccess accessors directly: no reflection, no
// arrow.Array type switch, one typed runtime call per column. The buffers are
// held on the encoder and reused across entities, so it is not goroutine-safe;
// one encoder per encoding goroutine.
type CanonWireEncoderFixedTable struct {
	tagger            CanonWireTaggerFixedTableI
	ra                *ReadAccessFixedTable
	ew                *cwruntime.EntityWriter
	awFixedTableCodes *cwruntime.AttributeWriter
	awFixedTableCode  *cwruntime.AttributeWriter
	awFixedTableHash  *cwruntime.AttributeWriter

	// The readaccess container accessors are iterators and report no
	// length, so an `h` column's elements are collected before the array
	// head can be written. One reusable slice per column.
	scratchTaggedCodesCs []string
}

// NewCanonWireEncoderFixedTable binds an encoder to ra, which must already be loaded from a batch.
//
// tagger may be nil: no attribute then carries an SD5 discriminator, and the
// decoding side has to resolve an ambiguous signature from content alone.
func NewCanonWireEncoderFixedTable(ra *ReadAccessFixedTable, tagger CanonWireTaggerFixedTableI) (inst *CanonWireEncoderFixedTable, err error) {
	if ra == nil {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("no read access to encode from")
		return
	}
	inst = &CanonWireEncoderFixedTable{ra: ra, tagger: tagger}
	inst.ew, err = cwruntime.NewEntityWriter()
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "fixed-table").Errorf("unable to create the entity writer: %w", err)
		return
	}
	inst.awFixedTableCodes, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "fixed-table").Str("slot", "codes").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awFixedTableCode, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "fixed-table").Str("slot", "code").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awFixedTableHash, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "fixed-table").Str("slot", "hash").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	return
}

// EncodeEntity writes one entity item to w: the plain sections under their item
// types, then the tagged slots under their CT signatures, both in the order the
// form fixes (ADR-0210 SD1–SD3). The attributes are held back only until the
// item is complete, which is what lets the writer sort them into canonical
// order.
func (inst *CanonWireEncoderFixedTable) EncodeEntity(entityIdx runtime.EntityIdx, w io.Writer) (err error) {
	ew := inst.ew
	ew.Begin()
	{ // plain section entity-id — u64
		cw := ew.BeginPlain(common.PlainItemTypeEntityId, 1)
		{
			v := inst.ra.EntityId.GetAttrValueId(entityIdx)
			cw.WriteUint(uint64(v))
		}
		ew.EndPlain()
	}
	{ // slot codes — "sx3h"
		sw := ew.Slot(CanonWireSignatureFixedTableCodes)
		aw := inst.awFixedTableCodes
		n := inst.ra.Codes.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v := range inst.ra.Codes.Memberships.GetMembValueLowCardRef(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelLowCardRef, v, nil, nil)
			}
			{
				cw := aw.BeginValue()
				s := inst.scratchTaggedCodesCs[:0]
				for v := range inst.ra.Codes.Attributes.GetAttrValueCs(entityIdx, attrIdx) {
					s = append(s, v)
				}
				inst.scratchTaggedCodesCs = s
				cw.ArrayHead(len(s))
				for _, v := range s {
					cw.WriteTextString(v)
				}
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "codes").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot code — "sx4"
		sw := ew.Slot(CanonWireSignatureFixedTableCode)
		aw := inst.awFixedTableCode
		n := inst.ra.Code.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v := range inst.ra.Code.Memberships.GetMembValueLowCardRef(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelLowCardRef, v, nil, nil)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Code.Attributes.GetAttrValueC(entityIdx, attrIdx)
				cw.WriteTextString(v)
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "code").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot hash — "yx4"
		sw := ew.Slot(CanonWireSignatureFixedTableHash)
		aw := inst.awFixedTableHash
		n := inst.ra.Hash.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v := range inst.ra.Hash.Memberships.GetMembValueLowCardRef(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelLowCardRef, v, nil, nil)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Hash.Attributes.GetAttrValueH(entityIdx, attrIdx)
				cw.WriteBytes(v[:])
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "hash").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	err = ew.Flush(w)
	if err != nil {
		err = eb.Build().Str("tableName", "fixed-table").Int("entityIdx", int(entityIdx)).Errorf("unable to flush the entity: %w", err)
	}
	return
}

// EncodeAll writes every entity of the loaded batch to w as a CBOR sequence
// (RFC 8742): one entity item after another, with no framing and no header.
func (inst *CanonWireEncoderFixedTable) EncodeAll(w io.Writer) (err error) {
	n := inst.ra.GetNumberOfEntities()
	for i := range n {
		err = inst.EncodeEntity(runtime.EntityIdx(i), w)
		if err != nil {
			err = eb.Build().Str("tableName", "fixed-table").Int("entityIdx", i).Errorf("unable to encode the entity: %w", err)
			return
		}
	}
	return
}

// canonWireAmbiguousFixedTable reports whether any signature of fixed-table is carried by more than one slot.
// It is what the decoder's constructor checks: an ambiguous table cannot be
// decoded without a dispatcher, and the refusal is at construction rather than
// at the first attribute that needs one (ADR-0210 SD5).
const canonWireAmbiguousFixedTable = false

// canonWireAcceptMasksFixedTable holds, per slot in slot order, the membership channels each member
// section accepts — one bitmask per section in signature order, over the
// mappingplan channel ordinals. It is the input to the narrowing step of
// ADR-0210 SD5: a section that cannot store the memberships an attribute
// carries is not that attribute's target.
var canonWireAcceptMasksFixedTable = [][]uint32{
	{0x01}, // codes
	{0x01}, // code
	{0x01}, // hash
}

// CanonWireDispatcherFixedTableI is the decoder half of ADR-0210 SD5's dispatch pair.
//
// It is consulted only for an attribute whose signature more than one slot of
// this table carries *and* whose memberships more than one of those slots can
// store: candidates holds the survivors of the narrowing step, in slot order,
// and is never empty and never of length one — those two cases are decided
// without a call. attr is the attribute as the wire carried it, with nothing
// of either endpoint's table description in it.
//
// The returned slot must be one of the candidates; anything else fails the
// entity.
type CanonWireDispatcherFixedTableI interface {
	Dispatch(candidates []CanonWireSlotFixedTableE, attr *cwruntime.AttributeView) (slot CanonWireSlotFixedTableE, err error)
}

// CanonWireOrdinalDispatcherFixedTable is the built-in dispatcher of ADR-0210 SD5 and the mirror of CanonWireOrdinalTaggerFixedTable: it reads
// the attribute's discriminator as the slot's ordinal within the full
// ambiguity set. It round-trips between two tables that declare the same
// sections in the same order, and is explicitly that declaration-order
// coupling — a consumer that wants section-name independence supplies its own
// pair.
type CanonWireOrdinalDispatcherFixedTable struct{}

var _ CanonWireDispatcherFixedTableI = CanonWireOrdinalDispatcherFixedTable{}

func (inst CanonWireOrdinalDispatcherFixedTable) Dispatch(candidates []CanonWireSlotFixedTableE, attr *cwruntime.AttributeView) (slot CanonWireSlotFixedTableE, err error) {
	_ = candidates
	_ = attr
	err = eb.Build().Str("tableName", "fixed-table").Errorf("the table has no ambiguous signature, so no attribute is dispatched: %w", cwruntime.ErrDispatch)
	return
}

// CanonWireDecoderFixedTable decodes leeway canonical wire entities (ADR-0210) into a InEntityFixedTable.
//
// It drives the generated dml builders directly: the plain setters, then one
// BeginAttribute / container append / AddMembership… / EndAttribute per wire
// attribute, then CommitEntity. No reflection, one typed runtime call per
// column. The buffers are held on the decoder and reused across entities, so
// it is not goroutine-safe; one decoder per decoding goroutine.
//
// A plain section the wire does not carry is simply not set — the entity gets
// the dml's zero for it, which is the entity a writer that never called that
// setter would have produced. Attributes come back in the form's canonical
// order, not the order they were written in.
type CanonWireDecoderFixedTable struct {
	dispatcher CanonWireDispatcherFixedTableI
	dml        *InEntityFixedTable
	r          *cwruntime.CborReader
	er         *cwruntime.EntityReader
	groups     [][]cwruntime.MembershipView
	values     [][]byte
	cands      []CanonWireSlotFixedTableE

	// A container column arrives as an array head and its elements; the dml
	// takes one element at a time, or a plain section's whole slice. One
	// reusable slice per column of the wire's key order.
	scratchSig00Col00 []string
	view              cwruntime.AttributeView
}

// NewCanonWireDecoderFixedTable binds a decoder to dml, which it drives one entity at a time.
//
// dispatcher may be nil only for a table with no ambiguous signature — canonWireAmbiguousFixedTable says
// which this is. A table that has one cannot resolve an attribute's slot from
// the key alone, so it is refused here rather than at the first attribute that
// would have needed the hook (ADR-0210 SD5).
func NewCanonWireDecoderFixedTable(dml *InEntityFixedTable, dispatcher CanonWireDispatcherFixedTableI) (inst *CanonWireDecoderFixedTable, err error) {
	if dml == nil {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("no dml builder to decode into")
		return
	}
	if dispatcher == nil && canonWireAmbiguousFixedTable {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("the table carries a signature more than one slot claims, which needs a dispatcher: %w", cwruntime.ErrDispatch)
		return
	}
	inst = &CanonWireDecoderFixedTable{dml: dml, dispatcher: dispatcher}
	inst.r = cwruntime.NewCborReader(nil)
	inst.er = cwruntime.NewEntityReader(inst.r)
	inst.groups = make([][]cwruntime.MembershipView, 1)
	inst.values = make([][]byte, 1)
	return
}

// slotAcceptsChannels is the narrowing step of ADR-0210 SD5: every membership
// the attribute carries must land on a channel the member section at that
// group's position declares. It is also the check an unambiguous slot goes
// through, because a signature match does not imply the carriage matches.
func (inst *CanonWireDecoderFixedTable) slotAcceptsChannels(slot CanonWireSlotFixedTableE) (accepts bool) {
	if int(slot) >= len(canonWireAcceptMasksFixedTable) {
		return false
	}
	masks := canonWireAcceptMasksFixedTable[slot]
	if len(masks) != len(inst.view.Groups) {
		return false
	}
	for g, mask := range masks {
		for _, m := range inst.view.Groups[g] {
			if uint(m.Channel) >= 32 || mask&(uint32(1)<<uint(m.Channel)) == 0 {
				return false
			}
		}
	}
	return true
}

// DecodeEntity decodes the one entity item at the start of b and writes it
// through the dml, returning how many bytes it consumed. On any failure the
// entity is rolled back, so the builder is left where it was and the next
// entity can still be decoded into it.
func (inst *CanonWireDecoderFixedTable) DecodeEntity(b []byte) (n int, err error) {
	inst.er.Reset(b)
	inst.dml.BeginEntity()
	err = inst.decodeEntity()
	if err == nil {
		err = inst.dml.CheckErrors()
		if err != nil {
			err = eb.Build().Str("tableName", "fixed-table").Errorf("the dml builder rejected the decoded entity: %w", err)
		}
	}
	if err != nil {
		if rerr := inst.dml.RollbackEntity(); rerr != nil {
			err = eb.Build().Str("tableName", "fixed-table").Str("rollbackError", rerr.Error()).Errorf("unable to roll the entity back: %w", err)
		}
		return 0, err
	}
	err = inst.dml.CommitEntity()
	if err != nil {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("unable to commit the decoded entity: %w", err)
		return 0, err
	}
	return inst.r.Pos(), nil
}

// DecodeAll decodes a CBOR sequence (RFC 8742) of entity items — what
// EncodeAll writes — and returns how many entities it wrote.
func (inst *CanonWireDecoderFixedTable) DecodeAll(b []byte) (nEntities int, err error) {
	for len(b) > 0 {
		var n int
		n, err = inst.DecodeEntity(b)
		if err != nil {
			err = eb.Build().Str("tableName", "fixed-table").Int("entityIdx", nEntities).Errorf("unable to decode the entity: %w", err)
			return
		}
		if n <= 0 {
			err = eb.Build().Str("tableName", "fixed-table").Int("entityIdx", nEntities).Errorf("the decoder consumed no bytes: %w", cwruntime.ErrOutOfRange)
			return
		}
		b = b[n:]
		nEntities++
	}
	return
}

// decodeEntity walks one entity item: the plain sections under their item
// types, then the tagged slots under their CT signatures. It leaves the dml's
// entity open — DecodeEntity commits or rolls it back.
func (inst *CanonWireDecoderFixedTable) decodeEntity() (err error) {
	er := inst.er
	r := inst.r
	er.Begin()
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("unable to begin the entity: %w", err)
		return
	}
	for {
		itemType, nCols, ok := er.NextPlain()
		if !ok {
			break
		}
		switch itemType {
		case common.PlainItemTypeEntityId:
			if nCols != 1 {
				err = eb.Build().Stringer("plainItemType", itemType).Int("columns", nCols).Int("want", 1).Errorf("the plain section does not carry the number of columns the table declares: %w", cwruntime.ErrOutOfRange)
				return
			}
			dp00 := r.ReadU64()
			if err = r.Err(); err != nil {
				err = eb.Build().Stringer("plainItemType", itemType).Errorf("unable to read the plain section: %w", err)
				return
			}
			inst.dml.SetId(dp00)
		default:
			err = eb.Build().Stringer("plainItemType", itemType).Errorf("the table declares no plain section of this item type: %w", cwruntime.ErrUnknownPlain)
			return
		}
	}
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("unable to read the entity's plain sections: %w", err)
		return
	}
	for {
		sig, nAttrs, ok := er.NextSlot()
		if !ok {
			break
		}
		switch string(sig) {
		case CanonWireSignatureFixedTableCodes: // codes
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				{
					p := r.Pos()
					nElem := r.ReadArrayHead()
					s := inst.scratchSig00Col00[:0]
					for j := 0; j < nElem; j++ {
						s = append(s, r.ReadTextStringFixed(3))
					}
					inst.scratchSig00Col00 = s
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "codes").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotFixedTableCodes) {
					err = eb.Build().Str("slot", "codes").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section codes
					at := inst.dml.GetSectionCodes().BeginAttribute()
					for _, v := range inst.scratchSig00Col00 {
						at.AddToContainerP(v)
					}
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelLowCardRef:
							at.AddMembershipLowCardRefP(m.Ref)
						default:
							err = eb.Build().Str("section", "codes").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureFixedTableCode: // code
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 string
				{
					p := r.Pos()
					dv00 = r.ReadTextStringFixed(4)
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "code").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotFixedTableCode) {
					err = eb.Build().Str("slot", "code").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section code
					at := inst.dml.GetSectionCode().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelLowCardRef:
							at.AddMembershipLowCardRefP(m.Ref)
						default:
							err = eb.Build().Str("section", "code").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureFixedTableHash: // hash
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 [4]byte
				{
					p := r.Pos()
					r.ReadBytesInto(dv00[:])
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "hash").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotFixedTableHash) {
					err = eb.Build().Str("slot", "hash").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section hash
					at := inst.dml.GetSectionHash().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelLowCardRef:
							at.AddMembershipLowCardRefP(m.Ref)
						default:
							err = eb.Build().Str("section", "hash").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		default:
			err = eb.Build().Bytes("signature", sig).Errorf("the table has no slot for this canonical type signature: %w", cwruntime.ErrUnknownSlot)
			return
		}
	}
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("unable to read the entity's tagged slots: %w", err)
		return
	}
	er.End()
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "fixed-table").Errorf("unable to close the entity: %w", err)
		return
	}
	return
}
