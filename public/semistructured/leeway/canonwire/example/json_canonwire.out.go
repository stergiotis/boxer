// Code generated by Leeway canonwire (github.com/stergiotis/boxer/public/semistructured/leeway/canonwire.test); DO NOT EDIT.

package example

import (
	"io"
	"time"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	cwruntime "github.com/stergiotis/boxer/public/semistructured/leeway/canonwire/runtime"
	"github.com/stergiotis/boxer/public/semistructured/leeway/common"
	"github.com/stergiotis/boxer/public/semistructured/leeway/mappingplan"
	"github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// The imports a table without plain sections, without memberships or without a
// temporal column would otherwise leave unused. The generated code is one file
// per table and the set it touches depends on the table's shape.
var (
	_ = time.Time{}
	_ = common.PlainItemTypeNone
	_ = mappingplan.MembershipChannelLowCardRef
	_ = eb.Build
)

///////////////////////////////////////////////////////////////////
// code generator
// canonwire.(*GoClassBuilder).ComposeCodec
// ./public/semistructured/leeway/canonwire/lw_cw_generator.go:209

// The slot keys of ADR-0210 SD2: a tagged section's CT group, or a co-section
// group's CT signature. They key the entity item's tagged map and are the only
// thing on the wire that comes from json's description: section names, column
// names, aspects and hints are not.

// CanonWireSignatureJsonUndefined keys the slot of section undefined.
const CanonWireSignatureJsonUndefined = ""

// CanonWireSignatureJsonNull keys the slot of section null.
const CanonWireSignatureJsonNull = ""

// CanonWireSignatureJsonEmptyObject keys the slot of section emptyObject.
const CanonWireSignatureJsonEmptyObject = ""

// CanonWireSignatureJsonEmptyArray keys the slot of section emptyArray.
const CanonWireSignatureJsonEmptyArray = ""

// CanonWireSignatureJsonBool keys the slot of section bool.
const CanonWireSignatureJsonBool = "b"

// CanonWireSignatureJsonFloat64 keys the slot of section float64.
const CanonWireSignatureJsonFloat64 = "f64"

// CanonWireSignatureJsonInt64 keys the slot of section int64.
const CanonWireSignatureJsonInt64 = "i64"

// CanonWireSignatureJsonString keys the slot of section string.
const CanonWireSignatureJsonString = "s"

// CanonWireSignatureJsonSymbol keys the slot of section symbol.
const CanonWireSignatureJsonSymbol = "s"

// CanonWireSlotJsonE names the table's tagged slots.
//
// They are in slot-table order — by signature, then by first section index —
// so the ordinals do not depend on map iteration.
type CanonWireSlotJsonE uint8

const (
	CanonWireSlotJsonUndefined CanonWireSlotJsonE = iota
	CanonWireSlotJsonNull
	CanonWireSlotJsonEmptyObject
	CanonWireSlotJsonEmptyArray
	CanonWireSlotJsonBool
	CanonWireSlotJsonFloat64
	CanonWireSlotJsonInt64
	CanonWireSlotJsonString
	CanonWireSlotJsonSymbol
)

func (inst CanonWireSlotJsonE) String() string {
	switch inst {
	case CanonWireSlotJsonUndefined:
		return "undefined"
	case CanonWireSlotJsonNull:
		return "null"
	case CanonWireSlotJsonEmptyObject:
		return "emptyObject"
	case CanonWireSlotJsonEmptyArray:
		return "emptyArray"
	case CanonWireSlotJsonBool:
		return "bool"
	case CanonWireSlotJsonFloat64:
		return "float64"
	case CanonWireSlotJsonInt64:
		return "int64"
	case CanonWireSlotJsonString:
		return "string"
	case CanonWireSlotJsonSymbol:
		return "symbol"
	}
	return "<invalid>"
}

// Signature returns the slot's CT signature — the key it rides under in an
// entity's tagged map.
func (inst CanonWireSlotJsonE) Signature() string {
	switch inst {
	case CanonWireSlotJsonUndefined:
		return CanonWireSignatureJsonUndefined
	case CanonWireSlotJsonNull:
		return CanonWireSignatureJsonNull
	case CanonWireSlotJsonEmptyObject:
		return CanonWireSignatureJsonEmptyObject
	case CanonWireSlotJsonEmptyArray:
		return CanonWireSignatureJsonEmptyArray
	case CanonWireSlotJsonBool:
		return CanonWireSignatureJsonBool
	case CanonWireSlotJsonFloat64:
		return CanonWireSignatureJsonFloat64
	case CanonWireSlotJsonInt64:
		return CanonWireSignatureJsonInt64
	case CanonWireSlotJsonString:
		return CanonWireSignatureJsonString
	case CanonWireSlotJsonSymbol:
		return CanonWireSignatureJsonSymbol
	}
	return ""
}

// Ambiguous reports whether the slot's signature is carried by more than one
// slot of this table. Those are the slots ADR-0210 SD5's dispatch has to
// resolve: the encoder consults its tagger for every attribute of one, and for
// no attribute of any other.
func (inst CanonWireSlotJsonE) Ambiguous() bool {
	switch inst {
	case CanonWireSlotJsonUndefined:
		return true
	case CanonWireSlotJsonNull:
		return true
	case CanonWireSlotJsonEmptyObject:
		return true
	case CanonWireSlotJsonEmptyArray:
		return true
	case CanonWireSlotJsonString:
		return true
	case CanonWireSlotJsonSymbol:
		return true
	}
	return false
}

// CanonWireTaggerJsonI is the encoder half of ADR-0210 SD5's dispatch pair.
//
// It is consulted for every attribute of a slot whose signature is ambiguous
// in this table, and its small opaque integer rides as the attribute item's
// trailing element for the decoding side's DispatcherI to read. Attributes of
// an unambiguous slot never reach it and never carry a discriminator.
type CanonWireTaggerJsonI interface {
	Tag(slot CanonWireSlotJsonE, entityIdx runtime.EntityIdx, attrIdx runtime.AttributeIdx) uint64
}

// CanonWireOrdinalTaggerJson is the built-in tagger of ADR-0210 SD5.
//
// It returns the slot's ordinal within its ambiguity set, which round-trips
// between two tables that declare the same sections in the same order and is
// explicitly that declaration-order coupling — a consumer that wants
// section-name independence supplies its own pair.
type CanonWireOrdinalTaggerJson struct{}

var _ CanonWireTaggerJsonI = CanonWireOrdinalTaggerJson{}

func (inst CanonWireOrdinalTaggerJson) Tag(slot CanonWireSlotJsonE, entityIdx runtime.EntityIdx, attrIdx runtime.AttributeIdx) uint64 {
	switch slot {
	case CanonWireSlotJsonNull:
		return 1
	case CanonWireSlotJsonEmptyObject:
		return 2
	case CanonWireSlotJsonEmptyArray:
		return 3
	case CanonWireSlotJsonSymbol:
		return 1
	}
	return 0
}

// CanonWireEncoderJson writes entities of a loaded ReadAccessJson as the leeway canonical wire (ADR-0210).
//
// It reads the generated readaccess accessors directly: no reflection, no
// arrow.Array type switch, one typed runtime call per column. The buffers are
// held on the encoder and reused across entities, so it is not goroutine-safe;
// one encoder per encoding goroutine.
type CanonWireEncoderJson struct {
	tagger            CanonWireTaggerJsonI
	ra                *ReadAccessJson
	ew                *cwruntime.EntityWriter
	awJsonUndefined   *cwruntime.AttributeWriter
	awJsonNull        *cwruntime.AttributeWriter
	awJsonEmptyObject *cwruntime.AttributeWriter
	awJsonEmptyArray  *cwruntime.AttributeWriter
	awJsonBool        *cwruntime.AttributeWriter
	awJsonFloat64     *cwruntime.AttributeWriter
	awJsonInt64       *cwruntime.AttributeWriter
	awJsonString      *cwruntime.AttributeWriter
	awJsonSymbol      *cwruntime.AttributeWriter
}

// NewCanonWireEncoderJson binds an encoder to ra, which must already be loaded from a batch.
//
// tagger may be nil: no attribute then carries an SD5 discriminator, and the
// decoding side has to resolve an ambiguous signature from content alone.
func NewCanonWireEncoderJson(ra *ReadAccessJson, tagger CanonWireTaggerJsonI) (inst *CanonWireEncoderJson, err error) {
	if ra == nil {
		err = eb.Build().Str("tableName", "json").Errorf("no read access to encode from")
		return
	}
	inst = &CanonWireEncoderJson{ra: ra, tagger: tagger}
	inst.ew, err = cwruntime.NewEntityWriter()
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Errorf("unable to create the entity writer: %w", err)
		return
	}
	inst.awJsonUndefined, err = cwruntime.NewAttributeWriter(0, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "undefined").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonNull, err = cwruntime.NewAttributeWriter(0, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "null").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonEmptyObject, err = cwruntime.NewAttributeWriter(0, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "emptyObject").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonEmptyArray, err = cwruntime.NewAttributeWriter(0, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "emptyArray").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonBool, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "bool").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonFloat64, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "float64").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonInt64, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "int64").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonString, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "string").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	inst.awJsonSymbol, err = cwruntime.NewAttributeWriter(1, 1)
	if err != nil {
		inst = nil
		err = eb.Build().Str("tableName", "json").Str("slot", "symbol").Errorf("unable to create the attribute writer: %w", err)
		return
	}
	return
}

// EncodeEntity writes one entity item to w: the plain sections under their item
// types, then the tagged slots under their CT signatures, both in the order the
// form fixes (ADR-0210 SD1–SD3). The attributes are held back only until the
// item is complete, which is what lets the writer sort them into canonical
// order.
func (inst *CanonWireEncoderJson) EncodeEntity(entityIdx runtime.EntityIdx, w io.Writer) (err error) {
	ew := inst.ew
	ew.Begin()
	{ // plain section entity-id — y
		cw := ew.BeginPlain(common.PlainItemTypeEntityId, 1)
		{
			v := inst.ra.EntityId.GetAttrValueBlake3hash(entityIdx)
			cw.WriteBytes(v)
		}
		ew.EndPlain()
	}
	{ // slot undefined — ""
		sw := ew.Slot(CanonWireSignatureJsonUndefined)
		aw := inst.awJsonUndefined
		n := inst.ra.Undefined.Memberships.AccelMixedLowCardVerbatim.GetEntityAttributeCount(int(entityIdx))
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.Undefined.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			if inst.tagger != nil {
				aw.Discriminator(inst.tagger.Tag(CanonWireSlotJsonUndefined, entityIdx, attrIdx))
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "undefined").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot null — ""
		sw := ew.Slot(CanonWireSignatureJsonNull)
		aw := inst.awJsonNull
		n := inst.ra.Null.Memberships.AccelMixedLowCardVerbatim.GetEntityAttributeCount(int(entityIdx))
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.Null.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			if inst.tagger != nil {
				aw.Discriminator(inst.tagger.Tag(CanonWireSlotJsonNull, entityIdx, attrIdx))
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "null").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot emptyObject — ""
		sw := ew.Slot(CanonWireSignatureJsonEmptyObject)
		aw := inst.awJsonEmptyObject
		n := inst.ra.EmptyObject.Memberships.AccelMixedLowCardVerbatim.GetEntityAttributeCount(int(entityIdx))
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.EmptyObject.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			if inst.tagger != nil {
				aw.Discriminator(inst.tagger.Tag(CanonWireSlotJsonEmptyObject, entityIdx, attrIdx))
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "emptyObject").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot emptyArray — ""
		sw := ew.Slot(CanonWireSignatureJsonEmptyArray)
		aw := inst.awJsonEmptyArray
		n := inst.ra.EmptyArray.Memberships.AccelMixedLowCardVerbatim.GetEntityAttributeCount(int(entityIdx))
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.EmptyArray.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			if inst.tagger != nil {
				aw.Discriminator(inst.tagger.Tag(CanonWireSlotJsonEmptyArray, entityIdx, attrIdx))
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "emptyArray").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot bool — "b"
		sw := ew.Slot(CanonWireSignatureJsonBool)
		aw := inst.awJsonBool
		n := inst.ra.Bool.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.Bool.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Bool.Attributes.GetAttrValueValue(entityIdx, attrIdx)
				cw.WriteBool(v)
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "bool").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot float64 — "f64"
		sw := ew.Slot(CanonWireSignatureJsonFloat64)
		aw := inst.awJsonFloat64
		n := inst.ra.Float64.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.Float64.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Float64.Attributes.GetAttrValueValue(entityIdx, attrIdx)
				cw.WriteF64(v)
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "float64").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot int64 — "i64"
		sw := ew.Slot(CanonWireSignatureJsonInt64)
		aw := inst.awJsonInt64
		n := inst.ra.Int64.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.Int64.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Int64.Attributes.GetAttrValueValue(entityIdx, attrIdx)
				cw.WriteInt(int64(v))
				aw.EndValue()
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "int64").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot string — "s"
		sw := ew.Slot(CanonWireSignatureJsonString)
		aw := inst.awJsonString
		n := inst.ra.String.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.String.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.String.Attributes.GetAttrValueValue(entityIdx, attrIdx)
				cw.WriteTextString(v)
				aw.EndValue()
			}
			if inst.tagger != nil {
				aw.Discriminator(inst.tagger.Tag(CanonWireSlotJsonString, entityIdx, attrIdx))
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "string").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	{ // slot symbol — "s"
		sw := ew.Slot(CanonWireSignatureJsonSymbol)
		aw := inst.awJsonSymbol
		n := inst.ra.Symbol.Attributes.GetNumberOfAttributes(entityIdx)
		for attrIdx := runtime.AttributeIdx(0); int64(attrIdx) < n; attrIdx++ {
			aw.Begin()
			for v1, v2 := range inst.ra.Symbol.Memberships.GetMembValueLowCardVerbatimHighCardParams(entityIdx, attrIdx) {
				aw.Membership(0, mappingplan.MembershipChannelMixedLowCardVerbatim, 0, v1, v2)
			}
			{
				cw := aw.BeginValue()
				v := inst.ra.Symbol.Attributes.GetAttrValueValue(entityIdx, attrIdx)
				cw.WriteTextString(v)
				aw.EndValue()
			}
			if inst.tagger != nil {
				aw.Discriminator(inst.tagger.Tag(CanonWireSlotJsonSymbol, entityIdx, attrIdx))
			}
			attr, aerr := aw.End()
			if aerr != nil {
				err = eb.Build().Str("slot", "symbol").Int("entityIdx", int(entityIdx)).Int("attrIdx", int(attrIdx)).Errorf("unable to finish the attribute: %w", aerr)
				return
			}
			sw.Add(attr)
		}
	}
	err = ew.Flush(w)
	if err != nil {
		err = eb.Build().Str("tableName", "json").Int("entityIdx", int(entityIdx)).Errorf("unable to flush the entity: %w", err)
	}
	return
}

// EncodeAll writes every entity of the loaded batch to w as a CBOR sequence
// (RFC 8742): one entity item after another, with no framing and no header.
func (inst *CanonWireEncoderJson) EncodeAll(w io.Writer) (err error) {
	n := inst.ra.GetNumberOfEntities()
	for i := range n {
		err = inst.EncodeEntity(runtime.EntityIdx(i), w)
		if err != nil {
			err = eb.Build().Str("tableName", "json").Int("entityIdx", i).Errorf("unable to encode the entity: %w", err)
			return
		}
	}
	return
}

// canonWireAmbiguousJson reports whether any signature of json is carried by more than one slot.
// It is what the decoder's constructor checks: an ambiguous table cannot be
// decoded without a dispatcher, and the refusal is at construction rather than
// at the first attribute that needs one (ADR-0210 SD5).
const canonWireAmbiguousJson = true

// canonWireAcceptMasksJson holds, per slot in slot order, the membership channels each member
// section accepts — one bitmask per section in signature order, over the
// mappingplan channel ordinals. It is the input to the narrowing step of
// ADR-0210 SD5: a section that cannot store the memberships an attribute
// carries is not that attribute's target.
var canonWireAcceptMasksJson = [][]uint32{
	{0x20}, // undefined
	{0x20}, // null
	{0x20}, // emptyObject
	{0x20}, // emptyArray
	{0x20}, // bool
	{0x20}, // float64
	{0x20}, // int64
	{0x20}, // string
	{0x20}, // symbol
}

// canonWireAmbiguitySetJson00 is the ambiguity set of the signature "": every slot that carries it, in
// slot order. The built-in tagger/dispatcher pair keys on a slot's position in
// this slice.
var canonWireAmbiguitySetJson00 = []CanonWireSlotJsonE{CanonWireSlotJsonUndefined, CanonWireSlotJsonNull, CanonWireSlotJsonEmptyObject, CanonWireSlotJsonEmptyArray}

// canonWireAmbiguitySetJson04 is the ambiguity set of the signature "s": every slot that carries it, in
// slot order. The built-in tagger/dispatcher pair keys on a slot's position in
// this slice.
var canonWireAmbiguitySetJson04 = []CanonWireSlotJsonE{CanonWireSlotJsonString, CanonWireSlotJsonSymbol}

// CanonWireDispatcherJsonI is the decoder half of ADR-0210 SD5's dispatch pair.
//
// It is consulted only for an attribute whose signature more than one slot of
// this table carries *and* whose memberships more than one of those slots can
// store: candidates holds the survivors of the narrowing step, in slot order,
// and is never empty and never of length one — those two cases are decided
// without a call. attr is the attribute as the wire carried it, with nothing
// of either endpoint's table description in it.
//
// The returned slot must be one of the candidates; anything else fails the
// entity.
type CanonWireDispatcherJsonI interface {
	Dispatch(candidates []CanonWireSlotJsonE, attr *cwruntime.AttributeView) (slot CanonWireSlotJsonE, err error)
}

// CanonWireOrdinalDispatcherJson is the built-in dispatcher of ADR-0210 SD5 and the mirror of CanonWireOrdinalTaggerJson: it reads
// the attribute's discriminator as the slot's ordinal within the full
// ambiguity set. It round-trips between two tables that declare the same
// sections in the same order, and is explicitly that declaration-order
// coupling — a consumer that wants section-name independence supplies its own
// pair.
type CanonWireOrdinalDispatcherJson struct{}

var _ CanonWireDispatcherJsonI = CanonWireOrdinalDispatcherJson{}

func (inst CanonWireOrdinalDispatcherJson) Dispatch(candidates []CanonWireSlotJsonE, attr *cwruntime.AttributeView) (slot CanonWireSlotJsonE, err error) {
	if len(candidates) == 0 {
		err = eb.Build().Str("tableName", "json").Errorf("no candidate slot to dispatch to: %w", cwruntime.ErrDispatch)
		return
	}
	if !attr.HasDiscriminator {
		err = eb.Build().Str("tableName", "json").Errorf("the attribute carries no discriminator, which is what the ordinal dispatcher decides by: %w", cwruntime.ErrDispatch)
		return
	}
	var set []CanonWireSlotJsonE
	switch candidates[0] {
	case CanonWireSlotJsonUndefined, CanonWireSlotJsonNull, CanonWireSlotJsonEmptyObject, CanonWireSlotJsonEmptyArray:
		set = canonWireAmbiguitySetJson00
	case CanonWireSlotJsonString, CanonWireSlotJsonSymbol:
		set = canonWireAmbiguitySetJson04
	default:
		err = eb.Build().Str("tableName", "json").Stringer("slot", candidates[0]).Errorf("the candidate is not in any ambiguity set: %w", cwruntime.ErrDispatch)
		return
	}
	if attr.Discriminator >= uint64(len(set)) {
		err = eb.Build().Str("tableName", "json").Uint64("discriminator", attr.Discriminator).Int("ambiguitySet", len(set)).Errorf("the discriminator is outside the ambiguity set: %w", cwruntime.ErrDispatch)
		return
	}
	slot = set[attr.Discriminator]
	for _, c := range candidates {
		if c == slot {
			return
		}
	}
	err = eb.Build().Str("tableName", "json").Uint64("discriminator", attr.Discriminator).Stringer("slot", slot).Errorf("the discriminator names a slot the attribute's memberships rule out: %w", cwruntime.ErrDispatch)
	return
}

// CanonWireDecoderJson decodes leeway canonical wire entities (ADR-0210) into a InEntityJson.
//
// It drives the generated dml builders directly: the plain setters, then one
// BeginAttribute / container append / AddMembership… / EndAttribute per wire
// attribute, then CommitEntity. No reflection, one typed runtime call per
// column. The buffers are held on the decoder and reused across entities, so
// it is not goroutine-safe; one decoder per decoding goroutine.
//
// A plain section the wire does not carry is simply not set — the entity gets
// the dml's zero for it, which is the entity a writer that never called that
// setter would have produced. Attributes come back in the form's canonical
// order, not the order they were written in.
type CanonWireDecoderJson struct {
	dispatcher CanonWireDispatcherJsonI
	dml        *InEntityJson
	r          *cwruntime.CborReader
	er         *cwruntime.EntityReader
	groups     [][]cwruntime.MembershipView
	values     [][]byte
	cands      []CanonWireSlotJsonE
	view       cwruntime.AttributeView
}

// NewCanonWireDecoderJson binds a decoder to dml, which it drives one entity at a time.
//
// dispatcher may be nil only for a table with no ambiguous signature — canonWireAmbiguousJson says
// which this is. A table that has one cannot resolve an attribute's slot from
// the key alone, so it is refused here rather than at the first attribute that
// would have needed the hook (ADR-0210 SD5).
func NewCanonWireDecoderJson(dml *InEntityJson, dispatcher CanonWireDispatcherJsonI) (inst *CanonWireDecoderJson, err error) {
	if dml == nil {
		err = eb.Build().Str("tableName", "json").Errorf("no dml builder to decode into")
		return
	}
	if dispatcher == nil && canonWireAmbiguousJson {
		err = eb.Build().Str("tableName", "json").Errorf("the table carries a signature more than one slot claims, which needs a dispatcher: %w", cwruntime.ErrDispatch)
		return
	}
	inst = &CanonWireDecoderJson{dml: dml, dispatcher: dispatcher}
	inst.r = cwruntime.NewCborReader(nil)
	inst.er = cwruntime.NewEntityReader(inst.r)
	inst.groups = make([][]cwruntime.MembershipView, 1)
	inst.values = make([][]byte, 1)
	return
}

// slotAcceptsChannels is the narrowing step of ADR-0210 SD5: every membership
// the attribute carries must land on a channel the member section at that
// group's position declares. It is also the check an unambiguous slot goes
// through, because a signature match does not imply the carriage matches.
func (inst *CanonWireDecoderJson) slotAcceptsChannels(slot CanonWireSlotJsonE) (accepts bool) {
	if int(slot) >= len(canonWireAcceptMasksJson) {
		return false
	}
	masks := canonWireAcceptMasksJson[slot]
	if len(masks) != len(inst.view.Groups) {
		return false
	}
	for g, mask := range masks {
		for _, m := range inst.view.Groups[g] {
			if uint(m.Channel) >= 32 || mask&(uint32(1)<<uint(m.Channel)) == 0 {
				return false
			}
		}
	}
	return true
}

// DecodeEntity decodes the one entity item at the start of b and writes it
// through the dml, returning how many bytes it consumed. On any failure the
// entity is rolled back, so the builder is left where it was and the next
// entity can still be decoded into it.
func (inst *CanonWireDecoderJson) DecodeEntity(b []byte) (n int, err error) {
	inst.er.Reset(b)
	inst.dml.BeginEntity()
	err = inst.decodeEntity()
	if err == nil {
		err = inst.dml.CheckErrors()
		if err != nil {
			err = eb.Build().Str("tableName", "json").Errorf("the dml builder rejected the decoded entity: %w", err)
		}
	}
	if err != nil {
		if rerr := inst.dml.RollbackEntity(); rerr != nil {
			err = eb.Build().Str("tableName", "json").Str("rollbackError", rerr.Error()).Errorf("unable to roll the entity back: %w", err)
		}
		return 0, err
	}
	err = inst.dml.CommitEntity()
	if err != nil {
		err = eb.Build().Str("tableName", "json").Errorf("unable to commit the decoded entity: %w", err)
		return 0, err
	}
	return inst.r.Pos(), nil
}

// DecodeAll decodes a CBOR sequence (RFC 8742) of entity items — what
// EncodeAll writes — and returns how many entities it wrote.
func (inst *CanonWireDecoderJson) DecodeAll(b []byte) (nEntities int, err error) {
	for len(b) > 0 {
		var n int
		n, err = inst.DecodeEntity(b)
		if err != nil {
			err = eb.Build().Str("tableName", "json").Int("entityIdx", nEntities).Errorf("unable to decode the entity: %w", err)
			return
		}
		if n <= 0 {
			err = eb.Build().Str("tableName", "json").Int("entityIdx", nEntities).Errorf("the decoder consumed no bytes: %w", cwruntime.ErrOutOfRange)
			return
		}
		b = b[n:]
		nEntities++
	}
	return
}

// decodeEntity walks one entity item: the plain sections under their item
// types, then the tagged slots under their CT signatures. It leaves the dml's
// entity open — DecodeEntity commits or rolls it back.
func (inst *CanonWireDecoderJson) decodeEntity() (err error) {
	er := inst.er
	r := inst.r
	er.Begin()
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "json").Errorf("unable to begin the entity: %w", err)
		return
	}
	for {
		itemType, nCols, ok := er.NextPlain()
		if !ok {
			break
		}
		switch itemType {
		case common.PlainItemTypeEntityId:
			if nCols != 1 {
				err = eb.Build().Stringer("plainItemType", itemType).Int("columns", nCols).Int("want", 1).Errorf("the plain section does not carry the number of columns the table declares: %w", cwruntime.ErrOutOfRange)
				return
			}
			dp00 := r.ReadBytes()
			if err = r.Err(); err != nil {
				err = eb.Build().Stringer("plainItemType", itemType).Errorf("unable to read the plain section: %w", err)
				return
			}
			inst.dml.SetId(dp00)
		default:
			err = eb.Build().Stringer("plainItemType", itemType).Errorf("the table declares no plain section of this item type: %w", cwruntime.ErrUnknownPlain)
			return
		}
	}
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "json").Errorf("unable to read the entity's plain sections: %w", err)
		return
	}
	for {
		sig, nAttrs, ok := er.NextSlot()
		if !ok {
			break
		}
		switch string(sig) {
		case CanonWireSignatureJsonUndefined: // undefined|null|emptyObject|emptyArray
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(0, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				inst.view.Values = inst.values[:0]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "undefined|null|emptyObject|emptyArray").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				cands := inst.cands[:0]
				for _, s := range canonWireAmbiguitySetJson00 {
					if inst.slotAcceptsChannels(s) {
						cands = append(cands, s)
					}
				}
				inst.cands = cands
				var slot CanonWireSlotJsonE
				switch len(cands) {
				case 0:
					err = eb.Build().Str("signature", "").Int("attrIdx", a).Errorf("no slot sharing the signature accepts the attribute's membership channels: %w", cwruntime.ErrChannelNotAccepted)
					return
				case 1:
					slot = cands[0]
				default:
					slot, err = inst.dispatcher.Dispatch(cands, &inst.view)
					if err != nil {
						err = eb.Build().Str("signature", "").Int("attrIdx", a).Errorf("the dispatcher refused the attribute: %w", err)
						return
					}
					{
						known := false
						for _, cand := range cands {
							if cand == slot {
								known = true
								break
							}
						}
						if !known {
							err = eb.Build().Str("signature", "").Int("attrIdx", a).Stringer("slot", slot).Errorf("the dispatcher returned a slot that is not a candidate: %w", cwruntime.ErrDispatch)
							return
						}
					}
				}
				switch slot {
				case CanonWireSlotJsonUndefined:
					{ // section undefined
						at := inst.dml.GetSectionUndefined().BeginAttribute()
						for _, m := range inst.view.Groups[0] {
							switch m.Channel {
							case mappingplan.MembershipChannelMixedLowCardVerbatim:
								at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
							default:
								err = eb.Build().Str("section", "undefined").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
								return
							}
						}
						at.EndAttributeP()
					}
				case CanonWireSlotJsonNull:
					{ // section null
						at := inst.dml.GetSectionNull().BeginAttribute()
						for _, m := range inst.view.Groups[0] {
							switch m.Channel {
							case mappingplan.MembershipChannelMixedLowCardVerbatim:
								at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
							default:
								err = eb.Build().Str("section", "null").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
								return
							}
						}
						at.EndAttributeP()
					}
				case CanonWireSlotJsonEmptyObject:
					{ // section emptyObject
						at := inst.dml.GetSectionEmptyObject().BeginAttribute()
						for _, m := range inst.view.Groups[0] {
							switch m.Channel {
							case mappingplan.MembershipChannelMixedLowCardVerbatim:
								at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
							default:
								err = eb.Build().Str("section", "emptyObject").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
								return
							}
						}
						at.EndAttributeP()
					}
				case CanonWireSlotJsonEmptyArray:
					{ // section emptyArray
						at := inst.dml.GetSectionEmptyArray().BeginAttribute()
						for _, m := range inst.view.Groups[0] {
							switch m.Channel {
							case mappingplan.MembershipChannelMixedLowCardVerbatim:
								at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
							default:
								err = eb.Build().Str("section", "emptyArray").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
								return
							}
						}
						at.EndAttributeP()
					}
				default:
					err = eb.Build().Str("signature", "").Int("attrIdx", a).Stringer("slot", slot).Errorf("the chosen slot does not carry the signature: %w", cwruntime.ErrDispatch)
					return
				}
			}
		case CanonWireSignatureJsonBool: // bool
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 bool
				{
					p := r.Pos()
					dv00 = r.ReadBool()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "bool").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotJsonBool) {
					err = eb.Build().Str("slot", "bool").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section bool
					at := inst.dml.GetSectionBool().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelMixedLowCardVerbatim:
							at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
						default:
							err = eb.Build().Str("section", "bool").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureJsonFloat64: // float64
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 float64
				{
					p := r.Pos()
					dv00 = r.ReadF64()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "float64").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotJsonFloat64) {
					err = eb.Build().Str("slot", "float64").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section float64
					at := inst.dml.GetSectionFloat64().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelMixedLowCardVerbatim:
							at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
						default:
							err = eb.Build().Str("section", "float64").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureJsonInt64: // int64
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 int64
				{
					p := r.Pos()
					dv00 = r.ReadI64()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "int64").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				if !inst.slotAcceptsChannels(CanonWireSlotJsonInt64) {
					err = eb.Build().Str("slot", "int64").Int("attrIdx", a).Errorf("the slot does not accept a membership channel the attribute carries: %w", cwruntime.ErrChannelNotAccepted)
					return
				}
				{ // section int64
					at := inst.dml.GetSectionInt64().BeginAttribute(dv00)
					for _, m := range inst.view.Groups[0] {
						switch m.Channel {
						case mappingplan.MembershipChannelMixedLowCardVerbatim:
							at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
						default:
							err = eb.Build().Str("section", "int64").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
							return
						}
					}
					at.EndAttributeP()
				}
			}
		case CanonWireSignatureJsonString: // string|symbol
			for a := 0; a < nAttrs; a++ {
				ar := er.Attributes()
				hasDisc := ar.Begin(1, 1)
				for g := 0; g < 1; g++ {
					nm := ar.NextGroup()
					mv := inst.groups[g][:0]
					for j := 0; j < nm; j++ {
						ch, ref, verbatim, params := ar.Membership()
						mv = append(mv, cwruntime.MembershipView{Channel: ch, Ref: ref, Verbatim: verbatim, Params: params})
					}
					inst.groups[g] = mv
				}
				inst.view.Groups = inst.groups[:1]
				var dv00 string
				{
					p := r.Pos()
					dv00 = r.ReadTextString()
					inst.values[0] = r.Since(p)
				}
				inst.view.Values = inst.values[:1]
				inst.view.HasDiscriminator = hasDisc
				inst.view.Discriminator = 0
				if hasDisc {
					inst.view.Discriminator = ar.Discriminator()
				}
				ar.End()
				if err = r.Err(); err != nil {
					err = eb.Build().Str("slot", "string|symbol").Int("attrIdx", a).Errorf("unable to read the attribute: %w", err)
					return
				}
				cands := inst.cands[:0]
				for _, s := range canonWireAmbiguitySetJson04 {
					if inst.slotAcceptsChannels(s) {
						cands = append(cands, s)
					}
				}
				inst.cands = cands
				var slot CanonWireSlotJsonE
				switch len(cands) {
				case 0:
					err = eb.Build().Str("signature", "s").Int("attrIdx", a).Errorf("no slot sharing the signature accepts the attribute's membership channels: %w", cwruntime.ErrChannelNotAccepted)
					return
				case 1:
					slot = cands[0]
				default:
					slot, err = inst.dispatcher.Dispatch(cands, &inst.view)
					if err != nil {
						err = eb.Build().Str("signature", "s").Int("attrIdx", a).Errorf("the dispatcher refused the attribute: %w", err)
						return
					}
					{
						known := false
						for _, cand := range cands {
							if cand == slot {
								known = true
								break
							}
						}
						if !known {
							err = eb.Build().Str("signature", "s").Int("attrIdx", a).Stringer("slot", slot).Errorf("the dispatcher returned a slot that is not a candidate: %w", cwruntime.ErrDispatch)
							return
						}
					}
				}
				switch slot {
				case CanonWireSlotJsonString:
					{ // section string
						at := inst.dml.GetSectionString().BeginAttribute(dv00)
						for _, m := range inst.view.Groups[0] {
							switch m.Channel {
							case mappingplan.MembershipChannelMixedLowCardVerbatim:
								at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
							default:
								err = eb.Build().Str("section", "string").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
								return
							}
						}
						at.EndAttributeP()
					}
				case CanonWireSlotJsonSymbol:
					{ // section symbol
						at := inst.dml.GetSectionSymbol().BeginAttribute(dv00)
						for _, m := range inst.view.Groups[0] {
							switch m.Channel {
							case mappingplan.MembershipChannelMixedLowCardVerbatim:
								at.AddMembershipMixedLowCardVerbatimP(m.Verbatim, m.Params)
							default:
								err = eb.Build().Str("section", "symbol").Uint8("channel", uint8(m.Channel)).Errorf("the section does not accept the membership channel: %w", cwruntime.ErrChannelNotAccepted)
								return
							}
						}
						at.EndAttributeP()
					}
				default:
					err = eb.Build().Str("signature", "s").Int("attrIdx", a).Stringer("slot", slot).Errorf("the chosen slot does not carry the signature: %w", cwruntime.ErrDispatch)
					return
				}
			}
		default:
			err = eb.Build().Bytes("signature", sig).Errorf("the table has no slot for this canonical type signature: %w", cwruntime.ErrUnknownSlot)
			return
		}
	}
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "json").Errorf("unable to read the entity's tagged slots: %w", err)
		return
	}
	er.End()
	if err = r.Err(); err != nil {
		err = eb.Build().Str("tableName", "json").Errorf("unable to close the entity: %w", err)
		return
	}
	return
}
