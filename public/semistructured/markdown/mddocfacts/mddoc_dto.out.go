// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package mddocfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindMddocKind        uint64 = 5764607535919136768
	kindMddocTitle       uint64 = 5764607535919136769
	kindMddocFileName    uint64 = 5764607535919136770
	kindMddocContent     uint64 = 5764607535919136771
	kindMddocContentHash uint64 = 5764607535919136772
	kindMddocWords       uint64 = 5764607535919136773
)

// mdDocSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdDocSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdDocSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdDocSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// mdDocStringArrayAttrI is the InAttr-side view of the stringArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdDocStringArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdDocStringArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdDocStringArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// mdDocTextArrayAttrI is the InAttr-side view of the textArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdDocTextArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdDocTextArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdDocTextArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// mdDocU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdDocU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdDocU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdDocU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// mdDocEntityI is the entity-builder surface mdDocAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type mdDocEntityI[
	SymbolAttr mdDocSymbolAttrI,
	SymbolSec mdDocSymbolSecI[SymbolAttr, Ent],
	StringArrayAttr mdDocStringArrayAttrI,
	StringArraySec mdDocStringArraySecI[StringArrayAttr, Ent],
	TextArrayAttr mdDocTextArrayAttrI,
	TextArraySec mdDocTextArraySecI[TextArrayAttr, Ent],
	U64ArrayAttr mdDocU64ArrayAttrI,
	U64ArraySec mdDocU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionStringArray() StringArraySec
	GetSectionTextArray() TextArraySec
	GetSectionU64Array() U64ArraySec
}

// mdDocEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdDocEmitSectionSymbol[
	SymbolAttr mdDocSymbolAttrI,
	SymbolSec mdDocSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row MdDoc) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindMddocKind)
	symbolSecAttr_Kind.EndAttributeP()
	return
}

// mdDocEmitSectionStringArray writes this kind's stringArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdDocEmitSectionStringArray[
	StringArrayAttr mdDocStringArrayAttrI,
	StringArraySec mdDocStringArraySecI[StringArrayAttr, Ent],
	Ent any,
](stringArraySec StringArraySec, row MdDoc) (err error) {
	stringArraySecAttr_Title := stringArraySec.BeginAttributeSingle(row.Title)
	stringArraySecAttr_Title.AddMembershipLowCardRefP(kindMddocTitle)
	stringArraySecAttr_Title.EndAttributeP()
	stringArraySecAttr_FileName := stringArraySec.BeginAttributeSingle(row.FileName)
	stringArraySecAttr_FileName.AddMembershipLowCardRefP(kindMddocFileName)
	stringArraySecAttr_FileName.EndAttributeP()
	stringArraySecAttr_ContentHash := stringArraySec.BeginAttributeSingle(row.ContentHash)
	stringArraySecAttr_ContentHash.AddMembershipLowCardRefP(kindMddocContentHash)
	stringArraySecAttr_ContentHash.EndAttributeP()
	return
}

// mdDocEmitSectionTextArray writes this kind's textArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdDocEmitSectionTextArray[
	TextArrayAttr mdDocTextArrayAttrI,
	TextArraySec mdDocTextArraySecI[TextArrayAttr, Ent],
	Ent any,
](textArraySec TextArraySec, row MdDoc) (err error) {
	textArraySecAttr_Content := textArraySec.BeginAttributeSingle(row.Content)
	textArraySecAttr_Content.AddMembershipLowCardRefP(kindMddocContent)
	textArraySecAttr_Content.EndAttributeP()
	return
}

// mdDocEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdDocEmitSectionU64Array[
	U64ArrayAttr mdDocU64ArrayAttrI,
	U64ArraySec mdDocU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row MdDoc) (err error) {
	u64ArraySecAttr_Words := u64ArraySec.BeginAttributeSingle(row.Words)
	u64ArraySecAttr_Words.AddMembershipLowCardRefP(kindMddocWords)
	u64ArraySecAttr_Words.EndAttributeP()
	return
}

// mdDocAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func mdDocAddSections[
	SymbolAttr mdDocSymbolAttrI,
	SymbolSec mdDocSymbolSecI[SymbolAttr, Ent],
	StringArrayAttr mdDocStringArrayAttrI,
	StringArraySec mdDocStringArraySecI[StringArrayAttr, Ent],
	TextArrayAttr mdDocTextArrayAttrI,
	TextArraySec mdDocTextArraySecI[TextArrayAttr, Ent],
	U64ArrayAttr mdDocU64ArrayAttrI,
	U64ArraySec mdDocU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
	DML mdDocEntityI[
		SymbolAttr, SymbolSec,
		StringArrayAttr, StringArraySec,
		TextArrayAttr, TextArraySec,
		U64ArrayAttr, U64ArraySec,
		Ent,
	],
](dml DML, row MdDoc) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = mdDocEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- stringArray. ---
	stringArraySec := dml.GetSectionStringArray()
	err = mdDocEmitSectionStringArray(stringArraySec, row)
	if err != nil {
		return
	}
	stringArraySec.EndSection()
	// --- textArray. ---
	textArraySec := dml.GetSectionTextArray()
	err = mdDocEmitSectionTextArray(textArraySec, row)
	if err != nil {
		return
	}
	textArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = mdDocEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	return
}

// mdDocSymbolAttrsReadI is the Attributes-side view of the symbol section.
type mdDocSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdDocSymbolMembsReadI is the Memberships-side view of the symbol section.
type mdDocSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdDocStringArrayAttrsReadI is the Attributes-side view of the stringArray section.
type mdDocStringArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdDocStringArrayMembsReadI is the Memberships-side view of the stringArray section.
type mdDocStringArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdDocTextArrayAttrsReadI is the Attributes-side view of the textArray section.
type mdDocTextArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdDocTextArrayMembsReadI is the Memberships-side view of the textArray section.
type mdDocTextArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdDocU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type mdDocU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdDocU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type mdDocU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdDocReadRow reads row i as one optional MdDoc component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func mdDocReadRow[
	SymbolAttrs mdDocSymbolAttrsReadI,
	SymbolMembs mdDocSymbolMembsReadI,
	StringArrayAttrs mdDocStringArrayAttrsReadI,
	StringArrayMembs mdDocStringArrayMembsReadI,
	TextArrayAttrs mdDocTextArrayAttrsReadI,
	TextArrayMembs mdDocTextArrayMembsReadI,
	U64ArrayAttrs mdDocU64ArrayAttrsReadI,
	U64ArrayMembs mdDocU64ArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	stringArrayAttrs StringArrayAttrs,
	stringArrayMembs StringArrayMembs,
	textArrayAttrs TextArrayAttrs,
	textArrayMembs TextArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
) (row MdDoc, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMddocKind:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "mddocKind").Int("got", symbolKindCount).Errorf("slot symbol@mddocKind (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	// --- stringArray. ---
	var stringArrayTitleVal string
	var stringArrayTitleCount int
	var stringArrayTitleLastAttr int64
	var stringArrayFileNameVal string
	var stringArrayFileNameCount int
	var stringArrayFileNameLastAttr int64
	var stringArrayContentHashVal string
	var stringArrayContentHashCount int
	var stringArrayContentHashLastAttr int64
	nstringArray := stringArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstringArray; attrJ++ {
		for membID := range stringArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMddocTitle:
				if stringArrayTitleLastAttr != attrJ+1 {
					stringArrayTitleLastAttr = attrJ + 1
					stringArrayTitleCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mddocTitle").Str("field", "Title").Errorf("slot stringArray@mddocTitle (field Title) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayTitleVal = val
			case kindMddocFileName:
				if stringArrayFileNameLastAttr != attrJ+1 {
					stringArrayFileNameLastAttr = attrJ + 1
					stringArrayFileNameCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mddocFileName").Str("field", "FileName").Errorf("slot stringArray@mddocFileName (field FileName) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayFileNameVal = val
			case kindMddocContentHash:
				if stringArrayContentHashLastAttr != attrJ+1 {
					stringArrayContentHashLastAttr = attrJ + 1
					stringArrayContentHashCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mddocContentHash").Str("field", "ContentHash").Errorf("slot stringArray@mddocContentHash (field ContentHash) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayContentHashVal = val
			}
		}
	}
	if stringArrayTitleCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mddocTitle").Int("got", stringArrayTitleCount).Errorf("slot stringArray@mddocTitle (field Title) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayTitleCount)
		return
	}
	if stringArrayTitleCount == 1 {
		row.Title = stringArrayTitleVal
		present = true
	}
	if stringArrayFileNameCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mddocFileName").Int("got", stringArrayFileNameCount).Errorf("slot stringArray@mddocFileName (field FileName) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayFileNameCount)
		return
	}
	if stringArrayFileNameCount == 1 {
		row.FileName = stringArrayFileNameVal
		present = true
	}
	if stringArrayContentHashCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mddocContentHash").Int("got", stringArrayContentHashCount).Errorf("slot stringArray@mddocContentHash (field ContentHash) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayContentHashCount)
		return
	}
	if stringArrayContentHashCount == 1 {
		row.ContentHash = stringArrayContentHashVal
		present = true
	}
	// --- textArray. ---
	var textArrayContentVal string
	var textArrayContentCount int
	var textArrayContentLastAttr int64
	ntextArray := textArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < ntextArray; attrJ++ {
		for membID := range textArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMddocContent:
				if textArrayContentLastAttr != attrJ+1 {
					textArrayContentLastAttr = attrJ + 1
					textArrayContentCount++
				}
				val, valErr := textArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "textArray").Str("membership", "mddocContent").Str("field", "Content").Errorf("slot textArray@mddocContent (field Content) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				textArrayContentVal = val
			}
		}
	}
	if textArrayContentCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "textArray").Str("membership", "mddocContent").Int("got", textArrayContentCount).Errorf("slot textArray@mddocContent (field Content) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", textArrayContentCount)
		return
	}
	if textArrayContentCount == 1 {
		row.Content = textArrayContentVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayWordsVal uint64
	var u64ArrayWordsCount int
	var u64ArrayWordsLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMddocWords:
				if u64ArrayWordsLastAttr != attrJ+1 {
					u64ArrayWordsLastAttr = attrJ + 1
					u64ArrayWordsCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mddocWords").Str("field", "Words").Errorf("slot u64Array@mddocWords (field Words) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayWordsVal = val
			}
		}
	}
	if u64ArrayWordsCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mddocWords").Int("got", u64ArrayWordsCount).Errorf("slot u64Array@mddocWords (field Words) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayWordsCount)
		return
	}
	if u64ArrayWordsCount == 1 {
		row.Words = u64ArrayWordsVal
		present = true
	}
	return
}
