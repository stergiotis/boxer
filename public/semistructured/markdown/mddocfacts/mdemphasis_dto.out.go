// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package mddocfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindMdEmphasisKind    uint64 = 5764607535919136806
	kindMdEmphasisDoc     uint64 = 5764607535919136807
	kindMdEmphasisDocHash uint64 = 5764607535919136808
	kindMdEmphasisOrdinal uint64 = 5764607535919136809
	kindMdEmphasisLine    uint64 = 5764607535919136810
	kindMdEmphasisSection uint64 = 5764607535919136811
	kindMdEmphasisStyle   uint64 = 5764607535919136812
	kindMdEmphasisText    uint64 = 5764607535919136813
)

// mdEmphasisSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdEmphasisSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdEmphasisSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdEmphasisSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// mdEmphasisForeignKeyAttrI is the InAttr-side view of the foreignKey section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdEmphasisForeignKeyAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdEmphasisForeignKeySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdEmphasisForeignKeySecI[Attr any, Ent any] interface {
	BeginAttribute(value uint64) Attr
	EndSection() Ent
}

// mdEmphasisBlobArrayAttrI is the InAttr-side view of the blobArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdEmphasisBlobArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdEmphasisBlobArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdEmphasisBlobArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value []byte) Attr
	EndSection() Ent
}

// mdEmphasisU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdEmphasisU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdEmphasisU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdEmphasisU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// mdEmphasisStringArrayAttrI is the InAttr-side view of the stringArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdEmphasisStringArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdEmphasisStringArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdEmphasisStringArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// mdEmphasisEntityI is the entity-builder surface mdEmphasisAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type mdEmphasisEntityI[
	SymbolAttr mdEmphasisSymbolAttrI,
	SymbolSec mdEmphasisSymbolSecI[SymbolAttr, Ent],
	ForeignKeyAttr mdEmphasisForeignKeyAttrI,
	ForeignKeySec mdEmphasisForeignKeySecI[ForeignKeyAttr, Ent],
	BlobArrayAttr mdEmphasisBlobArrayAttrI,
	BlobArraySec mdEmphasisBlobArraySecI[BlobArrayAttr, Ent],
	U64ArrayAttr mdEmphasisU64ArrayAttrI,
	U64ArraySec mdEmphasisU64ArraySecI[U64ArrayAttr, Ent],
	StringArrayAttr mdEmphasisStringArrayAttrI,
	StringArraySec mdEmphasisStringArraySecI[StringArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionForeignKey() ForeignKeySec
	GetSectionBlobArray() BlobArraySec
	GetSectionU64Array() U64ArraySec
	GetSectionStringArray() StringArraySec
}

// mdEmphasisEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdEmphasisEmitSectionSymbol[
	SymbolAttr mdEmphasisSymbolAttrI,
	SymbolSec mdEmphasisSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row MdEmphasis) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindMdEmphasisKind)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Style := symbolSec.BeginAttribute(row.Style)
	symbolSecAttr_Style.AddMembershipLowCardRefP(kindMdEmphasisStyle)
	symbolSecAttr_Style.EndAttributeP()
	return
}

// mdEmphasisEmitSectionForeignKey writes this kind's foreignKey attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdEmphasisEmitSectionForeignKey[
	ForeignKeyAttr mdEmphasisForeignKeyAttrI,
	ForeignKeySec mdEmphasisForeignKeySecI[ForeignKeyAttr, Ent],
	Ent any,
](foreignKeySec ForeignKeySec, row MdEmphasis) (err error) {
	foreignKeySecAttr_Doc := foreignKeySec.BeginAttribute(row.Doc)
	foreignKeySecAttr_Doc.AddMembershipLowCardRefP(kindMdEmphasisDoc)
	foreignKeySecAttr_Doc.EndAttributeP()
	return
}

// mdEmphasisEmitSectionBlobArray writes this kind's blobArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdEmphasisEmitSectionBlobArray[
	BlobArrayAttr mdEmphasisBlobArrayAttrI,
	BlobArraySec mdEmphasisBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
](blobArraySec BlobArraySec, row MdEmphasis) (err error) {
	blobArraySecAttr_DocHash := blobArraySec.BeginAttributeSingle(row.DocHash)
	blobArraySecAttr_DocHash.AddMembershipLowCardRefP(kindMdEmphasisDocHash)
	blobArraySecAttr_DocHash.EndAttributeP()
	return
}

// mdEmphasisEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdEmphasisEmitSectionU64Array[
	U64ArrayAttr mdEmphasisU64ArrayAttrI,
	U64ArraySec mdEmphasisU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row MdEmphasis) (err error) {
	u64ArraySecAttr_Ordinal := u64ArraySec.BeginAttributeSingle(row.Ordinal)
	u64ArraySecAttr_Ordinal.AddMembershipLowCardRefP(kindMdEmphasisOrdinal)
	u64ArraySecAttr_Ordinal.EndAttributeP()
	u64ArraySecAttr_Line := u64ArraySec.BeginAttributeSingle(row.Line)
	u64ArraySecAttr_Line.AddMembershipLowCardRefP(kindMdEmphasisLine)
	u64ArraySecAttr_Line.EndAttributeP()
	if row.Section.Has {
		u64ArraySecAttr_Section := u64ArraySec.BeginAttributeSingle(row.Section.Val)
		u64ArraySecAttr_Section.AddMembershipLowCardRefP(kindMdEmphasisSection)
		u64ArraySecAttr_Section.EndAttributeP()
	}
	return
}

// mdEmphasisEmitSectionStringArray writes this kind's stringArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdEmphasisEmitSectionStringArray[
	StringArrayAttr mdEmphasisStringArrayAttrI,
	StringArraySec mdEmphasisStringArraySecI[StringArrayAttr, Ent],
	Ent any,
](stringArraySec StringArraySec, row MdEmphasis) (err error) {
	stringArraySecAttr_Text := stringArraySec.BeginAttributeSingle(row.Text)
	stringArraySecAttr_Text.AddMembershipLowCardRefP(kindMdEmphasisText)
	stringArraySecAttr_Text.EndAttributeP()
	return
}

// mdEmphasisAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func mdEmphasisAddSections[
	SymbolAttr mdEmphasisSymbolAttrI,
	SymbolSec mdEmphasisSymbolSecI[SymbolAttr, Ent],
	ForeignKeyAttr mdEmphasisForeignKeyAttrI,
	ForeignKeySec mdEmphasisForeignKeySecI[ForeignKeyAttr, Ent],
	BlobArrayAttr mdEmphasisBlobArrayAttrI,
	BlobArraySec mdEmphasisBlobArraySecI[BlobArrayAttr, Ent],
	U64ArrayAttr mdEmphasisU64ArrayAttrI,
	U64ArraySec mdEmphasisU64ArraySecI[U64ArrayAttr, Ent],
	StringArrayAttr mdEmphasisStringArrayAttrI,
	StringArraySec mdEmphasisStringArraySecI[StringArrayAttr, Ent],
	Ent any,
	DML mdEmphasisEntityI[
		SymbolAttr, SymbolSec,
		ForeignKeyAttr, ForeignKeySec,
		BlobArrayAttr, BlobArraySec,
		U64ArrayAttr, U64ArraySec,
		StringArrayAttr, StringArraySec,
		Ent,
	],
](dml DML, row MdEmphasis) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = mdEmphasisEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- foreignKey. ---
	foreignKeySec := dml.GetSectionForeignKey()
	err = mdEmphasisEmitSectionForeignKey(foreignKeySec, row)
	if err != nil {
		return
	}
	foreignKeySec.EndSection()
	// --- blobArray. ---
	blobArraySec := dml.GetSectionBlobArray()
	err = mdEmphasisEmitSectionBlobArray(blobArraySec, row)
	if err != nil {
		return
	}
	blobArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = mdEmphasisEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	// --- stringArray. ---
	stringArraySec := dml.GetSectionStringArray()
	err = mdEmphasisEmitSectionStringArray(stringArraySec, row)
	if err != nil {
		return
	}
	stringArraySec.EndSection()
	return
}

// mdEmphasisSymbolAttrsReadI is the Attributes-side view of the symbol section.
type mdEmphasisSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdEmphasisSymbolMembsReadI is the Memberships-side view of the symbol section.
type mdEmphasisSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdEmphasisForeignKeyAttrsReadI is the Attributes-side view of the foreignKey section.
type mdEmphasisForeignKeyAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) uint64
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdEmphasisForeignKeyMembsReadI is the Memberships-side view of the foreignKey section.
type mdEmphasisForeignKeyMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdEmphasisBlobArrayAttrsReadI is the Attributes-side view of the blobArray section.
type mdEmphasisBlobArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) ([]byte, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdEmphasisBlobArrayMembsReadI is the Memberships-side view of the blobArray section.
type mdEmphasisBlobArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdEmphasisU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type mdEmphasisU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdEmphasisU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type mdEmphasisU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdEmphasisStringArrayAttrsReadI is the Attributes-side view of the stringArray section.
type mdEmphasisStringArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdEmphasisStringArrayMembsReadI is the Memberships-side view of the stringArray section.
type mdEmphasisStringArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdEmphasisReadRow reads row i as one optional MdEmphasis component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func mdEmphasisReadRow[
	SymbolAttrs mdEmphasisSymbolAttrsReadI,
	SymbolMembs mdEmphasisSymbolMembsReadI,
	ForeignKeyAttrs mdEmphasisForeignKeyAttrsReadI,
	ForeignKeyMembs mdEmphasisForeignKeyMembsReadI,
	BlobArrayAttrs mdEmphasisBlobArrayAttrsReadI,
	BlobArrayMembs mdEmphasisBlobArrayMembsReadI,
	U64ArrayAttrs mdEmphasisU64ArrayAttrsReadI,
	U64ArrayMembs mdEmphasisU64ArrayMembsReadI,
	StringArrayAttrs mdEmphasisStringArrayAttrsReadI,
	StringArrayMembs mdEmphasisStringArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	foreignKeyAttrs ForeignKeyAttrs,
	foreignKeyMembs ForeignKeyMembs,
	blobArrayAttrs BlobArrayAttrs,
	blobArrayMembs BlobArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
	stringArrayAttrs StringArrayAttrs,
	stringArrayMembs StringArrayMembs,
) (row MdEmphasis, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolStyleVal string
	var symbolStyleCount int
	var symbolStyleLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdEmphasisKind:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindMdEmphasisStyle:
				if symbolStyleLastAttr != attrJ+1 {
					symbolStyleLastAttr = attrJ + 1
					symbolStyleCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolStyleVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "mdEmphasisKind").Int("got", symbolKindCount).Errorf("slot symbol@mdEmphasisKind (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolStyleCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "mdEmphasisStyle").Int("got", symbolStyleCount).Errorf("slot symbol@mdEmphasisStyle (field Style) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolStyleCount)
		return
	}
	if symbolStyleCount == 1 {
		row.Style = symbolStyleVal
		present = true
	}
	// --- foreignKey. ---
	var foreignKeyDocVal uint64
	var foreignKeyDocCount int
	var foreignKeyDocLastAttr int64
	nforeignKey := foreignKeyAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nforeignKey; attrJ++ {
		for membID := range foreignKeyMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdEmphasisDoc:
				if foreignKeyDocLastAttr != attrJ+1 {
					foreignKeyDocLastAttr = attrJ + 1
					foreignKeyDocCount++
				}
				val := foreignKeyAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				foreignKeyDocVal = val
			}
		}
	}
	if foreignKeyDocCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "foreignKey").Str("membership", "mdEmphasisDoc").Int("got", foreignKeyDocCount).Errorf("slot foreignKey@mdEmphasisDoc (field Doc) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", foreignKeyDocCount)
		return
	}
	if foreignKeyDocCount == 1 {
		row.Doc = foreignKeyDocVal
		present = true
	}
	// --- blobArray. ---
	var blobArrayDocHashVal []byte
	var blobArrayDocHashCount int
	var blobArrayDocHashLastAttr int64
	nblobArray := blobArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nblobArray; attrJ++ {
		for membID := range blobArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdEmphasisDocHash:
				if blobArrayDocHashLastAttr != attrJ+1 {
					blobArrayDocHashLastAttr = attrJ + 1
					blobArrayDocHashCount++
				}
				val, valErr := blobArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "mdEmphasisDocHash").Str("field", "DocHash").Errorf("slot blobArray@mdEmphasisDocHash (field DocHash) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				cp := make([]byte, len(val))
				copy(cp, val)
				blobArrayDocHashVal = cp
			}
		}
	}
	if blobArrayDocHashCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "mdEmphasisDocHash").Int("got", blobArrayDocHashCount).Errorf("slot blobArray@mdEmphasisDocHash (field DocHash) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobArrayDocHashCount)
		return
	}
	if blobArrayDocHashCount == 1 {
		row.DocHash = blobArrayDocHashVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayOrdinalVal uint64
	var u64ArrayOrdinalCount int
	var u64ArrayOrdinalLastAttr int64
	var u64ArrayLineVal uint64
	var u64ArrayLineCount int
	var u64ArrayLineLastAttr int64
	var u64ArraySectionVal uint64
	var u64ArraySectionCount int
	var u64ArraySectionLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdEmphasisOrdinal:
				if u64ArrayOrdinalLastAttr != attrJ+1 {
					u64ArrayOrdinalLastAttr = attrJ + 1
					u64ArrayOrdinalCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdEmphasisOrdinal").Str("field", "Ordinal").Errorf("slot u64Array@mdEmphasisOrdinal (field Ordinal) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayOrdinalVal = val
			case kindMdEmphasisLine:
				if u64ArrayLineLastAttr != attrJ+1 {
					u64ArrayLineLastAttr = attrJ + 1
					u64ArrayLineCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdEmphasisLine").Str("field", "Line").Errorf("slot u64Array@mdEmphasisLine (field Line) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayLineVal = val
			case kindMdEmphasisSection:
				if u64ArraySectionLastAttr != attrJ+1 {
					u64ArraySectionLastAttr = attrJ + 1
					u64ArraySectionCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdEmphasisSection").Str("field", "Section").Errorf("slot u64Array@mdEmphasisSection (field Section) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArraySectionVal = val
			}
		}
	}
	if u64ArrayOrdinalCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdEmphasisOrdinal").Int("got", u64ArrayOrdinalCount).Errorf("slot u64Array@mdEmphasisOrdinal (field Ordinal) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayOrdinalCount)
		return
	}
	if u64ArrayOrdinalCount == 1 {
		row.Ordinal = u64ArrayOrdinalVal
		present = true
	}
	if u64ArrayLineCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdEmphasisLine").Int("got", u64ArrayLineCount).Errorf("slot u64Array@mdEmphasisLine (field Line) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayLineCount)
		return
	}
	if u64ArrayLineCount == 1 {
		row.Line = u64ArrayLineVal
		present = true
	}
	if u64ArraySectionCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdEmphasisSection").Int("got", u64ArraySectionCount).Errorf("slot u64Array@mdEmphasisSection (field Section) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArraySectionCount)
		return
	}
	if u64ArraySectionCount == 1 {
		row.Section.Val = u64ArraySectionVal
		row.Section.Has = true
		present = true
	}
	// --- stringArray. ---
	var stringArrayTextVal string
	var stringArrayTextCount int
	var stringArrayTextLastAttr int64
	nstringArray := stringArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstringArray; attrJ++ {
		for membID := range stringArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdEmphasisText:
				if stringArrayTextLastAttr != attrJ+1 {
					stringArrayTextLastAttr = attrJ + 1
					stringArrayTextCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdEmphasisText").Str("field", "Text").Errorf("slot stringArray@mdEmphasisText (field Text) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayTextVal = val
			}
		}
	}
	if stringArrayTextCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdEmphasisText").Int("got", stringArrayTextCount).Errorf("slot stringArray@mdEmphasisText (field Text) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayTextCount)
		return
	}
	if stringArrayTextCount == 1 {
		row.Text = stringArrayTextVal
		present = true
	}
	return
}
