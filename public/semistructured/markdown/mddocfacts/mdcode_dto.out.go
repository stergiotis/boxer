// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package mddocfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindMdCodeKind     uint64 = 5764607535919136785
	kindMdCodeDoc      uint64 = 5764607535919136786
	kindMdCodeDocHash  uint64 = 5764607535919136787
	kindMdCodeOrdinal  uint64 = 5764607535919136788
	kindMdCodeLine     uint64 = 5764607535919136789
	kindMdCodeSection  uint64 = 5764607535919136790
	kindMdCodeLanguage uint64 = 5764607535919136791
	kindMdCodeInfo     uint64 = 5764607535919136792
	kindMdCodeContent  uint64 = 5764607535919136793
	kindMdCodeLines    uint64 = 5764607535919136794
)

// mdCodeBlockSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdCodeBlockSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdCodeBlockSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdCodeBlockSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// mdCodeBlockForeignKeyAttrI is the InAttr-side view of the foreignKey section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdCodeBlockForeignKeyAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdCodeBlockForeignKeySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdCodeBlockForeignKeySecI[Attr any, Ent any] interface {
	BeginAttribute(value uint64) Attr
	EndSection() Ent
}

// mdCodeBlockBlobArrayAttrI is the InAttr-side view of the blobArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdCodeBlockBlobArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdCodeBlockBlobArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdCodeBlockBlobArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value []byte) Attr
	EndSection() Ent
}

// mdCodeBlockU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdCodeBlockU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdCodeBlockU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdCodeBlockU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// mdCodeBlockStringArrayAttrI is the InAttr-side view of the stringArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdCodeBlockStringArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdCodeBlockStringArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdCodeBlockStringArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// mdCodeBlockTextArrayAttrI is the InAttr-side view of the textArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdCodeBlockTextArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdCodeBlockTextArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdCodeBlockTextArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// mdCodeBlockEntityI is the entity-builder surface mdCodeBlockAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type mdCodeBlockEntityI[
	SymbolAttr mdCodeBlockSymbolAttrI,
	SymbolSec mdCodeBlockSymbolSecI[SymbolAttr, Ent],
	ForeignKeyAttr mdCodeBlockForeignKeyAttrI,
	ForeignKeySec mdCodeBlockForeignKeySecI[ForeignKeyAttr, Ent],
	BlobArrayAttr mdCodeBlockBlobArrayAttrI,
	BlobArraySec mdCodeBlockBlobArraySecI[BlobArrayAttr, Ent],
	U64ArrayAttr mdCodeBlockU64ArrayAttrI,
	U64ArraySec mdCodeBlockU64ArraySecI[U64ArrayAttr, Ent],
	StringArrayAttr mdCodeBlockStringArrayAttrI,
	StringArraySec mdCodeBlockStringArraySecI[StringArrayAttr, Ent],
	TextArrayAttr mdCodeBlockTextArrayAttrI,
	TextArraySec mdCodeBlockTextArraySecI[TextArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionForeignKey() ForeignKeySec
	GetSectionBlobArray() BlobArraySec
	GetSectionU64Array() U64ArraySec
	GetSectionStringArray() StringArraySec
	GetSectionTextArray() TextArraySec
}

// mdCodeBlockEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdCodeBlockEmitSectionSymbol[
	SymbolAttr mdCodeBlockSymbolAttrI,
	SymbolSec mdCodeBlockSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row MdCodeBlock) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindMdCodeKind)
	symbolSecAttr_Kind.EndAttributeP()
	symbolSecAttr_Language := symbolSec.BeginAttribute(row.Language)
	symbolSecAttr_Language.AddMembershipLowCardRefP(kindMdCodeLanguage)
	symbolSecAttr_Language.EndAttributeP()
	return
}

// mdCodeBlockEmitSectionForeignKey writes this kind's foreignKey attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdCodeBlockEmitSectionForeignKey[
	ForeignKeyAttr mdCodeBlockForeignKeyAttrI,
	ForeignKeySec mdCodeBlockForeignKeySecI[ForeignKeyAttr, Ent],
	Ent any,
](foreignKeySec ForeignKeySec, row MdCodeBlock) (err error) {
	foreignKeySecAttr_Doc := foreignKeySec.BeginAttribute(row.Doc)
	foreignKeySecAttr_Doc.AddMembershipLowCardRefP(kindMdCodeDoc)
	foreignKeySecAttr_Doc.EndAttributeP()
	return
}

// mdCodeBlockEmitSectionBlobArray writes this kind's blobArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdCodeBlockEmitSectionBlobArray[
	BlobArrayAttr mdCodeBlockBlobArrayAttrI,
	BlobArraySec mdCodeBlockBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
](blobArraySec BlobArraySec, row MdCodeBlock) (err error) {
	blobArraySecAttr_DocHash := blobArraySec.BeginAttributeSingle(row.DocHash)
	blobArraySecAttr_DocHash.AddMembershipLowCardRefP(kindMdCodeDocHash)
	blobArraySecAttr_DocHash.EndAttributeP()
	return
}

// mdCodeBlockEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdCodeBlockEmitSectionU64Array[
	U64ArrayAttr mdCodeBlockU64ArrayAttrI,
	U64ArraySec mdCodeBlockU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row MdCodeBlock) (err error) {
	u64ArraySecAttr_Ordinal := u64ArraySec.BeginAttributeSingle(row.Ordinal)
	u64ArraySecAttr_Ordinal.AddMembershipLowCardRefP(kindMdCodeOrdinal)
	u64ArraySecAttr_Ordinal.EndAttributeP()
	u64ArraySecAttr_Line := u64ArraySec.BeginAttributeSingle(row.Line)
	u64ArraySecAttr_Line.AddMembershipLowCardRefP(kindMdCodeLine)
	u64ArraySecAttr_Line.EndAttributeP()
	if row.Section.Has {
		u64ArraySecAttr_Section := u64ArraySec.BeginAttributeSingle(row.Section.Val)
		u64ArraySecAttr_Section.AddMembershipLowCardRefP(kindMdCodeSection)
		u64ArraySecAttr_Section.EndAttributeP()
	}
	u64ArraySecAttr_Lines := u64ArraySec.BeginAttributeSingle(row.Lines)
	u64ArraySecAttr_Lines.AddMembershipLowCardRefP(kindMdCodeLines)
	u64ArraySecAttr_Lines.EndAttributeP()
	return
}

// mdCodeBlockEmitSectionStringArray writes this kind's stringArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdCodeBlockEmitSectionStringArray[
	StringArrayAttr mdCodeBlockStringArrayAttrI,
	StringArraySec mdCodeBlockStringArraySecI[StringArrayAttr, Ent],
	Ent any,
](stringArraySec StringArraySec, row MdCodeBlock) (err error) {
	stringArraySecAttr_Info := stringArraySec.BeginAttributeSingle(row.Info)
	stringArraySecAttr_Info.AddMembershipLowCardRefP(kindMdCodeInfo)
	stringArraySecAttr_Info.EndAttributeP()
	return
}

// mdCodeBlockEmitSectionTextArray writes this kind's textArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdCodeBlockEmitSectionTextArray[
	TextArrayAttr mdCodeBlockTextArrayAttrI,
	TextArraySec mdCodeBlockTextArraySecI[TextArrayAttr, Ent],
	Ent any,
](textArraySec TextArraySec, row MdCodeBlock) (err error) {
	textArraySecAttr_Content := textArraySec.BeginAttributeSingle(row.Content)
	textArraySecAttr_Content.AddMembershipLowCardRefP(kindMdCodeContent)
	textArraySecAttr_Content.EndAttributeP()
	return
}

// mdCodeBlockAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func mdCodeBlockAddSections[
	SymbolAttr mdCodeBlockSymbolAttrI,
	SymbolSec mdCodeBlockSymbolSecI[SymbolAttr, Ent],
	ForeignKeyAttr mdCodeBlockForeignKeyAttrI,
	ForeignKeySec mdCodeBlockForeignKeySecI[ForeignKeyAttr, Ent],
	BlobArrayAttr mdCodeBlockBlobArrayAttrI,
	BlobArraySec mdCodeBlockBlobArraySecI[BlobArrayAttr, Ent],
	U64ArrayAttr mdCodeBlockU64ArrayAttrI,
	U64ArraySec mdCodeBlockU64ArraySecI[U64ArrayAttr, Ent],
	StringArrayAttr mdCodeBlockStringArrayAttrI,
	StringArraySec mdCodeBlockStringArraySecI[StringArrayAttr, Ent],
	TextArrayAttr mdCodeBlockTextArrayAttrI,
	TextArraySec mdCodeBlockTextArraySecI[TextArrayAttr, Ent],
	Ent any,
	DML mdCodeBlockEntityI[
		SymbolAttr, SymbolSec,
		ForeignKeyAttr, ForeignKeySec,
		BlobArrayAttr, BlobArraySec,
		U64ArrayAttr, U64ArraySec,
		StringArrayAttr, StringArraySec,
		TextArrayAttr, TextArraySec,
		Ent,
	],
](dml DML, row MdCodeBlock) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = mdCodeBlockEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- foreignKey. ---
	foreignKeySec := dml.GetSectionForeignKey()
	err = mdCodeBlockEmitSectionForeignKey(foreignKeySec, row)
	if err != nil {
		return
	}
	foreignKeySec.EndSection()
	// --- blobArray. ---
	blobArraySec := dml.GetSectionBlobArray()
	err = mdCodeBlockEmitSectionBlobArray(blobArraySec, row)
	if err != nil {
		return
	}
	blobArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = mdCodeBlockEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	// --- stringArray. ---
	stringArraySec := dml.GetSectionStringArray()
	err = mdCodeBlockEmitSectionStringArray(stringArraySec, row)
	if err != nil {
		return
	}
	stringArraySec.EndSection()
	// --- textArray. ---
	textArraySec := dml.GetSectionTextArray()
	err = mdCodeBlockEmitSectionTextArray(textArraySec, row)
	if err != nil {
		return
	}
	textArraySec.EndSection()
	return
}

// mdCodeBlockSymbolAttrsReadI is the Attributes-side view of the symbol section.
type mdCodeBlockSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdCodeBlockSymbolMembsReadI is the Memberships-side view of the symbol section.
type mdCodeBlockSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdCodeBlockForeignKeyAttrsReadI is the Attributes-side view of the foreignKey section.
type mdCodeBlockForeignKeyAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) uint64
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdCodeBlockForeignKeyMembsReadI is the Memberships-side view of the foreignKey section.
type mdCodeBlockForeignKeyMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdCodeBlockBlobArrayAttrsReadI is the Attributes-side view of the blobArray section.
type mdCodeBlockBlobArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) ([]byte, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdCodeBlockBlobArrayMembsReadI is the Memberships-side view of the blobArray section.
type mdCodeBlockBlobArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdCodeBlockU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type mdCodeBlockU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdCodeBlockU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type mdCodeBlockU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdCodeBlockStringArrayAttrsReadI is the Attributes-side view of the stringArray section.
type mdCodeBlockStringArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdCodeBlockStringArrayMembsReadI is the Memberships-side view of the stringArray section.
type mdCodeBlockStringArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdCodeBlockTextArrayAttrsReadI is the Attributes-side view of the textArray section.
type mdCodeBlockTextArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdCodeBlockTextArrayMembsReadI is the Memberships-side view of the textArray section.
type mdCodeBlockTextArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdCodeBlockReadRow reads row i as one optional MdCodeBlock component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func mdCodeBlockReadRow[
	SymbolAttrs mdCodeBlockSymbolAttrsReadI,
	SymbolMembs mdCodeBlockSymbolMembsReadI,
	ForeignKeyAttrs mdCodeBlockForeignKeyAttrsReadI,
	ForeignKeyMembs mdCodeBlockForeignKeyMembsReadI,
	BlobArrayAttrs mdCodeBlockBlobArrayAttrsReadI,
	BlobArrayMembs mdCodeBlockBlobArrayMembsReadI,
	U64ArrayAttrs mdCodeBlockU64ArrayAttrsReadI,
	U64ArrayMembs mdCodeBlockU64ArrayMembsReadI,
	StringArrayAttrs mdCodeBlockStringArrayAttrsReadI,
	StringArrayMembs mdCodeBlockStringArrayMembsReadI,
	TextArrayAttrs mdCodeBlockTextArrayAttrsReadI,
	TextArrayMembs mdCodeBlockTextArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	foreignKeyAttrs ForeignKeyAttrs,
	foreignKeyMembs ForeignKeyMembs,
	blobArrayAttrs BlobArrayAttrs,
	blobArrayMembs BlobArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
	stringArrayAttrs StringArrayAttrs,
	stringArrayMembs StringArrayMembs,
	textArrayAttrs TextArrayAttrs,
	textArrayMembs TextArrayMembs,
) (row MdCodeBlock, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	var symbolLanguageVal string
	var symbolLanguageCount int
	var symbolLanguageLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdCodeKind:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			case kindMdCodeLanguage:
				if symbolLanguageLastAttr != attrJ+1 {
					symbolLanguageLastAttr = attrJ + 1
					symbolLanguageCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolLanguageVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "mdCodeKind").Int("got", symbolKindCount).Errorf("slot symbol@mdCodeKind (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	if symbolLanguageCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "mdCodeLanguage").Int("got", symbolLanguageCount).Errorf("slot symbol@mdCodeLanguage (field Language) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolLanguageCount)
		return
	}
	if symbolLanguageCount == 1 {
		row.Language = symbolLanguageVal
		present = true
	}
	// --- foreignKey. ---
	var foreignKeyDocVal uint64
	var foreignKeyDocCount int
	var foreignKeyDocLastAttr int64
	nforeignKey := foreignKeyAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nforeignKey; attrJ++ {
		for membID := range foreignKeyMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdCodeDoc:
				if foreignKeyDocLastAttr != attrJ+1 {
					foreignKeyDocLastAttr = attrJ + 1
					foreignKeyDocCount++
				}
				val := foreignKeyAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				foreignKeyDocVal = val
			}
		}
	}
	if foreignKeyDocCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "foreignKey").Str("membership", "mdCodeDoc").Int("got", foreignKeyDocCount).Errorf("slot foreignKey@mdCodeDoc (field Doc) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", foreignKeyDocCount)
		return
	}
	if foreignKeyDocCount == 1 {
		row.Doc = foreignKeyDocVal
		present = true
	}
	// --- blobArray. ---
	var blobArrayDocHashVal []byte
	var blobArrayDocHashCount int
	var blobArrayDocHashLastAttr int64
	nblobArray := blobArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nblobArray; attrJ++ {
		for membID := range blobArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdCodeDocHash:
				if blobArrayDocHashLastAttr != attrJ+1 {
					blobArrayDocHashLastAttr = attrJ + 1
					blobArrayDocHashCount++
				}
				val, valErr := blobArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "mdCodeDocHash").Str("field", "DocHash").Errorf("slot blobArray@mdCodeDocHash (field DocHash) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				cp := make([]byte, len(val))
				copy(cp, val)
				blobArrayDocHashVal = cp
			}
		}
	}
	if blobArrayDocHashCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "mdCodeDocHash").Int("got", blobArrayDocHashCount).Errorf("slot blobArray@mdCodeDocHash (field DocHash) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobArrayDocHashCount)
		return
	}
	if blobArrayDocHashCount == 1 {
		row.DocHash = blobArrayDocHashVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayOrdinalVal uint64
	var u64ArrayOrdinalCount int
	var u64ArrayOrdinalLastAttr int64
	var u64ArrayLineVal uint64
	var u64ArrayLineCount int
	var u64ArrayLineLastAttr int64
	var u64ArraySectionVal uint64
	var u64ArraySectionCount int
	var u64ArraySectionLastAttr int64
	var u64ArrayLinesVal uint64
	var u64ArrayLinesCount int
	var u64ArrayLinesLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdCodeOrdinal:
				if u64ArrayOrdinalLastAttr != attrJ+1 {
					u64ArrayOrdinalLastAttr = attrJ + 1
					u64ArrayOrdinalCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeOrdinal").Str("field", "Ordinal").Errorf("slot u64Array@mdCodeOrdinal (field Ordinal) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayOrdinalVal = val
			case kindMdCodeLine:
				if u64ArrayLineLastAttr != attrJ+1 {
					u64ArrayLineLastAttr = attrJ + 1
					u64ArrayLineCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeLine").Str("field", "Line").Errorf("slot u64Array@mdCodeLine (field Line) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayLineVal = val
			case kindMdCodeSection:
				if u64ArraySectionLastAttr != attrJ+1 {
					u64ArraySectionLastAttr = attrJ + 1
					u64ArraySectionCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeSection").Str("field", "Section").Errorf("slot u64Array@mdCodeSection (field Section) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArraySectionVal = val
			case kindMdCodeLines:
				if u64ArrayLinesLastAttr != attrJ+1 {
					u64ArrayLinesLastAttr = attrJ + 1
					u64ArrayLinesCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeLines").Str("field", "Lines").Errorf("slot u64Array@mdCodeLines (field Lines) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayLinesVal = val
			}
		}
	}
	if u64ArrayOrdinalCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeOrdinal").Int("got", u64ArrayOrdinalCount).Errorf("slot u64Array@mdCodeOrdinal (field Ordinal) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayOrdinalCount)
		return
	}
	if u64ArrayOrdinalCount == 1 {
		row.Ordinal = u64ArrayOrdinalVal
		present = true
	}
	if u64ArrayLineCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeLine").Int("got", u64ArrayLineCount).Errorf("slot u64Array@mdCodeLine (field Line) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayLineCount)
		return
	}
	if u64ArrayLineCount == 1 {
		row.Line = u64ArrayLineVal
		present = true
	}
	if u64ArraySectionCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeSection").Int("got", u64ArraySectionCount).Errorf("slot u64Array@mdCodeSection (field Section) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArraySectionCount)
		return
	}
	if u64ArraySectionCount == 1 {
		row.Section.Val = u64ArraySectionVal
		row.Section.Has = true
		present = true
	}
	if u64ArrayLinesCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdCodeLines").Int("got", u64ArrayLinesCount).Errorf("slot u64Array@mdCodeLines (field Lines) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayLinesCount)
		return
	}
	if u64ArrayLinesCount == 1 {
		row.Lines = u64ArrayLinesVal
		present = true
	}
	// --- stringArray. ---
	var stringArrayInfoVal string
	var stringArrayInfoCount int
	var stringArrayInfoLastAttr int64
	nstringArray := stringArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstringArray; attrJ++ {
		for membID := range stringArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdCodeInfo:
				if stringArrayInfoLastAttr != attrJ+1 {
					stringArrayInfoLastAttr = attrJ + 1
					stringArrayInfoCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdCodeInfo").Str("field", "Info").Errorf("slot stringArray@mdCodeInfo (field Info) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayInfoVal = val
			}
		}
	}
	if stringArrayInfoCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdCodeInfo").Int("got", stringArrayInfoCount).Errorf("slot stringArray@mdCodeInfo (field Info) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayInfoCount)
		return
	}
	if stringArrayInfoCount == 1 {
		row.Info = stringArrayInfoVal
		present = true
	}
	// --- textArray. ---
	var textArrayContentVal string
	var textArrayContentCount int
	var textArrayContentLastAttr int64
	ntextArray := textArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < ntextArray; attrJ++ {
		for membID := range textArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdCodeContent:
				if textArrayContentLastAttr != attrJ+1 {
					textArrayContentLastAttr = attrJ + 1
					textArrayContentCount++
				}
				val, valErr := textArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "textArray").Str("membership", "mdCodeContent").Str("field", "Content").Errorf("slot textArray@mdCodeContent (field Content) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				textArrayContentVal = val
			}
		}
	}
	if textArrayContentCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "textArray").Str("membership", "mdCodeContent").Int("got", textArrayContentCount).Errorf("slot textArray@mdCodeContent (field Content) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", textArrayContentCount)
		return
	}
	if textArrayContentCount == 1 {
		row.Content = textArrayContentVal
		present = true
	}
	return
}
