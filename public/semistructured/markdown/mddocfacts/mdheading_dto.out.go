// Code generated by boxer/public/semistructured/leeway/marshall/go/marshallgen — DO NOT EDIT.

package mddocfacts

import (
	"iter"

	"github.com/stergiotis/boxer/public/observability/eh/eb"
	dmlruntime "github.com/stergiotis/boxer/public/semistructured/leeway/dml/runtime"
	raruntime "github.com/stergiotis/boxer/public/semistructured/leeway/readaccess/runtime"
)

// --- Caller-assigned membership ids (registry-stable target). ---

const (
	kindMdHeadingKind    uint64 = 5764607535919136774
	kindMdHeadingDoc     uint64 = 5764607535919136775
	kindMdHeadingDocHash uint64 = 5764607535919136776
	kindMdHeadingOrdinal uint64 = 5764607535919136777
	kindMdHeadingLine    uint64 = 5764607535919136778
	kindMdHeadingLevel   uint64 = 5764607535919136779
	kindMdHeadingText    uint64 = 5764607535919136780
	kindMdHeadingSlug    uint64 = 5764607535919136781
	kindMdHeadingAnchor  uint64 = 5764607535919136782
	kindMdHeadingParent  uint64 = 5764607535919136783
	kindMdHeadingPath    uint64 = 5764607535919136784
)

// mdHeadingSymbolAttrI is the InAttr-side view of the symbol section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdHeadingSymbolAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdHeadingSymbolSecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdHeadingSymbolSecI[Attr any, Ent any] interface {
	BeginAttribute(value string) Attr
	EndSection() Ent
}

// mdHeadingForeignKeyAttrI is the InAttr-side view of the foreignKey section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdHeadingForeignKeyAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdHeadingForeignKeySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdHeadingForeignKeySecI[Attr any, Ent any] interface {
	BeginAttribute(value uint64) Attr
	EndSection() Ent
}

// mdHeadingBlobArrayAttrI is the InAttr-side view of the blobArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdHeadingBlobArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdHeadingBlobArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdHeadingBlobArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value []byte) Attr
	EndSection() Ent
}

// mdHeadingU64ArrayAttrI is the InAttr-side view of the u64Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdHeadingU64ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdHeadingU64ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdHeadingU64ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint64) Attr
	EndSection() Ent
}

// mdHeadingU8ArrayAttrI is the InAttr-side view of the u8Array section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdHeadingU8ArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	EndAttributeP()
}

// mdHeadingU8ArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdHeadingU8ArraySecI[Attr any, Ent any] interface {
	BeginAttributeSingle(value uint8) Attr
	EndSection() Ent
}

// mdHeadingStringArrayAttrI is the InAttr-side view of the stringArray section. P-variants only —
// every method returns void so no F-bounded `[Self]` parameter is
// needed.
type mdHeadingStringArrayAttrI interface {
	dmlruntime.InAttributeMembershipLowCardRefPI
	AddToContainerP(value string)
	EndAttributeP()
}

// mdHeadingStringArraySecI is the Section-side view: opens an attribute and closes
// the section. Attr and Ent are bound at the call site by inference.
type mdHeadingStringArraySecI[Attr any, Ent any] interface {
	BeginAttribute() Attr
	BeginAttributeSingle(value string) Attr
	EndSection() Ent
}

// mdHeadingEntityI is the entity-builder surface mdHeadingAddSections drives.
// It always lists the per-section getters; the entity-frame methods
// (BeginEntity / plain setters / CommitEntity) are added only for the
// full codec's BuildEntities. AddSections stacks sections onto a frame
// the caller already owns, so it needs none of them — which lets a
// store drive it with a builder whose frame control is unexported
// (ADR-0100 SD6). Ent is the builder pointer.
type mdHeadingEntityI[
	SymbolAttr mdHeadingSymbolAttrI,
	SymbolSec mdHeadingSymbolSecI[SymbolAttr, Ent],
	ForeignKeyAttr mdHeadingForeignKeyAttrI,
	ForeignKeySec mdHeadingForeignKeySecI[ForeignKeyAttr, Ent],
	BlobArrayAttr mdHeadingBlobArrayAttrI,
	BlobArraySec mdHeadingBlobArraySecI[BlobArrayAttr, Ent],
	U64ArrayAttr mdHeadingU64ArrayAttrI,
	U64ArraySec mdHeadingU64ArraySecI[U64ArrayAttr, Ent],
	U8ArrayAttr mdHeadingU8ArrayAttrI,
	U8ArraySec mdHeadingU8ArraySecI[U8ArrayAttr, Ent],
	StringArrayAttr mdHeadingStringArrayAttrI,
	StringArraySec mdHeadingStringArraySecI[StringArrayAttr, Ent],
	Ent any,
] interface {
	GetSectionSymbol() SymbolSec
	GetSectionForeignKey() ForeignKeySec
	GetSectionBlobArray() BlobArraySec
	GetSectionU64Array() U64ArraySec
	GetSectionU8Array() U8ArraySec
	GetSectionStringArray() StringArraySec
}

// mdHeadingEmitSectionSymbol writes this kind's symbol attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdHeadingEmitSectionSymbol[
	SymbolAttr mdHeadingSymbolAttrI,
	SymbolSec mdHeadingSymbolSecI[SymbolAttr, Ent],
	Ent any,
](symbolSec SymbolSec, row MdHeading) (err error) {
	symbolSecAttr_Kind := symbolSec.BeginAttribute(row.Kind)
	symbolSecAttr_Kind.AddMembershipLowCardRefP(kindMdHeadingKind)
	symbolSecAttr_Kind.EndAttributeP()
	return
}

// mdHeadingEmitSectionForeignKey writes this kind's foreignKey attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdHeadingEmitSectionForeignKey[
	ForeignKeyAttr mdHeadingForeignKeyAttrI,
	ForeignKeySec mdHeadingForeignKeySecI[ForeignKeyAttr, Ent],
	Ent any,
](foreignKeySec ForeignKeySec, row MdHeading) (err error) {
	foreignKeySecAttr_Doc := foreignKeySec.BeginAttribute(row.Doc)
	foreignKeySecAttr_Doc.AddMembershipLowCardRefP(kindMdHeadingDoc)
	foreignKeySecAttr_Doc.EndAttributeP()
	return
}

// mdHeadingEmitSectionBlobArray writes this kind's blobArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdHeadingEmitSectionBlobArray[
	BlobArrayAttr mdHeadingBlobArrayAttrI,
	BlobArraySec mdHeadingBlobArraySecI[BlobArrayAttr, Ent],
	Ent any,
](blobArraySec BlobArraySec, row MdHeading) (err error) {
	blobArraySecAttr_DocHash := blobArraySec.BeginAttributeSingle(row.DocHash)
	blobArraySecAttr_DocHash.AddMembershipLowCardRefP(kindMdHeadingDocHash)
	blobArraySecAttr_DocHash.EndAttributeP()
	return
}

// mdHeadingEmitSectionU64Array writes this kind's u64Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdHeadingEmitSectionU64Array[
	U64ArrayAttr mdHeadingU64ArrayAttrI,
	U64ArraySec mdHeadingU64ArraySecI[U64ArrayAttr, Ent],
	Ent any,
](u64ArraySec U64ArraySec, row MdHeading) (err error) {
	u64ArraySecAttr_Ordinal := u64ArraySec.BeginAttributeSingle(row.Ordinal)
	u64ArraySecAttr_Ordinal.AddMembershipLowCardRefP(kindMdHeadingOrdinal)
	u64ArraySecAttr_Ordinal.EndAttributeP()
	u64ArraySecAttr_Line := u64ArraySec.BeginAttributeSingle(row.Line)
	u64ArraySecAttr_Line.AddMembershipLowCardRefP(kindMdHeadingLine)
	u64ArraySecAttr_Line.EndAttributeP()
	if row.Parent.Has {
		u64ArraySecAttr_Parent := u64ArraySec.BeginAttributeSingle(row.Parent.Val)
		u64ArraySecAttr_Parent.AddMembershipLowCardRefP(kindMdHeadingParent)
		u64ArraySecAttr_Parent.EndAttributeP()
	}
	return
}

// mdHeadingEmitSectionU8Array writes this kind's u8Array attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdHeadingEmitSectionU8Array[
	U8ArrayAttr mdHeadingU8ArrayAttrI,
	U8ArraySec mdHeadingU8ArraySecI[U8ArrayAttr, Ent],
	Ent any,
](u8ArraySec U8ArraySec, row MdHeading) (err error) {
	u8ArraySecAttr_Level := u8ArraySec.BeginAttributeSingle(row.Level)
	u8ArraySecAttr_Level.AddMembershipLowCardRefP(kindMdHeadingLevel)
	u8ArraySecAttr_Level.EndAttributeP()
	return
}

// mdHeadingEmitSectionStringArray writes this kind's stringArray attributes into an
// ALREADY-OPEN section frame, and does not close it. The caller owns
// the frame: one kind's AddSections, or a builder deferring the close
// until every component that shares the section has written.
func mdHeadingEmitSectionStringArray[
	StringArrayAttr mdHeadingStringArrayAttrI,
	StringArraySec mdHeadingStringArraySecI[StringArrayAttr, Ent],
	Ent any,
](stringArraySec StringArraySec, row MdHeading) (err error) {
	stringArraySecAttr_Text := stringArraySec.BeginAttributeSingle(row.Text)
	stringArraySecAttr_Text.AddMembershipLowCardRefP(kindMdHeadingText)
	stringArraySecAttr_Text.EndAttributeP()
	stringArraySecAttr_Slug := stringArraySec.BeginAttributeSingle(row.Slug)
	stringArraySecAttr_Slug.AddMembershipLowCardRefP(kindMdHeadingSlug)
	stringArraySecAttr_Slug.EndAttributeP()
	if row.Anchor.Has {
		stringArraySecAttr_Anchor := stringArraySec.BeginAttributeSingle(row.Anchor.Val)
		stringArraySecAttr_Anchor.AddMembershipLowCardRefP(kindMdHeadingAnchor)
		stringArraySecAttr_Anchor.EndAttributeP()
	}
	if len(row.Path) > 0 {
		stringArraySecAttr_Path := stringArraySec.BeginAttribute()
		for _, v := range row.Path {
			stringArraySecAttr_Path.AddToContainerP(v)
		}
		stringArraySecAttr_Path.AddMembershipLowCardRefP(kindMdHeadingPath)
		stringArraySecAttr_Path.EndAttributeP()
	}
	return
}

// mdHeadingAddSections contributes this kind's tagged sections to the OPEN
// entity on dml — the BuildEntities body without the entity frame.
// The caller owns BeginEntity / plain setters / CommitEntity.
func mdHeadingAddSections[
	SymbolAttr mdHeadingSymbolAttrI,
	SymbolSec mdHeadingSymbolSecI[SymbolAttr, Ent],
	ForeignKeyAttr mdHeadingForeignKeyAttrI,
	ForeignKeySec mdHeadingForeignKeySecI[ForeignKeyAttr, Ent],
	BlobArrayAttr mdHeadingBlobArrayAttrI,
	BlobArraySec mdHeadingBlobArraySecI[BlobArrayAttr, Ent],
	U64ArrayAttr mdHeadingU64ArrayAttrI,
	U64ArraySec mdHeadingU64ArraySecI[U64ArrayAttr, Ent],
	U8ArrayAttr mdHeadingU8ArrayAttrI,
	U8ArraySec mdHeadingU8ArraySecI[U8ArrayAttr, Ent],
	StringArrayAttr mdHeadingStringArrayAttrI,
	StringArraySec mdHeadingStringArraySecI[StringArrayAttr, Ent],
	Ent any,
	DML mdHeadingEntityI[
		SymbolAttr, SymbolSec,
		ForeignKeyAttr, ForeignKeySec,
		BlobArrayAttr, BlobArraySec,
		U64ArrayAttr, U64ArraySec,
		U8ArrayAttr, U8ArraySec,
		StringArrayAttr, StringArraySec,
		Ent,
	],
](dml DML, row MdHeading) (err error) {
	// --- symbol. ---
	symbolSec := dml.GetSectionSymbol()
	err = mdHeadingEmitSectionSymbol(symbolSec, row)
	if err != nil {
		return
	}
	symbolSec.EndSection()
	// --- foreignKey. ---
	foreignKeySec := dml.GetSectionForeignKey()
	err = mdHeadingEmitSectionForeignKey(foreignKeySec, row)
	if err != nil {
		return
	}
	foreignKeySec.EndSection()
	// --- blobArray. ---
	blobArraySec := dml.GetSectionBlobArray()
	err = mdHeadingEmitSectionBlobArray(blobArraySec, row)
	if err != nil {
		return
	}
	blobArraySec.EndSection()
	// --- u64Array. ---
	u64ArraySec := dml.GetSectionU64Array()
	err = mdHeadingEmitSectionU64Array(u64ArraySec, row)
	if err != nil {
		return
	}
	u64ArraySec.EndSection()
	// --- u8Array. ---
	u8ArraySec := dml.GetSectionU8Array()
	err = mdHeadingEmitSectionU8Array(u8ArraySec, row)
	if err != nil {
		return
	}
	u8ArraySec.EndSection()
	// --- stringArray. ---
	stringArraySec := dml.GetSectionStringArray()
	err = mdHeadingEmitSectionStringArray(stringArraySec, row)
	if err != nil {
		return
	}
	stringArraySec.EndSection()
	return
}

// mdHeadingSymbolAttrsReadI is the Attributes-side view of the symbol section.
type mdHeadingSymbolAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) string
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdHeadingSymbolMembsReadI is the Memberships-side view of the symbol section.
type mdHeadingSymbolMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdHeadingForeignKeyAttrsReadI is the Attributes-side view of the foreignKey section.
type mdHeadingForeignKeyAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) uint64
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdHeadingForeignKeyMembsReadI is the Memberships-side view of the foreignKey section.
type mdHeadingForeignKeyMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdHeadingBlobArrayAttrsReadI is the Attributes-side view of the blobArray section.
type mdHeadingBlobArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) ([]byte, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdHeadingBlobArrayMembsReadI is the Memberships-side view of the blobArray section.
type mdHeadingBlobArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdHeadingU64ArrayAttrsReadI is the Attributes-side view of the u64Array section.
type mdHeadingU64ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint64, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdHeadingU64ArrayMembsReadI is the Memberships-side view of the u64Array section.
type mdHeadingU64ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdHeadingU8ArrayAttrsReadI is the Attributes-side view of the u8Array section.
type mdHeadingU8ArrayAttrsReadI interface {
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (uint8, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdHeadingU8ArrayMembsReadI is the Memberships-side view of the u8Array section.
type mdHeadingU8ArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdHeadingStringArrayAttrsReadI is the Attributes-side view of the stringArray section.
type mdHeadingStringArrayAttrsReadI interface {
	GetAttrValueValue(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[string]
	GetAttrValueSingle(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) (string, error)
	GetNumberOfAttributes(entityIdx raruntime.EntityIdx) int64
}

// mdHeadingStringArrayMembsReadI is the Memberships-side view of the stringArray section.
type mdHeadingStringArrayMembsReadI interface {
	GetMembValueLowCardRef(entityIdx raruntime.EntityIdx, attrIdx raruntime.AttributeIdx) iter.Seq[uint64]
}

// mdHeadingReadRow reads row i as one optional MdHeading component: presence-
// gated (a row carrying none of the kind's memberships yields
// present=false), membership-matched. A slot carrying more
// attributes than this kind's shape admits is an error, for every
// shape including containers. Plain-
// bound fields stay zero — the caller owns the envelope. The
// Attrs/Membs readers bind by type inference at the call site, as
// with FillFromArrow.
func mdHeadingReadRow[
	SymbolAttrs mdHeadingSymbolAttrsReadI,
	SymbolMembs mdHeadingSymbolMembsReadI,
	ForeignKeyAttrs mdHeadingForeignKeyAttrsReadI,
	ForeignKeyMembs mdHeadingForeignKeyMembsReadI,
	BlobArrayAttrs mdHeadingBlobArrayAttrsReadI,
	BlobArrayMembs mdHeadingBlobArrayMembsReadI,
	U64ArrayAttrs mdHeadingU64ArrayAttrsReadI,
	U64ArrayMembs mdHeadingU64ArrayMembsReadI,
	U8ArrayAttrs mdHeadingU8ArrayAttrsReadI,
	U8ArrayMembs mdHeadingU8ArrayMembsReadI,
	StringArrayAttrs mdHeadingStringArrayAttrsReadI,
	StringArrayMembs mdHeadingStringArrayMembsReadI,
](
	i int,
	symbolAttrs SymbolAttrs,
	symbolMembs SymbolMembs,
	foreignKeyAttrs ForeignKeyAttrs,
	foreignKeyMembs ForeignKeyMembs,
	blobArrayAttrs BlobArrayAttrs,
	blobArrayMembs BlobArrayMembs,
	u64ArrayAttrs U64ArrayAttrs,
	u64ArrayMembs U64ArrayMembs,
	u8ArrayAttrs U8ArrayAttrs,
	u8ArrayMembs U8ArrayMembs,
	stringArrayAttrs StringArrayAttrs,
	stringArrayMembs StringArrayMembs,
) (row MdHeading, present bool, err error) {
	// --- symbol. ---
	var symbolKindVal string
	var symbolKindCount int
	var symbolKindLastAttr int64
	nsymbol := symbolAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nsymbol; attrJ++ {
		for membID := range symbolMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdHeadingKind:
				if symbolKindLastAttr != attrJ+1 {
					symbolKindLastAttr = attrJ + 1
					symbolKindCount++
				}
				val := symbolAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				symbolKindVal = val
			}
		}
	}
	if symbolKindCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "symbol").Str("membership", "mdHeadingKind").Int("got", symbolKindCount).Errorf("slot symbol@mdHeadingKind (field Kind) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", symbolKindCount)
		return
	}
	if symbolKindCount == 1 {
		row.Kind = symbolKindVal
		present = true
	}
	// --- foreignKey. ---
	var foreignKeyDocVal uint64
	var foreignKeyDocCount int
	var foreignKeyDocLastAttr int64
	nforeignKey := foreignKeyAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nforeignKey; attrJ++ {
		for membID := range foreignKeyMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdHeadingDoc:
				if foreignKeyDocLastAttr != attrJ+1 {
					foreignKeyDocLastAttr = attrJ + 1
					foreignKeyDocCount++
				}
				val := foreignKeyAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				foreignKeyDocVal = val
			}
		}
	}
	if foreignKeyDocCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "foreignKey").Str("membership", "mdHeadingDoc").Int("got", foreignKeyDocCount).Errorf("slot foreignKey@mdHeadingDoc (field Doc) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", foreignKeyDocCount)
		return
	}
	if foreignKeyDocCount == 1 {
		row.Doc = foreignKeyDocVal
		present = true
	}
	// --- blobArray. ---
	var blobArrayDocHashVal []byte
	var blobArrayDocHashCount int
	var blobArrayDocHashLastAttr int64
	nblobArray := blobArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nblobArray; attrJ++ {
		for membID := range blobArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdHeadingDocHash:
				if blobArrayDocHashLastAttr != attrJ+1 {
					blobArrayDocHashLastAttr = attrJ + 1
					blobArrayDocHashCount++
				}
				val, valErr := blobArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "mdHeadingDocHash").Str("field", "DocHash").Errorf("slot blobArray@mdHeadingDocHash (field DocHash) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				cp := make([]byte, len(val))
				copy(cp, val)
				blobArrayDocHashVal = cp
			}
		}
	}
	if blobArrayDocHashCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "blobArray").Str("membership", "mdHeadingDocHash").Int("got", blobArrayDocHashCount).Errorf("slot blobArray@mdHeadingDocHash (field DocHash) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", blobArrayDocHashCount)
		return
	}
	if blobArrayDocHashCount == 1 {
		row.DocHash = blobArrayDocHashVal
		present = true
	}
	// --- u64Array. ---
	var u64ArrayOrdinalVal uint64
	var u64ArrayOrdinalCount int
	var u64ArrayOrdinalLastAttr int64
	var u64ArrayLineVal uint64
	var u64ArrayLineCount int
	var u64ArrayLineLastAttr int64
	var u64ArrayParentVal uint64
	var u64ArrayParentCount int
	var u64ArrayParentLastAttr int64
	nu64Array := u64ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu64Array; attrJ++ {
		for membID := range u64ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdHeadingOrdinal:
				if u64ArrayOrdinalLastAttr != attrJ+1 {
					u64ArrayOrdinalLastAttr = attrJ + 1
					u64ArrayOrdinalCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdHeadingOrdinal").Str("field", "Ordinal").Errorf("slot u64Array@mdHeadingOrdinal (field Ordinal) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayOrdinalVal = val
			case kindMdHeadingLine:
				if u64ArrayLineLastAttr != attrJ+1 {
					u64ArrayLineLastAttr = attrJ + 1
					u64ArrayLineCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdHeadingLine").Str("field", "Line").Errorf("slot u64Array@mdHeadingLine (field Line) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayLineVal = val
			case kindMdHeadingParent:
				if u64ArrayParentLastAttr != attrJ+1 {
					u64ArrayParentLastAttr = attrJ + 1
					u64ArrayParentCount++
				}
				val, valErr := u64ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdHeadingParent").Str("field", "Parent").Errorf("slot u64Array@mdHeadingParent (field Parent) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u64ArrayParentVal = val
			}
		}
	}
	if u64ArrayOrdinalCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdHeadingOrdinal").Int("got", u64ArrayOrdinalCount).Errorf("slot u64Array@mdHeadingOrdinal (field Ordinal) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayOrdinalCount)
		return
	}
	if u64ArrayOrdinalCount == 1 {
		row.Ordinal = u64ArrayOrdinalVal
		present = true
	}
	if u64ArrayLineCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdHeadingLine").Int("got", u64ArrayLineCount).Errorf("slot u64Array@mdHeadingLine (field Line) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayLineCount)
		return
	}
	if u64ArrayLineCount == 1 {
		row.Line = u64ArrayLineVal
		present = true
	}
	if u64ArrayParentCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u64Array").Str("membership", "mdHeadingParent").Int("got", u64ArrayParentCount).Errorf("slot u64Array@mdHeadingParent (field Parent) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u64ArrayParentCount)
		return
	}
	if u64ArrayParentCount == 1 {
		row.Parent.Val = u64ArrayParentVal
		row.Parent.Has = true
		present = true
	}
	// --- u8Array. ---
	var u8ArrayLevelVal uint8
	var u8ArrayLevelCount int
	var u8ArrayLevelLastAttr int64
	nu8Array := u8ArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nu8Array; attrJ++ {
		for membID := range u8ArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdHeadingLevel:
				if u8ArrayLevelLastAttr != attrJ+1 {
					u8ArrayLevelLastAttr = attrJ + 1
					u8ArrayLevelCount++
				}
				val, valErr := u8ArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "u8Array").Str("membership", "mdHeadingLevel").Str("field", "Level").Errorf("slot u8Array@mdHeadingLevel (field Level) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				u8ArrayLevelVal = val
			}
		}
	}
	if u8ArrayLevelCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "u8Array").Str("membership", "mdHeadingLevel").Int("got", u8ArrayLevelCount).Errorf("slot u8Array@mdHeadingLevel (field Level) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", u8ArrayLevelCount)
		return
	}
	if u8ArrayLevelCount == 1 {
		row.Level = u8ArrayLevelVal
		present = true
	}
	// --- stringArray. ---
	var stringArrayTextVal string
	var stringArrayTextCount int
	var stringArrayTextLastAttr int64
	var stringArraySlugVal string
	var stringArraySlugCount int
	var stringArraySlugLastAttr int64
	var stringArrayAnchorVal string
	var stringArrayAnchorCount int
	var stringArrayAnchorLastAttr int64
	var stringArrayPathSlice []string
	var stringArrayPathCount int
	var stringArrayPathLastAttr int64
	nstringArray := stringArrayAttrs.GetNumberOfAttributes(raruntime.EntityIdx(i))
	for attrJ := int64(0); attrJ < nstringArray; attrJ++ {
		for membID := range stringArrayMembs.GetMembValueLowCardRef(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
			switch membID {
			case kindMdHeadingText:
				if stringArrayTextLastAttr != attrJ+1 {
					stringArrayTextLastAttr = attrJ + 1
					stringArrayTextCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdHeadingText").Str("field", "Text").Errorf("slot stringArray@mdHeadingText (field Text) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayTextVal = val
			case kindMdHeadingSlug:
				if stringArraySlugLastAttr != attrJ+1 {
					stringArraySlugLastAttr = attrJ + 1
					stringArraySlugCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdHeadingSlug").Str("field", "Slug").Errorf("slot stringArray@mdHeadingSlug (field Slug) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArraySlugVal = val
			case kindMdHeadingAnchor:
				if stringArrayAnchorLastAttr != attrJ+1 {
					stringArrayAnchorLastAttr = attrJ + 1
					stringArrayAnchorCount++
				}
				val, valErr := stringArrayAttrs.GetAttrValueSingle(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ))
				if valErr != nil {
					err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdHeadingAnchor").Str("field", "Anchor").Errorf("slot stringArray@mdHeadingAnchor (field Anchor) has an attribute carrying other than one value, but the field's `,unit` shape admits exactly one: %w", valErr)
					return
				}
				stringArrayAnchorVal = val
			case kindMdHeadingPath:
				if stringArrayPathLastAttr != attrJ+1 {
					stringArrayPathLastAttr = attrJ + 1
					stringArrayPathCount++
				}
				for v := range stringArrayAttrs.GetAttrValueValue(raruntime.EntityIdx(i), raruntime.AttributeIdx(attrJ)) {
					stringArrayPathSlice = append(stringArrayPathSlice, v)
				}
			}
		}
	}
	if stringArrayTextCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdHeadingText").Int("got", stringArrayTextCount).Errorf("slot stringArray@mdHeadingText (field Text) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayTextCount)
		return
	}
	if stringArrayTextCount == 1 {
		row.Text = stringArrayTextVal
		present = true
	}
	if stringArraySlugCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdHeadingSlug").Int("got", stringArraySlugCount).Errorf("slot stringArray@mdHeadingSlug (field Slug) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArraySlugCount)
		return
	}
	if stringArraySlugCount == 1 {
		row.Slug = stringArraySlugVal
		present = true
	}
	if stringArrayAnchorCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdHeadingAnchor").Int("got", stringArrayAnchorCount).Errorf("slot stringArray@mdHeadingAnchor (field Anchor) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayAnchorCount)
		return
	}
	if stringArrayAnchorCount == 1 {
		row.Anchor.Val = stringArrayAnchorVal
		row.Anchor.Has = true
		present = true
	}
	if stringArrayPathCount > 1 {
		err = eb.Build().Int("row", i).Str("section", "stringArray").Str("membership", "mdHeadingPath").Int("got", stringArrayPathCount).Errorf("slot stringArray@mdHeadingPath (field Path) carries %d attributes but the DTO admits at most 1 — several producers claim this slot, so the reader cannot tell which attribute is this kind's", stringArrayPathCount)
		return
	}
	if stringArrayPathSlice != nil {
		row.Path = stringArrayPathSlice
		present = true
	}
	return
}
